# 향해 Project

## Project Context

이 저장소는 SSAFY 6인 팀이 진행하는 특화 프로젝트 **'향해'**이다.

향해는 단순한 향수 검색·구매 서비스가 아니라 사용자가 여러 향을 탐색하면서 자신의 취향을 발견할 수 있도록 만드는 향수 탐색 서비스다.

서비스의 주요 컨셉은 **바다와 항해**이며, 사용자가 향의 세계를 탐험한다는 경험을 중심으로 설계한다.

향수는 구매 빈도가 높은 상품이 아니라 사치재·기호재의 성격이 강하다는 점을 기본 전제로 한다. 따라서 사용자가 많은 향수를 보유하거나 지속적으로 구매한다고 가정해서 기능을 설계하지 않는다.

사용자가 향수를 구매하지 않는 기간에도 새로운 향을 탐색하고, 자신의 취향을 발견하고, 다른 사용자의 향에 대한 표현과 경험을 접할 수 있는 서비스를 지향한다.

## Core Product Direction

현재 서비스의 주요 기능에는 다음 영역이 포함된다.

- 향 지도 기반 향수 탐색
- 자연어 기반 향수 추천
- 향수 상세 정보
- 향 관련 커뮤니티
- 관심 향수 및 개인 취향 관리

기존 요구사항이나 팀에서 이미 결정한 기능을 구현 편의를 위해 임의로 변경하지 않는다.

코드와 현재 기획이 충돌하거나 요구사항이 불분명하다면 임의로 비즈니스 규칙을 만들어 구현하지 말고 충돌 또는 불확실한 부분을 먼저 설명한다.

## Scent Map

향 지도는 향해의 핵심 탐색 기능 중 하나다.

향수를 단순히 화면에 시각적으로 배치하는 것이 목적이 아니라, 향수의 향 특성과 향수 사이의 관계를 사용자가 공간적으로 탐색할 수 있도록 만드는 것이 목적이다.

지도에서 가까운 향수에는 데이터 관점에서도 가까이 배치할 이유가 있어야 한다.

사용자가 특정 향수 이름을 알지 못하더라도 지도를 탐색하면서 새로운 향수를 발견하고, 주변의 비슷한 향수로 탐색을 확장할 수 있어야 한다.

현재 약 200개의 대표 향수를 향 지도에서 보여주는 방향을 생각하고 있다.

현재 단계에서는 주요 향 계열 내부에서 향수 사이의 관계를 표현하는 것을 우선한다. 서로 다른 모든 향 계열을 하나의 완벽한 좌표 체계로 연결하는 것은 현재 필수 요구사항이 아니다.

향수의 유사성을 어떻게 정의할지, 어떤 알고리즘으로 좌표를 생성할지, 대표 향수를 어떻게 선정할지는 실제 데이터 분석과 실험을 통해 결정한다.

특정 알고리즘을 사용하기 위해 향 지도의 문제 정의를 바꾸지 않는다.

## Perfume Data

주요 향수 데이터 원천으로 Fragrantica 기반 데이터셋을 사용한다.

이 데이터에는 향수 이름과 브랜드뿐 아니라 향수의 향 특성을 분석하는 데 사용할 수 있는 accords, notes, 사용자 평가 등의 정보가 포함될 수 있다.

다만 실제 작업에서는 데이터셋이나 코드에 존재하는 값을 직접 확인한 뒤 판단한다.

- 데이터 컬럼이 존재한다고 추측하지 않는다.
- 컬럼 이름만 보고 의미를 임의로 해석하지 않는다.
- 실제 값과 분포를 확인하기 전에 데이터 활용 방식을 확정하지 않는다.
- 모든 향수가 동일한 수준의 정보를 가진다고 가정하지 않는다.
- 결측치와 데이터 품질을 확인한다.
- 원본 데이터는 임의로 수정하거나 덮어쓰지 않는다.

향수를 단순히 하나의 카테고리에 속한 제품으로만 보지 않는다. 하나의 향수는 여러 향 특성을 동시에 가질 수 있다는 점을 고려한다.

## AI / Data Development

AI 또는 데이터 기반 기능에서는 기술을 사용하는 것 자체를 성과로 간주하지 않는다.

새로운 모델, 알고리즘, 검색 방식 또는 데이터 처리 방식을 제안할 때는 먼저 이 프로젝트의 어떤 문제를 해결하는지 확인한다.

가능하다면 새로운 방법을 적용하기 전에 비교 가능한 기존 방식인 Baseline을 확보한다.

의미 있는 비교가 가능한 작업에서는 다음을 고려한다.

- 변경 전 상태
- 평가 지표
- 실험 조건
- 사용 데이터
- 변경 후 결과
- Trade-off

측정하지 않은 개선을 실제 개선이라고 단정하지 않는다.

실제로 측정하지 않은 정확도, 성능, 응답 시간, 추천 품질 등의 숫자를 만들어내지 않는다.

한 번의 결과만으로 결론을 내리기보다 작업 특성상 가능하다면 반복 측정이나 동일 조건 비교를 사용한다.

다만 모든 기능을 억지로 수치화하지 않는다. 실제 프로젝트 품질이나 의사결정에 의미가 있는 지표를 우선한다.

## Development Rules

작업을 시작하기 전에 현재 repository 구조와 관련 구현을 먼저 조사한다.

새로운 파일이나 구조를 만들기 전에 기존 구현을 재사용할 수 있는지 확인한다.

기존 구조로 충분히 해결할 수 있다면 불필요하게 새로운 계층, abstraction, utility 또는 framework를 만들지 않는다.

요청 범위 밖의 코드를 정리한다는 이유로 임의로 리팩터링하지 않는다.

특정 기능을 구현하면서 발견한 별개의 문제는 현재 작업에 반드시 필요한 경우가 아니라면 함께 수정하지 않는다.

기획 단계에서 아직 확정하지 않은 내용을 코드 구현 과정에서 편의를 위해 임의로 결정하지 않는다.

프로젝트 규모와 기간, 6인 팀이 유지보수할 수 있는 수준을 고려한다.

복잡한 방법보다 단순한 방법으로 같은 목적을 달성할 수 있다면 단순한 방법부터 검토한다.

## Exploration and Planning

복잡한 기능이나 데이터 작업은 바로 구현하기보다 먼저 현재 코드, 데이터, 관련 설정을 확인한다.

아직 최적의 접근 방법 자체를 결정하지 않은 작업에서는 사용자가 제시한 목적과 제약을 기준으로 여러 접근법을 검토할 수 있다.

사람이 구현 세부 절차를 지정하지 않았다면 정해진 절차를 억지로 만들지 않는다.

특히 향 지도처럼 아직 구현 방법을 결정하는 단계의 작업에서는 실제 데이터와 기존 프로젝트 구조를 조사한 뒤 가능한 방법과 Trade-off를 설명하고 구현 방향을 함께 결정한다.

사용자가 분석이나 계획만 요청했다면 명시적인 구현 요청 전까지 소스 코드를 수정하지 않는다.

## Verification

기능을 구현한 뒤 가능한 경우 실제 실행 또는 테스트로 동작을 확인한다.

데이터 처리 결과는 가능하면 입력 데이터 수, 출력 데이터 수, 오류 또는 제외된 데이터 등을 확인한다.

AI 또는 추천 관련 변경은 "결과가 그럴듯하다"는 이유만으로 성공했다고 판단하지 않는다.

테스트하지 못했거나 확인하지 못한 부분은 완료된 것처럼 표현하지 않는다.

실험 과정에서 기대한 개선이 나타나지 않아도 결과를 숨기거나 실제보다 좋게 표현하지 않는다.

## Team Project Safety

팀원이 작성한 기존 코드를 불필요하게 변경하지 않는다.

현재 작업과 직접 관계없는 다른 팀원의 기능을 임의로 수정하지 않는다.

대규모 파일 이동, 삭제, 데이터 삭제, dependency 변경 등 영향 범위가 큰 작업은 특별히 주의한다.

원본 데이터, 팀 공용 설정, 환경 설정을 임의로 덮어쓰지 않는다.

보안 정보, API key, credential 등의 값을 코드에 직접 작성하지 않는다.

기존 convention이 repository에 존재한다면 새로운 convention을 만들기보다 기존 convention을 따른다.

## Documentation and Decisions

코드만 보고 명확히 알 수 있는 내용을 불필요하게 별도 문서로 반복 생성하지 않는다.

일회성 분석 결과를 위해 새로운 Markdown 또는 CSV 파일을 무분별하게 만들지 않는다.

후속 개발이나 의사결정에서 실제로 다시 사용할 가치가 있는 산출물인지 판단한 뒤 파일을 생성한다.

중요한 기술적 의사결정이나 실험 결과를 기록해야 한다면 무엇을 선택했는지만 기록하지 말고 가능한 경우 다음 내용을 함께 남긴다.

문제 → 검토한 방법 → 결정 → 근거 → 결과 → Trade-off

실패하거나 사용하지 않기로 한 실험도 이후 의사결정에 의미가 있다면 보존한다.

## Final Principle

이 프로젝트에서는 기능을 많이 만드는 것보다 사용자의 향 탐색 경험을 실제로 개선하는 것을 우선한다.

기술 선택과 구현은 프로젝트의 목적을 위한 수단이다.

프로젝트를 먼저 이해하고, 실제 데이터와 기존 구현을 확인하고, 가장 단순하고 검증 가능한 방법으로 문제를 해결한다.
