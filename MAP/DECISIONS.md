# 향 지도 의사결정 기록

형식: 문제 → 검토한 방법 → 결정 → 근거 → 결과 → Trade-off

모든 수치는 실제 실행 출력이다. 재현 방법은 각 항목에 적었다.

---

## D1. 향수 간 유사도를 어떻게 정의할 것인가

**문제** — 향 지도는 "가까이 배치할 데이터적 이유"가 필요하다. 유사도를 새로 설계해야 하는가?

**검토한 방법**
1. 기존 EDA 04~06의 Base Similarity 재사용
2. description 텍스트 임베딩
3. `reminds_me_of` 그래프 자체를 거리로 사용

**결정** — (1) 재사용. `sim = 0.5·cosine(accord strength, L2) + 0.5·IDF 가중 Jaccard(note 합집합)`

**근거**
- EDA 04~09가 이미 `reminds_me_of`를 정답으로 삼아 향수-향수 유사도를 측정 완료했다. 향 지도가 새로 푸는 문제는 유사도가 아니라 **2D 투영**이다.
- (3)은 정답을 입력으로 쓰는 것이라 평가가 불가능해진다. 게다가 top1000 중 신뢰 간선 보유 노드가 451개뿐이라 나머지는 배치할 수 없다.
- (2)는 description 품질이 미검증이고 별도 검증 절차가 필요해 2주 안에 무리다.

**결과** — `verify_similarity.py`로 EDA 04의 Development 700 평가를 재현했다. **소수점 4자리까지 일치**했다.

| 모델 | recall@10 | ndcg@10 | EDA 04 기록 |
|---|---|---|---|
| Accord Cosine | 0.1868 | 0.1688 | 0.1688 (차이 0.0000) |
| Note Jaccard | 0.2545 | 0.2345 | 0.2345 (차이 0.0000) |
| Accord + Note | 0.2900 | 0.2668 | 0.2668 (차이 0.0000) |
| **Accord + IDF Note** (채택) | **0.3010** | **0.2709** | 04에 없음 (05에서 채택) |

query 수(12,150 / 3,835), vocab(92 / 2,523), nnz(1,000,570 / 1,091,811) 전부 일치.

**Trade-off** — accord는 향수당 8개 상한이 있어 표현력에 천장이 있다. note는 표층 문자열 기반이라 의미적 유사성(예: 서로 다른 이름의 같은 원료)을 못 잡는다.

---

## D2. 2D 배치 방법 — MDS에서 UMAP으로 뒤집음

**문제** — 1000×1000 거리행렬을 2D로 어떻게 떨어뜨릴 것인가.

**검토한 방법** — Metric MDS / UMAP / t-SNE / PCA

**최초 판단** — MDS. 이유는 (a) 목적함수가 "거리 보존" 자체라 제품 주장과 일치, (b) `sklearn` 기설치로 의존성 추가 불필요, (c) stress가 해석 가능.

**뒤집은 계기** — 의존성 논거가 약하다는 지적을 받고 실제 거리행렬을 측정했다.

| 측정 항목 | 값 |
|---|---|
| pairwise 거리 평균 / 표준편차 | 0.824 / 0.107 |
| 1번째 최근접 이웃 평균 거리 | 0.466 |
| 20번째 최근접 이웃 평균 거리 | 0.596 |
| 중앙값 파트너 거리 | 0.837 |
| 국소 신호 구간(1~20위)에 드는 pair 비율 | **2.77%** |
| 그 pair가 차지하는 제곱거리 합(MDS 목적함수 질량) | **1.24%** |

→ MDS는 최적화 노력의 **98.8%를 정보 없는 pair**에 쓴다. 고차원 희소 데이터의 거리 집중 현상이다.

성분을 분리하면 실패 방식까지 예측됐다.

| 성분 | 거리 평균 | 표준편차 | d<0.8 비율 |
|---|---|---|---|
| accord cosine | 0.694 | 0.195 | 65.6% |
| IDF note Jaccard | 0.954 | 0.048 | **1.1%** |

note는 거의 전부 "멀다"에 포화. 그런데 EDA 측정상 **더 강한 신호가 note**(ndcg 0.2345 vs 0.1688). MDS는 퍼짐이 큰 accord에 지배되고 note 구조를 버릴 것으로 예측했다.

**결정** — UMAP (`metric='precomputed'`, `n_neighbors=10`, `min_dist=0.1`, `random_state=42`)

**추가 근거** — CLAUDE.md가 "계열 내부 관계 우선, 전 계열 통합 좌표계는 필수 아님"이라고 명시한다. 전역 좌표 일관성이 MDS의 유일한 강점인데 그게 요구사항이 아니었다.

**결과** (`results/layout_comparison.csv`)

| layout | trust@10 | trust@20 | knn_overlap@10 | reminds_pct |
|---|---|---|---|---|
| random | 0.5020 | 0.5045 | 0.0064 | 0.5132 |
| pca | 0.7650 | 0.7663 | 0.0812 | 0.1964 |
| mds | 0.7747 | 0.7767 | 0.0934 | 0.2422 |
| **umap nn=10 md=0.1** | **0.9411** | **0.9208** | **0.3874** | 0.1738 |
| umap nn=10 md=0.3 | 0.9122 | 0.8987 | 0.3468 | 0.1711 |
| umap nn=15 md=0.1 | 0.9369 | 0.9232 | 0.3731 | 0.1804 |
| umap nn=15 md=0.3 | 0.9109 | 0.8965 | 0.3285 | 0.1658 |
| umap nn=30 md=0.1 | 0.9274 | 0.9179 | 0.3403 | 0.1658 |
| umap nn=30 md=0.3 | 0.9016 | 0.8950 | 0.2959 | 0.1606 |

- **예측이 맞았다.** MDS는 kNN overlap 0.0934로 PCA(0.0812)를 겨우 넘고, `reminds_pct`는 오히려 PCA보다 **나쁘다**(0.2422 vs 0.1964). UMAP은 kNN overlap에서 MDS의 **4.1배**.
- `n_neighbors` 3개 값에서 지표가 일관됐다(trust@10 0.90~0.94). 단일 설정의 우연이 아니다.
- random·PCA 대비 세 지표 모두 우위.

**Trade-off** — "지도상 거리 = 유사도"라고 말할 수 없게 된다. UMAP 좌표는 거리 척도가 아니다. 대신 정확한 답은 고차원 기준 `neighbors` 배열이 담당하도록 설계했고, JSON에 `distance_is_metric: false`를 명시했다. 다만 위 측정대로 MDS로도 이 데이터에선 낮은 stress를 얻을 수 없으므로 실질 손실은 없다.

---

## D3. 지도에 올릴 향수 선정

**문제** — 어떤 향수를 지도에 올릴 것인가. 기획 초안은 "대표 향수 약 200개".

**검토한 방법** — 인기순 top-N / accord 계열별 할당

**결정** — 화면 200개 + 좌표 1000개, 계열(dominant accord)별 **제곱근 비례 할당**(하한 5 / 상한 60), 후보 풀 `people >= 500`. 화면 200개도 같은 규칙.

**근거 1 — 200개로는 검증이 불가능하다** (규모별 집합 내부 정답 간선 측정)

| 집합 | 내부 간선 | 신뢰 간선 | 신뢰 간선 보유 노드 |
|---|---|---|---|
| 인기 top200 | 203 | 94 | **70 / 200** |
| 인기 top1000 | 2,425 | 943 | 483 / 1000 |
| 인기 top2000 | 7,393 | 2,611 | 1,145 / 2000 |

**근거 2 — 인기순은 편향돼 있다** (`results/selection_comparison.csv`)

| 지표 | A. 인기순 top1000 | B. 계열별 할당 |
|---|---|---|
| dominant accord 커버리지 | 43종 | **58종 (+35%)** |
| 브랜드 수 | 179 | **219** |
| 상위 10개 브랜드 점유율 | 29.2% | **25.5%** |
| vote_count 중앙값 | 5,695 | 5,518 |
| 집합내 reminds 간선 | 2,425 | 2,367 (**−2.4%**) |
| 신뢰 간선 | 943 | 896 (**−5.0%**) |

**결과** — 계열 커버리지 +35%와 브랜드 다양성을 정답 간선 5% 손실로 얻었다. 절대량(신뢰 간선 896개)이 검증에 충분하다.

**Trade-off** — 최소 투표수가 3,403 → 510으로 내려간다. "사용자가 아는 향수" 전제가 약간 약해진다. 후보 풀을 `people>=200`까지 넓히면 계열이 66종으로 늘지만 신뢰 간선이 883으로 더 줄고 최소 투표수가 203까지 떨어져 채택하지 않았다.

---

## D4. 클러스터 개수와 라벨

**문제** — 프론트가 "향 계열 영역"을 그리려면 라벨이 필요하다. 몇 개로, 어떤 형태로?

**검토한 방법** — k = 8 / 12 / 16을 모두 실행해 accord 구성표를 비교

**결정** — **k=12**, 라벨은 **상위 accord 3개**만 내보냄

**근거**

| k | 최대 클러스터 | 문제 |
|---|---|---|
| 8 | 411 (41%) | 한 영역이 지도의 41% — "이 영역이 무슨 향인가"에 답이 안 됨 |
| **12** | **230 (23%)** | 411을 woody·warm spicy(183)와 aromatic·citrus(226)로 분리 |
| 16 | 230 (23%) | leather·rose·green을 더 갈라주지만 색 구분 한계를 넘고 2~3개짜리 클러스터 증가 |

라벨을 한국어 이름 대신 accord 구성으로 둔 이유는 CLAUDE.md의 "근거 없는 scent inference 금지"다. `["woody","warm spicy","amber"]`는 데이터에서 직접 나온 사실이라 검증 가능하고, 사용자에게 보일 한국어 이름은 기획이 나중에 붙일 수 있다.

**결과** — 클러스터가 2D에서 실제로 영역을 이룬다.

- 2D 최근접 10개 중 같은 클러스터 비율 **0.748** vs 무작위 기대값 **0.171** (4.4배)
- 큰 클러스터들의 응집도: aromatic·woody·citrus 0.824 / sweet·vanilla·fruity 0.809 / white floral 0.777 / floral·fresh·rose 0.743 / woody·warm spicy 0.728

**Trade-off** — n=2~3짜리 클러스터가 3개 생기고 응집도가 낮다(c7 0.100, c10 0.000, c11 0.164). average linkage의 특성이다. UI에서는 무시하거나 인접 클러스터에 병합하는 처리가 필요할 수 있다.

---

## D5. 좌표 정규화

**문제** — 프론트에 x, y를 어떤 범위로 넘길 것인가.

**결정** — [0,1] 정규화, 단 **x·y에 같은 배율**

**근거** — 축별로 따로 정규화하면 종횡비가 깨져 UMAP이 만든 거리 구조가 그대로 왜곡된다. 두 축 중 넓은 쪽 기준으로 같은 배율을 적용하고 좁은 축에 여백을 둔다.

**결과** — x폭 1.0000 / y폭 0.7242. 한쪽만 1.0을 채우는 것이 정상이다.

**Trade-off** — 프론트가 캔버스에 채울 때 여백 처리를 해야 한다.

---

## D6. `also_liked` 사용 범위

**문제** — `also_liked`는 top1000 내부 간선이 19,268개로 `reminds_me_of`(2,367)보다 8배 조밀하다. 쓸 것인가?

**결정** — **진단 지표로만** 측정. 합격 판정에 사용하지 않음.

**근거** — 투표가 없는 알고리즘 출력이라 "냄새가 닮았다"의 근거가 아니다. 인기·취향이 섞여 있다.

**결과** — 두 지표가 같은 방향으로 움직였다. `also_liked` 기준 거리 백분위: random 0.4876 → pca 0.3885 → umap 0.3706. `reminds_me_of` 기준과 순서가 일치해 결과 신뢰도를 보강한다.

**Trade-off** — 정답이 없는 향수 517개에 대해서는 여전히 검증 정보가 없다.

---

## D7. 실행 환경 분리

**문제** — `umap-learn`은 `numba`/`llvmlite`를 끌어온다. EDA venv는 numpy 2.4.6이고 numba는 보통 numpy 상한을 건다. 팀 공용 venv에서 numpy가 다운그레이드되면 기존 노트북 23개가 전부 영향을 받는다.

**결정** — `MAP/venv`를 별도로 생성.

**결과** — numba 0.67.0이 numpy 2.4.6을 지원해 **실제 충돌은 발생하지 않았다.** 설치 후 EDA venv가 numpy 2.4.6 / pandas 3.0.5 / sklearn 1.9.0으로 변경 없음을 확인했다.

**Trade-off** — 위험이 현실화되지 않았으므로 사후적으로는 불필요한 조치였다. 다만 실행 전에는 알 수 없었고, 되돌릴 수 없는 손상(팀 노트북 전체)에 비해 venv 하나의 비용이 작아 판단 자체는 유지한다.

---

## D8. `perfumes.csv`의 note 파싱 버그

**문제** — Step 1 검증에서 note vocab이 2,524로 EDA의 2,523보다 1개 많았다.

**원인** — `Eustoma | Lisianthus`라는 note 이름에 `|` 문자가 들어 있는데, 그게 `perfumes.csv`의 note 컬럼 구분자다. 이스케이프되지 않아 `Eustoma `와 ` Lisianthus` 둘로 쪼개졌다. **EDA 노트북들은 JSONL을 써서 영향이 없었다.**

**결정** — 원본 CSV는 건드리지 않고, 로더에서 EDA가 만든 정식 vocabulary(`10_note_dictionary.csv`, 2,523종)를 기준으로 되붙인다.

**결과** — vocab 2,523 / nnz 1,091,811로 EDA와 완전히 일치. 지표는 수정 전후 동일했다(영향 향수 1개).

**Trade-off** — 이 복원은 EDA가 만든 note 사전에 의존한다. 사전에 없는 신규 note가 `|`를 포함하면 다시 쪼개진다. 근본 해결은 CSV 대신 JSONL에서 note를 읽는 것이지만, 영향 범위가 향수 1개라 현재 규모에서는 과한 변경으로 판단했다.

---

## 최종 산출물

| 파일 | 용도 |
|---|---|
| `output/scent_map_v1.json` (0.70 MB) | **백엔드 전달 대상.** 1000개 좌표 + display 200 + 클러스터 + 고차원 기준 이웃 10개 |
| `output/diagnostic_edges.json` | 검증 뷰어 전용 |
| `results/layout_comparison.csv` | 배치 방법 9종 × 지표 5종 |
| `results/selection_comparison.csv` | 선정 방식 A/B |
| `map_preview.html` | 검증 뷰어 (전달 대상 아님) |

## 아직 하지 않은 것

- **Final Holdout 300 (PLAN.md Step 6)** — EDA 04에서 만들고 한 번도 쓰지 않은 자산. 모든 튜닝이 끝난 뒤 1회만 확인해야 하며, 보고 나서 튜닝하면 안 된다.
- 계열 정의(dominant accord)의 적절성 재검토. 58계열이 평균 2.76개 클러스터에 걸쳐 있고 17종은 1개 클러스터에만 있다. accord 1순위만으로 계열을 정하는 것이 거친 근사라는 점은 남아 있다.
