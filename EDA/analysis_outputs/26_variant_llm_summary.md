# 변형 표현 LLM Baseline

## 질문

같은 뜻의 한국어 향 표현을 다르게 쓰면, 사전 없이 LLM만으로 같은 accord에 도착하는가.

## 설계

- 팀 문서 `perfume_14families_korean_descriptors.md` 20-B / 19-B / 19-D 장에서 변형어 21쌍을 목적 표집
- **표준형과 변형형을 서로 다른 요청으로 분리** — 같은 요청에 넣으면 모델이 억지로 일관되게 답할 수 있다
- 정답을 사람이 정하지 않고 두 답이 같은지만 본다
- Model `gpt-5.4-nano` / Prompt `variant_accord_v1` / SHA256 `243c87037e965910...`

## 결과

| 지표 | 값 |
|---|---|
| accord 집합 완전 일치 | 2/21 (10%) |
| 교집합 존재 | 11/21 (52%) |
| 1순위 accord 일치 | 7/21 (33%) |
| AND 검색 Jaccard 중앙값 | 0.8% |
| 점수 검색 TOP50 겹침 중앙값 | 0.0% |
| TOP50에서 전혀 겹치지 않는 쌍 | 15/21 |
| 허용 목록 밖 accord 생성 | 3종 ['dry', 'rain', 'sharp'] |

## 유형별

| 유형 | 쌍 | 완전 일치 | 1순위 일치 |
|---|---|---|---|
| 문화적 비유 | 7 | 0% | 0% |
| 방언·의태어 | 5 | 0% | 0% |
| 외래어 파생 | 4 | 25% | 100% |
| 철자 변형 | 4 | 25% | 75% |
| 커뮤니티 은어 | 1 | 0% | 0% |

## 해석

LLM 단독으로는 같은 뜻의 표현을 같은 accord로 보내지 못한다.
accord 집합이 완전히 일치한 것은 2/21이고, 점수 기반 상위 50개에서
전혀 겹치지 않는 쌍이 15개다.
즉 같은 취향을 다르게 표현한 두 사용자가 서로 다른 향수를 추천받는다.

유형별로 갈린다. **철자 변형과 외래어 파생은 처리하고, 방언·의태어와 커뮤니티 은어와
문화적 비유는 처리하지 못한다.** 후자 셋은 학습 데이터에 드문 한국 향수 커뮤니티 고유 어휘다.

허용 목록 92개를 프롬프트에 명시했음에도 목록 밖 이름을 만들었다.
출력을 JSON Schema enum으로 강제해야 한다.

## 한계

- 21쌍은 목적 표집이며 무작위 표본이 아니다. 이 비율을 전체 성능으로 일반화하지 않는다
- 단일 실행이다. 반복 측정이나 온도 변화 실험을 하지 않았다
- 모델 하나만 측정했다
- "완전 일치"는 엄격한 기준이므로 부분 일치와 검색 겹침을 함께 보고했다
- 1순위 비교는 응답 순서에 의존한다. `상큼한`/`상콤한`은 집합이 같은데 순서가 달라 1순위로는 불일치가 됐다
