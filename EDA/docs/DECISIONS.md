# 자연어 추천 의사결정 기록

형식: 문제 → 검토한 방법 → 결정 → 근거 → 결과 → Trade-off

모든 수치는 실제 실행 출력이다. 재현 방법은 각 항목에 적었다.

번호는 `N`(자연어) 접두사를 쓴다. 향 지도 기록(`MAP/docs/DECISIONS.md`)의 `D` 번호와 구분하기 위해서다.

---

## N1. 향 사전에서 방언·커뮤니티 은어를 후순위로 둔다

**문제** — 한국어 향 표현을 Fragrantica accord로 매핑하는 사전을 만들 때, 방언(`쿰쿰한`/`쓸쿰한`)과
커뮤니티 은어(`화플`), 문화적 비유(`절간 냄새`)를 초기 범위에 넣어야 하는가.

팀 문서 `perfume_14families_korean_descriptors.md`의 19장·20-B장에 이미 목록이 정리돼 있고,
LLM 실험에서 이 세 유형의 처리 실패율이 100%로 나왔다. 그대로 보면 사전이 반드시 담아야 한다.

**검토한 방법**

1. 팀 문서의 방언·은어·비유를 전부 초기 사전에 포함한다
2. 실사용 빈도를 먼저 재고 그 결과로 우선순위를 정한다
3. LLM에 전적으로 맡기고 사전에서 제외한다

**결정** — (2)를 택했고, 측정 결과에 따라 다음과 같이 정했다.

- **방언은 `달달` 1항목만 초기 사전에 포함한다**
- **커뮤니티 은어와 문화적 비유는 후순위로 둔다**
- 목록을 삭제하지 않는다. 미매칭 로그를 통한 승격 경로를 남긴다
- 대신 **향수 전문 용어(노트·탑·베이스·잔향·지속력)를 최우선으로 올린다**

---

### 근거 1 — LLM 단독으로는 이 세 유형을 처리하지 못한다

노트북 26에서 팀 문서의 변형어 21쌍을 사전 없이 LLM에 넣고, 표준형과 변형형을
**서로 다른 요청**으로 물어 같은 accord로 가는지 측정했다.

| 유형 | 쌍 | 1순위 accord 일치 | 점수 검색 TOP50 겹침 |
|---|---:|---:|---:|
| 외래어 파생 (우디한, 파우더리한, 바닐라리한) | 4 | **100%** | 27.0% |
| 철자 변형 (달큰한, 달달한, 보송한, 상콤한) | 4 | **75%** | 25.0% |
| 방언·의태어 (쓸쿰한, 퀴퀴한, 쾨쾨한, 폭신한, 매쾌한) | 5 | **0%** | 0.8% |
| 문화적 비유 (절간 냄새, 쾌남 스킨, 에프킬라, 젖은 흙) | 7 | **0%** | 0.3% |
| 커뮤니티 은어 (화플) | 1 | **0%** | 0.0% |

전체로는 accord 집합 완전 일치 2/21(10%), 점수 검색 TOP50에서 전혀 겹치지 않는 쌍이 15/21이었다.
**이 근거만 보면 세 유형을 사전이 담아야 한다.**

### 근거 2 — 그러나 실사용에서는 거의 나오지 않는다

실제 사용자 설문 응답 155건에서 각 유형의 등장 빈도를 셌다.

| 유형 | golden 200 (팀원 작성) | **survey 155 (실사용자)** | survey 내역 |
|---|---:|---:|---|
| 방언·의태어 | 6 (3.0%) | **19 (12.3%)** | 달달×18, 알싸×1, 향긋×1 |
| 커뮤니티 은어 | 0 (0.0%) | **1 (0.6%)** | 아디파×1 |
| 문화적 비유 | 2 (1.0%) | **1 (0.6%)** | 목욕탕×1 |
| **향수 전문 용어** | 11 (5.5%) | **28 (18.1%)** | 잔향×11, 지속력×11, 노트×8, 베이스×6, 탑×4, 미들×3, 발향×2, 리니어×1 |

`golden 200`은 팀원 6명이 작성한 합성 데이터이고(Audit 시트 `source_author`), `survey 155`가
실제 일반 사용자 응답이다. 따라서 실사용 분포는 오른쪽 열로 읽어야 한다.

**결과**

- **커뮤니티 은어 1건, 문화적 비유 1건.** 합쳐서 155건 중 2건(1.3%)이다
- **방언 19건 중 18건이 `달달` 하나다.** 나머지는 `알싸` `향긋` 각 1건
- 즉 `달달` 한 항목이 방언 등장의 **94.7%(18/19)** 를 덮는다
- 반대로 **향수 전문 용어가 28건(18.1%)** 으로 가장 많다. LLM 실험에서 다루지 않은 유형이다

LLM이 0%로 실패하던 세 유형의 실사용 등장이 은어·비유 기준 1.3%인 반면,
사전이 다루지 않던 전문 용어가 18.1%다. **우선순위가 뒤바뀐다.**

---

**Trade-off**

- 후순위로 두면 실제로 방언·은어를 쓰는 소수 사용자는 매핑을 받지 못한다.
  미매칭 로그로 감지해 사후에 보완하는 것으로 대신한다. 즉 **누락을 없애는 대신 늦게 발견하기로** 했다.
- `향긋`은 방언 검색에 걸렸지만 사전에 넣지 않는다. 표준국어대사전 정의가
  "은근히 향기로운 느낌이 있다"로 묘사 내용이 없는 순수 평가어다. `NO_MAPPING`으로 분류한다.
- 팀 문서의 방언·은어 목록을 만드는 데 든 조사 비용이 이번 범위에서는 회수되지 않는다.
  다만 목록 자체가 이 측정의 검색어가 됐으므로 전량 낭비는 아니다.

---

**한계 — 이 근거로 말할 수 있는 것과 없는 것**

1. **응답자 구성을 모른다.** 설문 원본(`data/survey/raw/향수 설문조사.xlsx`)의 컬럼은
   타임스탬프·이메일·자유응답 3문항·객관식 2문항뿐이고 **나이·지역·성별 문항이 없다.**
   방언 사용은 지역·연령과 상관이 있는데 이를 통제하거나 확인할 수 없다.
   따라서 "한국인 일반이 방언을 안 쓴다"가 아니라 **"이 설문 응답자 155명은 거의 쓰지 않았다"** 까지만 말할 수 있다.
2. **표본 155건은 작다.** 1% 수준의 빈도를 추정하기에 특히 부족하다.
3. **검색어가 팀 문서에서 왔다.** 19장·20-B장에 없는 방언·은어는 애초에 검색되지 않았다.
   빈도가 낮게 나온 데에는 검색어 목록의 한계도 섞여 있다.
4. **한 시점의 설문이다** (2026년 8월). 유행어는 시간에 따라 바뀐다.
5. **다만 입력 방식이 서비스와 같다.** 설문도 우리 서비스도 텍스트 입력이다.
   사람은 말할 때보다 글로 쓸 때 표준어에 가깝게 쓰는데, 그 조건이 측정과 배포에서 동일하다.
   이 점은 근거를 약화시키지 않고 오히려 강화한다.

---

**발표 대응**

Q. *"방언이나 커뮤니티 은어를 쓰는 사용자는 어떻게 하나요?"*

세 단계로 답한다.

1. **측정했다.** 실사용 설문 155건에서 커뮤니티 은어 1건(0.6%), 문화적 비유 1건(0.6%)이었다.
   방언은 19건이지만 그중 18건이 `달달` 하나여서 사전 1항목으로 94.7%가 해결된다.
2. **버린 것이 아니라 후순위다.** 목록은 팀 문서에 남아 있고, 미매칭 로그에 3회 이상 쌓이면
   사전으로 승격하는 경로를 설계에 넣었다.
3. **한계를 안다.** 설문에 인구통계 문항이 없어 응답자의 지역·연령 편중 가능성을 배제할 수 없다.
   그래서 "한국인 일반"이 아니라 "이 응답자 집단"으로 범위를 좁혀 말한다.

Q. *"그러면 사전은 무엇을 담나요?"*

측정으로 정해진 우선순위는 다음과 같다.

| 순위 | 담을 것 | 근거 |
|---|---|---|
| 1 | 향수 전문 용어 (노트·탑·베이스·잔향·지속력) | 실사용 18.1%. Fragrantica 스키마와 직접 대응해 근거가 명확 |
| 2 | 코퍼스 지지도 (foresty는 향수 1개) | LLM이 구조적으로 알 수 없는 우리 데이터 사실 |
| 3 | 한국어 드리프트 (머스크 ≠ musky 단독) | 코퍼스 31% 중 의도한 의미는 1.0% |
| 4 | 문맥 분기 (깨끗한 = 비누냐 물이냐) | 설명 가능성 요구 |
| 5 | 매핑 금지 (향긋한에는 accord를 붙이지 않음) | 근거 없는 추론 방지 |
| 6 | 방언 `달달` 1항목 | 방언 등장의 94.7% |
| 7 | 은어·비유 | 실사용 1.3%. 로그 기반 승격 |

---

**재현 방법**

```bash
cd EDA
# 근거 1 — LLM 유형별 처리 (API 호출 없음, 저장된 응답 재채점)
#   26_variant_expression_llm_baseline.ipynb 를 FORCE_RERUN=False 로 실행
#   출력: analysis_outputs/26_variant_pair_results.csv
#         analysis_outputs/26_variant_retrieval_overlap.csv

# 근거 2 — 실사용 빈도
./venv/Scripts/python.exe - <<'PY'
import pandas as pd
g = list(pd.read_excel('evaluation_data/stage1/13_stage1_golden_set_v1_200.xlsx',
        sheet_name='Golden Set', header=4, dtype=str, keep_default_na=False).query_text)
s = list(pd.read_csv('data/survey/processed/survey_nlp_queries_candidates.csv',
        keep_default_na=False).query_text)
GROUPS = {
 "방언·의태어": ["쿰쿰","쓸쿰","퀴퀴","쾨쾨","폭닥","매캐","매쾌","달달","달큰","상콤","뽀송",
              "보송","꿉꿉","텁텁","녹진","알싸","향긋","구수","시큼"],
 "커뮤니티 은어": ["화플","그리너리","바닐라리","커정","아디파","위분","텐텐","향린이","향알못","향찔이","냐샹기"],
 "문화적 비유":  ["절간","절 냄새","쾌남","에프킬라","바버샵","바버숍","목욕탕","아저씨 스킨",
              "아빠 스킨","성당","사찰","젖은 흙","비 온 뒤 숲"],
 "향수 전문용어": ["노트","탑","미들","베이스","리니어","시향","잔향","발향","지속력","확산","실라지","부향률","오드"],
}
for name, terms in GROUPS.items():
    for lab, T in [("golden 200", g), ("survey 155", s)]:
        n = sum(1 for x in T if any(t in x for t in terms))
        print(f"{name:8s} {lab:12s} {n:3d} ({n/len(T):.1%})")
PY
```

**관련 자료**

- `26_variant_expression_llm_baseline.ipynb` — 근거 1
- `docs/nlr_engineering_notes.md` — 측정 기록
- `docs/spec.md` §4.3 — 사전 스키마
- `data/scent_knowledge/source/perfume_14families_korean_descriptors.md` 19장·20-B장 — 검색어 출처
- `data/survey/processed/README.md` — 설문 데이터 수집 조건

---

## N2. 향 사전은 표현을 accord 조합으로 매핑한다 — 단독 매핑을 금지한다

- 날짜: 2026-09-10
- 상태: **제안** — 근거는 실측이지만 팀 승인 전
- **수정됨 — [N10](#n10-직역-대응은-단독-매핑을-허용한다--n2가-비워-둔-임계값을-채운다)(2026-09-14)이 「한계」 5번의 빈칸을 채웠다. 표현이 accord 이름의 직역이면 단독 매핑을 허용한다.**

**문제** — 사전을 쓰기 전에 두 가지를 확정해야 했다.

1. **accord 92개로 사용자 표현을 담을 수 있는가.** 담을 수 없다면 사전이 accord만 겨냥하는
   현재 설계(`spec.md` §4.3)를 재검토해야 한다
2. **한 표현을 accord 하나로 매핑해도 되는가**

`spec.md` §8 남은 작업 1번이 (1)을 묻고 있었고, 노트북 22의 사람 평가 70건에서 답을 얻을 계획이었다.

**검토한 방법**

1. 노트북 22의 사람 평가 70건을 먼저 판정한다
2. 검색 성립성(후보 개수와 순위 변별력)을 먼저 계산하고, 그 결과로 사람 판정 범위를 정한다
3. canonical note 178개를 target에 다시 넣는다 — 노트북 23의 accord-only 축소를 되돌린다

**결정** — (2)를 택했고, 계산 결과로 다음을 정했다.

- **accord 92개를 유지한다.** canonical note를 target에 다시 넣지 않는다 → **(3) 기각**
- **한 표현은 accord 2개 이상으로 매핑한다. 단독 매핑을 금지한다**
- **`corpus_support`로 검색이 불가능한 accord를 배제한다**
- 70건 판정은 이 계산 뒤로 미뤘다 → **(1) 보류.** 근거 3

---

### 근거 1 — 단독 매핑은 상위 5개를 뽑지 못한다

노트북 27에서 v2(노트북 23)가 제안한 accord 7종을 단독으로 검색해, strength 합으로 정렬했을 때
상위 5위에 들려면 몇 개와 동점 경쟁을 하는지 셌다.

| accord | 보유 향수 | 전체 대비 | 최대 강도 동점 | 단독 5위 동점 |
|---|---:|---:|---:|---:|
| `citrus` | 59,969 | 46.4% | 17,736 | **17,736** |
| `warm spicy` | 45,319 | 35.1% | 6,281 | **6,281** |
| `fresh` | 33,478 | 25.9% | 830 | **830** |
| `earthy` | 19,049 | 14.7% | 533 | **533** |
| `aquatic` | 8,872 | 6.9% | 609 | **609** |
| `soapy` | 1,720 | 1.3% | 18 | 18 |
| `foresty` | 1 | 0.0% | 1 | 1 |

`citrus` 단독은 17,736개가 똑같이 강도 100이다. 그중 5개를 고르는 것은 제비뽑기다.
7종 중 **5종이 단독으로는 순위를 만들지 못한다.**

측정 기록 2번이 `머스크 → musky`를 두고 *"틀린 매핑이 아니라 무효한 매핑"* 이라고 판정한 것과
같은 문제이며, `musky` 단독은 후보 41,154개에 5위 동점 4,010개다.

### 근거 2 — 그런데 조합하면 변별력이 생긴다

같은 accord를 AND로 묶고 strength를 합해 정렬하면 결과가 뒤집힌다.

| 조합 | 후보 | 5위 동점 |
|---|---:|---:|
| `soapy` + `warm spicy` | 114 | **5** |
| `fresh` + `soapy` | 1,049 | **6** |
| `aquatic` + `fresh` | 4,963 | **14** |
| `fresh` + `citrus` | 18,826 | **25** |
| `musky` + `powdery` + `vanilla` | 8,007 | **9** |

`fresh`(25.9%)와 `citrus`(46.4%)는 각각 너무 넓지만, 둘을 묶으면 후보 18,826개에
5위 동점이 25개다. **넓은 accord도 조합하면 쓸 수 있다.**

즉 문제는 accord 목록의 크기가 아니라 **매핑 단위**였다.

### 근거 3 — 검색 불가 3건의 원인은 target space가 아니라 proposer였다

v2가 번역한 10건 중 3건이 검색 불가였고, 원인은 전부 `foresty`(보유 향수 1개) 하나다.
다른 accord와 AND로 묶으면 0이 된다.

```
SQ0061  비 온 뒤의 숲의 냄새    foresty + earthy          →  0개
SQ0032  숲 속에 온 듯한 향      foresty                   →  1개
SQ0073  편백나무·산내음        fresh + foresty (+direct)  →  0개
```

그런데 **같은 92개 안에 대안이 있었다.**

| 표현 | v2 원본 | 92개 안의 대안 |
|---|---:|---|
| 비 온 뒤의 숲 | 0개 | `mossy+earthy` → 4,715개 (동점 8) |
| 숲 속에 온 듯한 향 | 1개 | `mossy+green` → 1,702개 (동점 20) |
| 편백나무·산내음 | 0개 | `woody+conifer+green` → 720개 (동점 5) |

`SQ0073`의 사용자 표현은 **편백나무**이고 92개 안에 **`conifer`(침엽수)** 가 있다.
글자 그대로 대응하는데 v2는 `foresty`를 골랐다.

`SEMANTIC_BRIDGE_PILOT_PLAN.md` §6.4의 분류로는 `TARGET_SPACE_LIMITATION`이 아니라
**`MAPPING_FAILURE`** 다. 그리고 §11의 REVISE 조항이 이 경우를 미리 정해뒀다 —
*"`MAPPING_FAILURE`가 많음: target vocabulary를 넓히기 전에 proposer prompt/model의
후보 탐색 문제를 우선 검토"*.

**왜 못 찾았는지도 설명된다.** `foresty`가 향수 1개라는 것은 세상 지식이 아니라 우리 데이터
안에만 있는 사실이므로 LLM이 알 방법이 없다. 측정 기록 1번의 문장이 그대로 적용된다 —
*"이 정보는 LLM이 구조적으로 알 수 없다."* **사전이 LLM보다 확실히 나은 지점이 여기다.**

### 근거 4 — 70건 판정을 미룬 이유

노트북 22의 70건은 **v1 실행 결과에 고정돼 있고, v1은 12건 중 7건이 스키마 검증에 실패했다**
(`23_..._v2_checkpoint.json`의 `source_v1`: `schema_valid_count 5, validation_failure_count 7`).
LLM 출력이 `target_type`에 선택지 문자열 `ACCORD|CANONICAL_NOTE`를 그대로 써서
계획 §6.3의 *"검증 후 target이 0개면 ABSTAIN으로 처리한다"* 규칙에 걸린 것이다.

그 7건이 **ABSTAIN 원인 판정 9건 중 7건**을 차지한다. 즉 (1)의 답을 담은 자리 9개 중 7개가
서식 오류로 채워져 있다. 노트북 23이 같은 7건을 전부 유효한 accord로 매핑했으므로
판정 결과는 이미 정해져 있다.

또 `retrieval_key`를 대조하니 **RETRIEVAL 53건 중 30건이 baseline과 treatment의 향수 집합이
동일**했다(v1이 ABSTAIN해서 treatment에 더할 bridge accord가 없었다). Δ Pooled Human NDCG@5가
구조적으로 0이므로 채점해도 정보가 나오지 않는다.

부수적으로, 계획 §11의 GO 기준 중 *"Mixed 6개 중 Treatment 승 4개 이상"* 은 대비가 있는
Mixed 쿼리가 2개뿐이어서, *"Pure 6개 중 Strong Hit@5 4개 이상"* 은 bridge TOP5가 있는 Pure
쿼리가 1개뿐이어서 **평가자가 무엇을 매겨도 달성할 수 없다.**

---

**결과**

- 쿼리 12건 중 **검색 가능 7 / 검색 불가 3 / 번역 없음 2**
- 검색 불가 3건의 원인은 `foresty` 하나이고, 92개 안에 대안이 있다
- 단독으로 순위를 못 내는 accord가 7종 중 **5종**
- 두 재현 게이트 통과 — accord 92개 `perfume_count` 오차 0, 2개 조합 14건 `cooccurrence_count` 오차 0

**결정이 설계를 바꾸지 않는다는 점이 중요하다.** 두 규칙 모두 `spec.md` §4.3이 이미 가진 칸으로 표현된다.

| 규칙 | 이미 있는 칸 |
|---|---|
| 조합으로 매핑한다 | `required` (`core` / `optional`) |
| 죽은 accord를 배제한다 | `corpus_support` (필수 컬럼, 측정 기록 1번의 결론) |

**Trade-off**

- **조합을 요구하면 사전 작성 비용이 오른다.** 한 표현마다 accord를 2개 이상 고르고 각각의
  근거를 써야 한다. 항목 30~50개면 accord 결정이 60~150건이 된다
- **조합은 결과를 좁힌다.** `soapy`(1,720개)를 필수로 걸면 후보가 매우 적어진다.
  `required`의 `core`/`optional` 구분과 `spec.md` §3의 조건 완화로 대응하지만,
  완화 순서 자체는 아직 측정되지 않았다
- **accord만 쓰기로 하면 `foresty` 같은 표현의 정밀도를 잃는다.** `비 온 뒤의 숲`을
  `mossy+earthy`로 근사하는 것은 `foresty`보다 의미가 넓다. note를 쓰면 더 정확할 수 있지만
  검증하지 않았다
- **70건 판정을 미루면 의미 판정이 계속 없는 상태가 된다.** 검색이 된다는 것과
  옳은 번역이라는 것은 다르다. 이 결정은 후자를 답하지 않는다
- `foresty`를 목록에서 지우지 않는다. 값만 기록하고 검색 단계에서 판단한다 (측정 기록 1번과 같은 방침)

---

**한계 — 이 근거로 말할 수 있는 것과 없는 것**

1. **쿼리 12개다.** 노트북 22의 사전등록 선정분이며 무작위 표본이 아니다.
   "숲 표현 3건이 전부 `foresty`로 갔다"는 12건 중 3건의 이야기다.
2. **대안 조합(`mossy+earthy` 등)은 검증된 매핑이 아니다.** 검색 개수만 확인했다.
   AI가 초안으로 고른 것이고 팀이 승인하지 않았으며 **의미 판정을 받지 않았다.**
   따라서 *"92개 안에 쓸 수 있는 조합이 존재한다"* 까지만 말할 수 있고
   *"이것이 옳은 번역이다"* 는 말할 수 없다.
3. **점수 식이 검증되지 않았다.** `spec.md` §3의 주의대로 쿼리→향수 점수 식은 미검증이며,
   여기서는 target accord strength의 단순 합만 썼다. 실제 식이 정해지면 동점 규모가 달라진다.
4. **계절·시간대와 note 조건을 계산에 넣지 않았다.** `spec.md` 제약 1대로 서비스 초기에는
   계절 데이터가 0건이고, `SQ0051`의 direct note `Peach`는 표시만 했다.
5. **단독 매핑 금지의 임계값을 정하지 않았다.** "동점 50개 이하"는 읽기 편하게 나눈
   서술용 구분이고 GO/STOP 기준이 아니다. 어느 동점 규모까지 허용할지는 정하지 않았다.
6. **단일 실행이다.**

---

**발표 대응**

Q. *"accord 92개로 한국어 향 표현을 다 표현할 수 있나요?"*

1. **측정했다.** 실사용 설문에서 뽑은 쿼리 12건으로 검색해봤다. 7건은 검색이 됐고 3건은
   후보가 0~1개였다. 원인은 `foresty` 하나였고, 보유 향수가 13만 개 중 1개다.
2. **그런데 같은 92개 안에 대안이 있었다.** `편백나무`에는 `conifer`(1,828개)가 있는데
   모델이 `foresty`를 골랐다. 목록이 부족한 게 아니라 고르는 쪽의 문제였다.
3. **그래서 사전이 필요하다.** `foresty`가 1개라는 건 우리 데이터 안에만 있는 사실이라
   LLM이 알 수 없다. 사전은 `corpus_support`를 들고 있어서 안다.
4. **한계도 안다.** 12건이고, 대안 조합의 의미 판정은 아직 받지 않았다.

Q. *"왜 단독 매핑을 금지하나요?"*

`citrus` 하나로 검색하면 후보 59,969개 중 17,736개가 강도 100으로 동점이다.
상위 5개가 제비뽑기가 된다. `fresh + citrus`로 묶으면 5위 동점이 25개로 줄어든다.
**같은 accord인데 단독이냐 조합이냐로 기능이 되고 안 된다.**

---

**재현 방법**

```bash
cd EDA
export PYTHONIOENCODING=utf-8
# 27_bridge_accord_search_feasibility.ipynb 를 REPORT_ONLY=False 로 실행
# API 호출 0회. 재현 게이트 2개가 먼저 통과해야 진행된다
#
# 출력:
#   analysis_outputs/27_bridge_accord_feasibility.csv     쿼리 12행
#   analysis_outputs/27_bridge_accord_alternatives.csv    대안 조합 14행
#   analysis_outputs/27_bridge_accord_summary.md
```

**관련 자료**

- `27_bridge_accord_search_feasibility.ipynb` — 근거 1~3
- `analysis_outputs/23_semantic_bridge_accord_only_v2_comparison.csv` — v2 매핑 (입력)
- `analysis_outputs/22_semantic_bridge_pilot_checkpoint.json` — v1 상태와 direct annotation (근거 4)
- `docs/nlr_engineering_notes.md` 1번 — `foresty` 코퍼스 지지도 문제의 최초 발견
- `docs/nlr_engineering_notes.md` 2번 — 단독 accord의 변별력 문제 (`머스크 → musky`)
- `docs/nlr_engineering_notes.md` 8번 — 이 측정의 기록
- `docs/spec.md` §4.3 — 사전 스키마의 `corpus_support` · `required`
- `docs/plans/SEMANTIC_BRIDGE_PILOT_PLAN.md` §6.4 · §11 — ABSTAIN 원인 분류와 REVISE 조항

---

## N3. 사전의 방법론 위치를 ISO 13299 "기존 용어 재사용"으로 규정하고 그 단점을 한계로 안고 간다

- 날짜: 2026-09-11
- 상태: **제안** — 근거는 조사로 확보했으나 팀 승인 전

**문제** — 사전 44행의 매핑에 의미 판정 근거가 없다. 검색 성립성은 계산했지만(N2)
`soapy + fresh`가 `빨래`의 옳은 번역인지는 계산으로 답할 수 없다. 팀에 전문 조향사가 없다.

발표에서 *"정답이 없는 자연어 표현으로 어떻게 향수를 추천했는가"* 를 물으면 답할 것이 필요하다.

**검토한 방법**

1. 정식 lexicon 개발 절차를 밟는다 — 훈련된 패널을 구성하고 accord마다 물리적 기준 물질을 지정한다
2. **공개된 전문가·공식 기관 자료에서 방법론 근거를 찾고, 표준이 허용하는 경로 중 우리가 선 자리를 규정한다**
3. 근거 없이 진행하고 사용자 만족도로만 개선한다

**결정** — (2)를 택했다.

- **우리 사전을 ISO 13299:2016 §6.2.3 Table 1의 Procedure 1(기존 용어·기준 물질 재사용)로 규정한다**
- **같은 표준이 명시한 그 경로의 단점을 한계로 기록하고 안고 간다**
- **ISO 의미의 `referenced`(물리적 기준 물질)를 충족하지 못함을 명시한다** → (1)을 현 단계에서 기각
- 개별 매핑의 의미 근거는 전문가 문헌이 아니라 **커뮤니티 용법 조사**로 확보한다

---

### 근거 1 — 표준이 이 경로를 허용된 절차로 명시한다

ISO 13299:2016 §6.2.3은 attribute 집합 선정의 목적을 이렇게 적는다.

> *"The purpose is to identify and select a set of **non-overlapping, singular, objective,
> unambiguous, and referenced** attributes that, as far as possible, permit a complete
> descriptive analysis of the samples under study."*

Table 1은 attribute 목록을 정하는 **허용된 세 절차**를 적고, Row 1이
*"Use existing terminology and reference standards"* 다. 우리가 IFRA·Michael Edwards 용어를
빌려 쓰는 것이 이 절차다. **표준 밖의 임의 관행이 아니다.**

인용할 때 지켜야 할 것 — 원문은 `shall`이 아니라 `The purpose is to…`이고 문서 제목이
*General guidance*다. **요구사항이 아니라 목적 진술로** 인용한다.

### 근거 2 — 같은 표준이 이 경로의 단점도 적어뒀다

Table 1 Row 1 Disadvantages:

> *"Existing terminology or reference standards may include choices that are **imprecise or
> inappropriate for a particular set of samples**. Attributes may be missed that could have been
> discovered during the development of new terminology."*

Row 2·3 Disadvantages:

> *"Profiles obtained are unique to a given panel and set of samples. They **cannot be interpreted
> by other groups if no reference standards are given**."*

즉 표준 스스로 **용어 목록이 아니라 지정된 기준 물질**을 그룹 간 해석 가능성의 조건으로 삼는다.
우리에게 불리한 근거이고, 그래서 함께 기록한다.

`may include`는 가능성 서술이므로 *"수입한 descriptor는 반드시 안 맞는다"* 로 강화하지 않는다.

### 근거 3 — 교차언어 문제의 표준 해법과, 그 해법이 못 푸는 것

미국·태국 패널이 간장 향미 용어를 서로 이해하지 못한 사례에서:

> *"the problems were solved once the meaning of each term was clearly defined with an agreement
> of both panels and an appropriate reference standard was used to represent each term."*

**그러나 한쪽 언어에만 있는 용어는 이 절차로도 풀리지 않았다.** `cured`(태국에 없음),
`roaches`(드묾), `brown`(복합 개념)은 정의가 아니라 **제외**로 처리돼 최종 59개만 합의됐다.

`빨래`·`이불`·`호텔`·`휴양지`·`비 오는 숲`이 정확히 그 범주다. **문헌은 버렸고 우리는 매핑했다.**

### 근거 4 — 개별 표현 근거는 전문가 문헌에서 나오지 않는다

출처 50개에서 주장 248건을 뽑아 25건을 3표 적대적 검증했다. **확정 6건 / 반박 19건.**

확정 6건 중 **표현 단위를 다루는 것이 0건**이다. 유일한 접점이 Dravnieks Atlas에
`soapy`·`powdery`라는 **용어가 존재한다**는 것인데, 그건 *"한국어 `포근한`이 그 용어를 가리킨다"* 와
다른 진술이다.

주장 248건의 키워드 집계도 같다 — `무난한`·`고급스러운`·`섹시한` 등 평가어 언급 **각 0건**,
전문 용어(`탑노트`·`잔향`) **0건**.

**그래서 개별 매핑 근거는 커뮤니티 용법 조사로 넘긴다.**

---

**결과**

- 방법론 방어 문단을 쓸 수 있게 됐다 — 표준이 허용하는 경로, 그 경로의 단점, 우리가 못 채운 칸
- Dravnieks Atlas descriptor 146개를 **유료 구매 없이** 확보했다 (Odeuropa SKOS/RDF, 원파일 카운트로 확인)
- `aquatic`·`musky`·`tropical`·`white floral`의 공식 정의는 **여전히 못 찾았다.**
  IFRA 미매칭이고 Dravnieks 181개 라벨에도 0건이다
- 사전의 `evidence_tier` 등급은 **바뀌지 않는다.** 이번 조사는 등급을 올릴 개별 근거를 주지 않았다

**Trade-off**

- **ISO를 인용하면 못 채운 칸도 같이 드러난다.** `referenced` 미충족이 명시적 결손이 된다.
  숨기는 대신 먼저 말하는 쪽을 택했다
- **(1)을 기각하면서 그룹 간 해석 가능성을 잃는다.** 표준이 말한 대로, 기준 물질이 없으면
  다른 집단이 우리 프로파일을 해석할 수 없다. 6인 팀의 기간과 비용으로 훈련 패널을 꾸릴 수 없다는
  현실적 제약을 받아들인 결과다
- **인용 위생 비용이 붙는다.** 목적 진술을 요구사항으로, 가능성 서술을 단정으로 바꾸면 근거가
  거꾸로 약점이 된다. 인용문마다 원문 표현을 유지해야 한다
- **ISO의 `a particular set of samples`는 물리적 자극이다.** 이를 텍스트 코퍼스로 옮기는 것은
  표준이 아니라 우리 쪽 유추다. 이 유추에 기대고 있음을 인정한다
- **간장 연구를 향수에 적용하는 것 자체가 도메인 전이다.** 감각 어휘 개발 절차가 공통이라는
  전제를 검증하지 않았다
- **조사 비용이 회수되지 않은 부분이 있다.** 각도 5개 중 2개(계열 분류 공식 근거, 장면 표현
  산업 선례)가 확정 0건이었다

---

**한계 — 이 근거로 말할 수 있는 것과 없는 것**

1. **ISO 조항은 "기준이 무엇인지"에만 쓸 수 있다.** §6.2.3의 다섯 성질은 훈련된 패널이 물리적
   샘플을 평가하는 상황을 전제하고, `referenced`는 평가자에게 descriptor를 가르치는 물리적
   기준 물질을 뜻한다. **"우리가 기준을 통과했다"고 말할 수 없다.**
2. **교차언어 근거는 자기인용이다.** 리뷰 저자가 2013 원논문 공저자라 규범적 결론에 자기홍보
   성격이 있다. 리뷰의 강화된 표현이 아니라 원문 표현을 쓴다.
3. **`포근한 → powdery`에 반박이 붙었다.** Dravnieks는 powderiness를 dryness와 한 concept으로
   묶는다(prefLabel `Dry`, altLabel `Powdery`). 유일하게 확보한 권위 자료가 그렇다.
4. **§2의 한국어 머스크 드리프트 주장이 여전히 코퍼스 관찰에만 의존한다.** white musk와
   animalic musk를 구분하는 업계 정의를 못 찾았다. Dravnieks에서 MUSK와 SOAPY가 별개
   descriptor인 것은 우리 관찰과 정합하지만 **정합일 뿐 정의 근거가 아니다.**
5. **반박률 76%다.** 검증을 절반만 돌렸을 때 확정으로 보이던 것들이 뒤집혔다.
   부분 집계로 결론을 내면 안 된다.

---

**폐기한 설명**

*"Dravnieks에 aquatic이 없는 것은 Calone 이후 성립한 범주라서"* — **틀렸다.** Calone 1951은
1966년 Pfizer에서 합성돼 1985년 atlas보다 앞선다. 부재의 이유는 연대가 아니라 **범위**다.
Dravnieks는 단일 냄새 분자를 평정한 심리물리 자료이지 perfumery accord 분류가 아니며,
`gourmand`·`chypre`·`fougère`·`amber`도 똑같이 없다.

---

**발표 대응**

Q. *"정답이 없는 자연어 표현으로 어떻게 향수를 추천할 수 있나요?"*

1. **정답 분류표가 없는 것은 우리 프로젝트의 결함이 아니라 이 분야의 상태입니다.**
2. **표준 절차는 문서화돼 있습니다.** ISO 13299:2016이 attribute 목록을 정하는 세 절차를 적고,
   그중 하나가 기존 용어와 기준 물질을 재사용하는 것입니다. 우리는 그 경로를 택했습니다.
3. **그 경로의 단점도 같은 표준에 적혀 있고, 우리는 그것을 한계로 기록했습니다.**
   빌려온 용어가 우리 데이터에 부정확할 수 있고, 기준 물질이 없으면 다른 집단이 해석할 수 없습니다.
4. **우리가 못 채운 칸을 압니다.** ISO가 요구하는 다섯 성질 중 `referenced`, 즉 각 descriptor를
   가르치는 물리적 기준 물질이 없습니다. 훈련 패널을 꾸릴 수 없는 조건에서 내린 선택입니다.

Q. *"그러면 개별 매핑은 무슨 근거인가요?"*

전문가 문헌에서는 나오지 않았습니다. 출처 50개에서 주장 248건을 검증했는데 표현 단위 근거가
0건이었습니다. 그래서 **실사용자의 용법**을 근거로 삼는 조사를 따로 하고 있습니다.
전문가 자료가 *"descriptor가 무엇인지"* 를 정의한다면, 커뮤니티 근거는
*"사람들이 이 말을 이 향에 쓴다"* 를 보여줍니다. 두 근거는 성격이 달라 나눠 다룹니다.

---

**재현 방법**

```
조사 실행 기록    workflow run wf_f11312a9-87c (검증 106 에이전트, 오류 0)

ISO 13299:2016    iso.org/standard/58042.html                    [유료: ISO 표준]
  무료 프리뷰      cdn.standards.iteh.ai/samples/58042/...         pp. i-7 (§6.2.3, Table 1)
교차언어          pmc.ncbi.nlm.nih.gov/articles/PMC6352027/      [오픈액세스]
  원 연구         Cherdchu et al. 2013 J Sens Stud 28:248-255    [유료: Wiley]
  무료 전문       krex.k-state.edu
Dravnieks 어휘    vocab.odeuropa.eu/dravnieks/en/                [기관 무료 공개]
  ASTM 원전       doi 10.1520/DS61-EB                            [유료: ASTM 간행물]
Pyrfume           pmc.ncbi.nlm.nih.gov/articles/PMC11557823/     [오픈액세스]
```

**관련 자료**

- `docs/nlr_engineering_notes.md` 9번 — 이 조사의 측정 기록과 각도별 수확
- `docs/nlr_engineering_notes.md` 2번 — 한국어 머스크 드리프트 (근거 5에서 한계 확인됨)
- `docs/spec.md` §4.3 — 사전 스키마와 매핑 규칙
- `data/scent_knowledge/domain_lexicon_v1.csv` — 이 결정이 적용되는 대상

---

## N4. 표현↔accord 의미 판정의 1차 근거를 `ai_summary` 신호로 삼는다

- 날짜: 2026-09-11
- 상태: **제안** — 측정은 끝났으나 팀 승인 전

**문제** — 사전 매핑 28행이 전부 `status=candidate` 다. *"`빨래`가 정말 `soapy` 인가"* 를
판정할 근거가 없었다. 밖에서 찾으려고 두 번 조사했고 둘 다 실패했다.

```
전문가 문헌 조사 (N3)   출처 50개·주장 248건  →  개별 표현 근거 0건
커뮤니티 후기 조사      에이전트 98개·5.6M 토큰 →  확정 3건 (글 1~2건 수준)
```

그 상태에서 커뮤니티 조사가 낸 글 1건이 `빨래 → soapy` 를 반증했고,
**글 1건으로 사전을 고칠 것인가** 에서 막혀 있었다.

**검토한 방법**

1. 커뮤니티 후기를 더 모은다 — DC 시향기 14,530건 본문 크롤링
2. 전문 조향사·공식 기관의 향수별 서술 자료를 찾는다
3. **`perfumes.jsonl` 의 `ai_summary` 를 쓴다** — 이미 갖고 있는 필드
4. 근거 없이 진행하고 사용자 만족도로만 개선한다

**결정** — (3)을 택했다.

- **`ai_summary` 서술문을 표현↔accord 신호의 1차 근거로 쓴다**
- **판정 전에 통제군으로 방법을 검증한다** — 무작위 집단에서 배수가 1 근처인지 확인
- **검색어가 accord 이름과 같으면 순환으로 표시하고 신호로 읽지 않는다**
- **이 근거로 사전을 자동으로 고치지 않는다.** 측정과 반영을 분리한다
- (1)은 **보류** — 작업이 4겹이고 법적 확인이 선행돼야 한다
- (2)는 **계속 열어둔다** — 이 근거는 전문가 자료가 아니므로 대체하지 못한다

---

### 근거 1 — 규모가 앞선 두 조사와 다르다

```
향수 131,930건 중 ai_summary 보유   12,432건 (9.4%)
서술문 총                          210,521개
```

`SCHEMA.md` — *"Fragrantica 의 AI-요약 의견; `up_votes`/`down_votes` = 동의/비동의한 사용자 수"*

**커뮤니티 조사가 찾아 헤매던 형태가 그대로 있다.** 향수별 서술 + 합의 수치.
`perfumes.csv` 59개 컬럼에 없고 jsonl 에만 있어서 노트북 01~29 가 보지 못했다.

### 근거 2 — 통제군이 방법의 건전성을 확인했다

표현과 무관하게 무작위 추출한 집단에서 같은 계산을 했다
(`spec.md` §7.3 조건 5와 같은 취지).

| 통제군 | 최대 배수 | 배수 1.5 이상 |
|---|---:|---:|
| n=500 | 1.17 | 0 |
| n=1,000 | 1.38 | 0 |
| n=2,500 | 1.14 | 0 |

**무작위로는 1.4배를 넘지 못한다.** 실제 표현에서는 `soapy` 4.50배, `coconut` 5.76배,
`mossy` 3.63배가 나온다. 방법이 신호를 지어내지 않는다.

### 근거 3 — 글 1건으로 내린 반증이 뒤집혔다

커뮤니티 조사는 Byredo Blanche **한 제품**에 `soapy` 가 없다는 이유로 `빨래 → soapy` 를
반증했다. 957건으로 보니 `soapy` 가 **배수 4.50 으로 1위**다. **Blanche 가 예외였다.**

**그때 고쳤다면 맞는 매핑을 지웠을 것이다.** 이것이 (3)을 (1)보다 앞에 둔 직접적 이유다.

### 근거 4 — 사전 28행 중 14행이 지지됐다

```
지지 14 · 약함 5 · 순환 4 · 반증(지지도 없음) 3 · 반증(전체보다 드묾) 2
```

`빨래→soapy` 4.50x 1위 · `휴양지→coconut` 5.76x 1위 · `비 오는 숲→mossy` 3.63x 1위 ·
`촉촉한→aquatic` 3.30x 1위 · `깨끗한→soapy` 2.18x 1위 · `달달→caramel` 1.61x 1위

그리고 **`NO_MAPPING` 판단이 지지됐다.** `무난한` 은 향수 6,217개에 최대 배수 1.30,
`고급스러운` 은 6,966개에 1.38로 **통제군 수준**이다. 어떤 accord 로도 수렴하지 않는다.

---

**결과**

- 노트북 30. 출력 3개 — 배수표 781행, 사전 대조 28행, 보고서
- accord 92개 `perfume_count` 재현 게이트 오차 0
- **사전 파일은 수정하지 않았다.** 측정과 반영을 분리했다

**Trade-off**

- **전문가 근거를 대체하지 못한다.** Fragrantica 의 AI 가 사용자 리뷰를 요약한 것이다.
  투표로 검증된 대중 의견이라는 점이 글 1~2건보다 나을 뿐이고, 사용자가 요청한
  *"전문 조향사·공식 기관 자료"* 는 여전히 없다
- **커버리지가 심하게 치우쳐 있다.** 보유 향수의 평가자 수 중앙값 **628명** vs
  미보유 **9명** — **70배**다. 대중적 향수에서의 관계이고 나머지 119,498건에는 보증할 수 없다
- **영어 서술이라 번역 가정이 한 단계 들어간다.** `포근한 = cozy` 는 내가 정했다.
  일반 번역이라 향 도메인 추론보다 약한 가정이지만 가정은 가정이다
- **출처가 Fragrantica 다.** `spec.md` §8 의 `ai-train=no` · EU 권리유보가 그대로 적용된다.
  **분석 근거로 쓰는 것과 서비스에 넣는 것은 다른 판단**이고 후자는 정하지 않았다
- **부분 문자열 검색의 잡음을 제거하지 않았다.** `이불` 에서 `cotton candy` 가 걸린 것으로 보인다
- **투표수를 쓰지 않았다.** 존재 여부만 봤다. 가중하면 결과가 달라질 수 있다
- **(1)을 보류하면 커뮤니티 고유 어휘를 계속 못 본다.** `ai_summary` 는 영어이므로
  `쿰쿰한`·`폭닥한` 같은 한국어 고유 표현은 여기서 검증되지 않는다

---

**한계 — 이 근거로 말할 수 있는 것과 없는 것**

1. **동반 출현이지 인과가 아니다.** 배수가 높다는 것은 같이 나타나는 일이 잦다는 뜻이다.
2. **6개 표현은 노트북 작성 전에 결과를 봤다** (`포근한`·`빨래`·`깨끗한`·`휴양지`·`무난한`·`고급스러운`).
   탐색 때 쓴 영어 대응어를 바꾸지 않았고 보지 않은 표현도 전부 넣었으나,
   사전 등록의 원칙이 부분적으로 훼손됐다. 노트북의 `known_deviation` 에 기록했다.
3. **`포근한 → powdery` 는 두 번 반증됐다.** 배수 1.35 · 14위로 통제군 최대치보다 낮다.
   그러나 이 매핑은 `spec.md` §3 의 워크드 예제이자 팀 문서 근거가 있는 항목이므로,
   바꾸려면 spec 본문도 함께 고쳐야 한다. **이 결정은 그 변경을 포함하지 않는다.**
4. **9.4% 커버리지의 편향을 보정하지 않았다.** 인기도로 층화해 다시 재면 결과가 달라질 수 있다.

---

**발표 대응**

Q. *"전문 조향사도 없는데 `빨래`가 `soapy` 라는 걸 어떻게 압니까?"*

1. **사람들이 그 향수를 어떻게 부르는지를 셌습니다.** 리뷰에서 '빨래·린넨·섬유유연제'로
   표현된 향수 957개를 모아, 그 향수들이 실제로 어떤 accord 를 갖는지 전체 평균과 비교했습니다.
   `soapy` 가 **4.5배** 흔했고 1위였습니다.
2. **방법이 신호를 지어내지 않는지 먼저 확인했습니다.** 표현과 무관하게 무작위로 뽑은 집단에서는
   최대 배수가 1.4를 넘지 않았습니다.
3. **한계도 압니다.** 전문 조향사가 아니라 사용자 의견 요약이고, 인기 향수에 치우쳐 있으며,
   영어 서술이라 번역 가정이 들어갑니다.

Q. *"그럼 사전을 고쳤나요?"*

아직 아닙니다. **측정과 반영을 분리했습니다.** 직전에 글 1건으로 반증했던 항목이
957건으로 보니 정반대였던 일이 있어서, 근거가 생겼다고 바로 고치지 않습니다.

---

**재현 방법**

```bash
cd EDA
export PYTHONIOENCODING=utf-8
# 30_ai_summary_expression_accord.ipynb 를 REPORT_ONLY=False 로 실행
# API 호출 0회. perfumes.jsonl 509MB 를 한 번 읽는다
```

**관련 자료**

- `30_ai_summary_expression_accord.ipynb` — 근거 1~4
- `analysis_outputs/30_lexicon_crosscheck.csv` — 사전 28행 판정
- `analysis_outputs/30_expression_accord_lift.csv` — 표현 × accord 배수 781행
- `docs/nlr_engineering_notes.md` 10번 — 이 측정의 기록
- `docs/nlr_engineering_notes.md` 9번 — 전문가 조사가 실패한 기록 (N3)
- `SCHEMA.md` — `ai_summary` 필드 정의

---

## N5. 측정 결과를 사전에 반영한다 — 강등하되 교체하지 않는다

- 날짜: 2026-09-11
- 상태: **제안** — 판정 기준은 사용자 승인(2026-09-11), 팀 승인 전

**문제** — N4가 측정을 끝냈지만 *"측정과 반영을 분리한다"* 로 닫혀 있었다. 사전 28행 중
반증 5행·약함 5행이 나왔는데 **무엇을 반증으로 볼 것인지, 반증되면 무엇을 할 것인지**
둘 다 정해져 있지 않았다.

특히 `포근한 → powdery` 는 `spec.md` §3의 워크드 예제여서, 고치면 문서 본문이 함께 움직인다.

**검토한 방법**

1. 반증된 행을 **삭제**한다
2. 데이터가 1위로 가리키는 accord 로 **교체**한다 (`포근한 → savory` 3.72배)
3. **`core` 에서 `optional` 로 내리고** 근거 칸에 기록한다
4. 아무것도 하지 않고 사람 판정을 기다린다

**결정** — (3)을 택했다. 판정 기준 네 가지를 먼저 정하고 기계적으로 적용했다.

| # | 질문 | 결정 |
|---|---|---|
| 1 | 팀 문서와 데이터가 엇갈리면 | **역할 분리.** 팀 문서는 *표현이 존재하는가*, `ai_summary` 는 *어느 accord 인가* |
| 2 | 배수 문턱 | **1.4** — 통제군 최대 배수가 1.38이므로 그 이하는 잡음과 구별되지 않는다 |
| 3 | 문턱 미달이면 | **`core` → `optional`, 근거 칸에 기록.** 삭제·교체하지 않는다 |
| 4 | `core` 가 2개 미만이 되면 | **`NO_MAPPING` 으로 돌린다** (§4.3 단독 매핑 금지) |

- **원본을 덮어쓰지 않는다.** `domain_lexicon_v1.csv` 를 두고 `v1_1.csv` 를 새로 만든다
- **`status` 는 `candidate` 그대로 둔다.** 강등은 검색 동작을 바꾼 것이지 의미 판정이 아니다
- **`spec.md` §3 워크드 예제를 `빨래`+`이불` 로 교체한다**

---

### 근거 1 — 삭제와 교체는 배수가 말하지 않은 것까지 말한다

배수는 *"그 표현으로 불린 향수에 이 accord 가 흔한가"* 만 답한다.
**`포근한` 이라는 표현이 존재한다는 사실은 반박하지 않는다.** 그 근거는 팀 문서에 있고
측정 대상이 아니었다. 그래서 (1) 삭제는 근거 범위를 넘는다.

(2) 교체는 방향을 뒤집으면 무너진다.

| accord | 보유 향수 | cozy 향수 중 비율 | **그 accord 중 cozy 비율** |
|---|---:|---:|---:|
| `savory` | 115 | 3.4% | **82.6%** |
| `powdery` | 6,291 | **68.2%** | 29.9% |

서비스는 **accord 로 향수를 골라 주는** 쪽이므로 오른쪽 열이 실제로 쓰이는 방향이다.
`savory` 는 맞히면 잘 맞지만 줄 향수가 115개뿐이고 `savory`+`nutty`+`cacao` 조합은
13만 개 중 **5개**다. 측정 기록 1번의 `foresty` 문제와 같은 모양이다.

**`powdery` 는 틀린 것이 아니라 필수로 걸기엔 너무 넓다.** cozy 향수의 68.2%가 갖고 있다.
`optional` 로 내리면 점수에는 기여하고 필터로는 작동하지 않는다.

### 근거 2 — 문턱 1.4는 통제군이 정했다

N4의 통제군에서 무작위 집단의 최대 배수가 **1.38**이었다. 1.4 미만은 무작위와 구별되지 않는다.
**문턱을 결과를 보고 고르지 않았다.** 통제군 수치가 먼저 있었다.

### 근거 3 — 노트북 30의 판정을 그대로 쓰지 않았다

노트북 30은 `min_support = 30`(해당군에서 30개 미만이면 버림)을 걸었다.
`반증 — 해당군에 지지도 없음` 3건을 원자료로 다시 셌더니 **2건이 오판이었다.**

| 표현 | accord | 해당군 | 보유 | 배수 | 재판정 |
|---|---|---:|---:|---:|---|
| 머스크 | `soapy` | 653 | 14 | **2.15x** | **문턱 통과** — 30개 미만이라 표에서 빠졌을 뿐 |
| 이불 | `soapy` | 112 | 2 | 1.79x | **표본 부족** — 2개로 낸 배수는 의미 없음 |
| 호텔 | `soapy` | 1,163 | 15 | 1.29x | 문턱 미달 (판정 유지) |

`soapy` 는 코퍼스의 1.0%라 653개의 기대값이 6.5개다. **14개는 신호인데 30개 문턱에 걸렸다.**
기계적으로 적용했다면 측정 기록 2번이 코퍼스로 확인한 한국어 드리프트 매핑을 잘못 지웠을 것이다.

### 근거 4 — 예제 교체가 미정 규칙 2개를 드러냈다

`포근한` 대신 `빨래` 를 예제에 넣자 두 가지가 걸렸다.

1. **`머스크` 는 사전에서 `musky` 와 `soapy` 를 둘 다 갖는다.** 회피가 사전 항목 전체를 쓰면
   `머스크는 빼줘` 한마디가 `빨래` 의 `soapy` 까지 지워 선호가 통째로 사라진다
2. **`깨끗한` 의 비누 갈래 조건에 `빨래`·`이불` 이 있고 물 갈래 조건에 `여름` 이 있다.**
   *"여름에 쓸 빨래 같은 깨끗한 향"* 은 두 갈래를 모두 만족시키는데 우선순위 규칙이 없다

**둘 다 정하지 않고 `spec.md` §8 11·12번으로 올렸다.** 예제에는 1번을 가정으로 표시하고
2번은 `깨끗한` 을 예제에서 빼는 것으로 피했을 뿐 해결하지 않았다.

---

**결과**

- 노트북 31. `data/scent_knowledge/domain_lexicon_v1_1.csv` — **44행 16컬럼 그대로, 삭제 0행**
- 바뀐 칸은 `target_field` 7행 · `required` 6행 · `rationale` 28행 **세 개뿐**
- `NO_MAPPING` 으로 돌아간 표현 **3개** — `포근한`·`차가운`·`호텔` (셋 다 `core` 0개)
- ACCORD 행 `core` 18 / `optional` 10, 표현별 `NO_MAPPING` 11 / `STAGE1_DIRECT` 7 / `STAGE2_BRIDGE` 8
- `entry_id`·`expression`·`candidate_name`·`evidence_tier`·`corpus_support`·`source_query_ids`
  **변경 0행**, `status` 전부 `candidate` 유지
- `domain_lexicon_v1.csv` **해시 대조로 미변경 확인**
- `spec.md` §3 워크드 예제 교체. §3 ④의 strength 를 **실측으로 교체** — 후보 86개 / 1위 212점 / 5위 동점 2

**Trade-off**

- **근거가 `ai_summary` 하나다.** N4의 Trade-off가 그대로 적용된다 — 커버리지 9.4%,
  인기 향수 편향(평가자 수 중앙값 628명 vs 9명), Fragrantica 출처 문제
- **`optional` 강등은 검색 결과를 실제로 바꾼다.** `포근한` 이 `NO_MAPPING` 이 되면 그 표현이
  들어온 쿼리는 사전에서 아무것도 받지 못하고 `LLM` 추정 경로로 넘어간다. **커버리지가 줄었다**
- **`빨래`+`이불` 은 청결감 한 축에 몰려 있다.** 원래 예제의 `포근한`+`깨끗한` 은 서로 다른
  축이었다. 축이 다른 조합의 검색 성립성은 따로 확인하지 않았다
- **조건 분기가 실제로 도는 예시가 없어졌다.** `깨끗한` 두 갈래는 §3 ②-c에 설명으로만 남았다
- **의미 판정은 여전히 없다.** 매핑 행이 전부 `candidate` 인 이유다. 강등은 *"필수로 걸 만큼
  좁은가"* 에 답한 것이지 *"의미가 맞는가"* 에 답한 것이 아니다
- **`savory` 를 넣지 않았다.** 그 accord 를 가진 향수의 82.6%가 cozy 로 불리므로 신호는
  진짜인데 보유 향수가 115개뿐이라 버렸다. **검색 성립성 때문에 맞는 신호를 못 쓴다**

---

**재현 방법**

```bash
cd EDA
export PYTHONIOENCODING=utf-8
# 31_lexicon_v1_1_apply.ipynb 를 REPORT_ONLY=False 로 실행
# API 호출 0회. domain_lexicon_v1.csv 를 읽기만 하고 v1_1.csv 를 새로 쓴다
#
# 출력:
#   data/scent_knowledge/domain_lexicon_v1_1.csv
#   analysis_outputs/31_lexicon_v1_1_changelog.md
```

**관련 자료**

- `31_lexicon_v1_1_apply.ipynb` — 근거 1~3
- `analysis_outputs/31_lexicon_v1_1_changelog.md` — 행 단위 변경 기록
- `docs/nlr_engineering_notes.md` 11번 — `min_support` 문턱의 함정
- `DECISIONS.md` N4 — 이 결정이 반영하는 측정
- `DECISIONS.md` N2 — 단독 매핑 금지. 기준 4번의 근거
- `spec.md` §10 `2026-09-11 (3차)` — 문서 쪽 변경

---

## N6. SCENE 표현을 유지하고 `LLM` 등급 4행을 `TEAM` 으로 올린다

- 날짜: 2026-09-12
- 상태: **제안** — 판정 기준은 사용자 승인(2026-09-12), 팀 승인 전

**문제** — `spec.md` §8 의 미해결 항목 두 개가 사전 확장을 막고 있었다.

- **6번** SCENE 표현(`빨래`·`이불`·`호텔`·`휴양지`·`비 오는 숲`, 사전 44행의 27%)을 유지할 것인가.
  교차언어 문헌은 **배제**를 택했는데(N3 근거 3) 우리 데이터는 배수 3.6~5.8배로 **지지**했다
- **2번** `LLM` 등급 7행을 사람이 검토해 `TEAM` 으로 올릴 것인가.
  그중 `core` 가 3행이라 기각하면 표현이 통째로 사라진다

두 항목은 순서가 있다. **`LLM` 7행 중 4행이 SCENE 안에 있어서**(`이불`·`비 오는 숲`·`휴양지`·`호텔`)
6번을 먼저 정하지 않으면 2번 검토의 절반 이상이 헛일이 된다.

**검토한 방법**

6번에 대해

1. **전부 유지** — 데이터가 3개를 1위로 지지한다
2. **배수 기준으로 가른다** — N5 의 문턱 1.4 를 그대로 적용한다
3. **전부 배제** — 문헌을 따른다

**결정** — 6번은 **(2)**, 2번은 **N4 배수 판정을 근거로 한 사람 검토**.

| # | 기준 | 내용 |
|---|---|---|
| 1 | 6번 문턱 | **1.4** — N5 가 통제군 최대 1.38 에서 정한 값을 그대로 쓴다. 새 기준을 만들지 않는다 |
| 2 | 6번 판정 불가 | 표본 부족으로 배수를 못 낸 행은 **강등하지 않는다.** 측정 불가는 반증이 아니다 (N5 근거 1) |
| 3 | 2번 지지 | `LLM` → `TEAM` 승격 |
| 4 | 2번 약함 | **`LLM` 등급 유지.** 지우지도 올리지도 않는다 |
| 5 | `status` | `candidate` 그대로. `active` 승격은 사전 전체의 의미 판정이라 별건 |

- **v1.1 을 덮어쓰지 않는다.** `domain_lexicon_v1_2.csv` 를 새로 만든다
- **삭제·교체 없음.** N5 의 *"강등하되 교체하지 않는다"* 를 이어간다

---

### 근거 1 — 문헌의 배제 사유가 우리 문제에 그대로 적용되지 않는다

N3 근거 3 이 인용한 사례는 **미국·태국 패널이 간장 향미 용어를 공유하려던 상황**이다.
`cured`·`roaches`·`brown` 이 정의로도 안 풀려 목록에서 빠졌고 59개만 합의됐다.

그 배제의 목적은 **두 집단이 같은 descriptor 를 같은 뜻으로 쓰게 만드는 것**이었다.
우리가 하는 것은 **한국어 쿼리를 우리 데이터 좌표로 옮기는 일방향 번역**이고
두 집단이 서로 이해해야 할 필요가 없다.

**이것이 문헌을 기각한다는 뜻은 아니다.** N3 근거 3 이 *"문헌은 버렸고 우리는 매핑했다"* 로
닫혀 있던 자리에 *"버린 이유가 우리에게도 적용되는가"* 를 채운 것이고, 이 논증 자체는
검증되지 않았다. Trade-off 에 남긴다.

### 근거 2 — 데이터는 SCENE 3개를 1위로 지지한다

| 표현 | accord | 해당군 | 배수 | 순위 |
|---|---|---:|---:|---:|
| 빨래 | `soapy` | 957 | **4.50** | 1위 |
| 휴양지 | `coconut` | 1,211 | **5.76** | 1위 |
| 비 오는 숲 | `mossy` | 241 | **3.63** | 1위 |
| 비 오는 숲 | `earthy` | 241 | 3.04 | 3위 |
| 이불 | `powdery` | 112 | 1.57 | 2위 |

무작위 집단의 최대 배수가 **1.38** 이므로 위 넷은 잡음과 구별된다 (N4 근거 2).

### 근거 3 — `이불` 만이 실제 판단 지점이었고, 판정하지 않는 것이 옳았다

`이불 → soapy` 는 N4 판정표에 **"반증 — 해당군에 지지도 없음"** 으로 되어 있다.
그대로 적용하면 강등되고, `core` 가 1개가 되어 `이불` 이 `NO_MAPPING` 으로 간다.

**그러나 측정 기록 11 이 이미 재계산해뒀다.**

```
이불 → soapy   해당군 112개 중 보유 2개 → 배수 1.79
               판정: 표본 부족. "2개로 낸 배수는 의미 없음"
```

그리고 측정 기록 10 이 **검색어 오염**을 함께 기록했다 — `bedding|cotton|blanket` 이
`cotton candy` 를 잡아 `caramel` 이 7.36배 1위로 나왔다.

**측정 불가는 반증이 아니다.** N5 근거 1 의 *"배수가 말하지 않은 것까지 말하지 않는다"* 를
적용해 강등하지 않고 근거 한계를 행에 적었다.

`호텔 → soapy` 는 같은 재계산에서 **1.29배 문턱 미달**로 확인됐고 v1.1 이 이미 처리했다.
**판정표의 `NaN` 세 개가 서로 다른 이유였다** — 문턱 통과 / 표본 부족 / 문턱 미달.

### 근거 4 — 승격 대상 4행이 전부 `core` 안전권이다

| 표현 | accord | `required` | 배수 | 기각했다면 |
|---|---|---|---:|---|
| 휴양지 | `coconut` | **core** | 5.76 (1위) | `휴양지` → `NO_MAPPING` |
| 달달 | `caramel` | **core** | 1.61 (1위) | `달달` → `NO_MAPPING`. N1 이 남긴 유일한 방언 항목 |
| 깨끗한 | `fresh` | **core** | 1.61 (5위) | 비누 갈래 소멸 → §8 12번이 사라짐 |
| 비 오는 숲 | `green` | optional | 2.00 (5위) | 영향 작음 |

**`core` 세 줄이 전부 지지 판정이라 사라진 표현이 없다.** 지금 사전은 모든 표현이
`core` 를 정확히 2개씩 갖고 있어 여유가 0 이므로, 이 결과가 아니었다면 표현 3개가 날아갔다.

---

**결과**

- 노트북 39. `data/scent_knowledge/domain_lexicon_v1_2.csv` — **44행 16컬럼 그대로, 삭제 0행**
- 바뀐 칸은 **`evidence_tier` 4행 · `rationale` 15행** 두 개뿐
- 고정 칸 14개(`entry_id`·`expression`·`target_field`·`required`·`candidate_name`·
  `corpus_support`·`status`·`source_query_ids` 등) **변경 0행**
- ACCORD 매핑 행 등급: `TEAM` 19 → **23** / `VERIFIED` 2 / `LLM` 7 → **3**
- `domain_lexicon_v1.csv` · `v1_1.csv` **해시 대조로 미변경 확인**
- `spec.md` §8 남은 것 11건 → **9건**

**Trade-off**

- **6번의 문헌 쟁점을 해소한 것이 아니다.** 근거 1 의 논증(일방향 번역이라 배제 사유가
  안 맞는다)은 **우리 쪽 유추이고 검증되지 않았다.** 문헌이 틀렸다고 말하는 것이 아니라
  적용 범위가 다르다고 주장한 것이며, 그 주장 자체에 근거가 필요하다
- **근거가 여전히 `ai_summary` 하나다.** N4 의 Trade-off 가 그대로 적용된다 — 커버리지 9.4%,
  인기 향수 편향(평가자 수 중앙값 628명 vs 9명), Fragrantica 출처
- **SCENE 이 실제로 일하는 양은 적다.** 합성 평가셋 600문장 중 **23건(3.8%)** 에서만 걸렸다.
  유지 결정의 이득이 크지 않다는 뜻이고, 배제했을 때의 손실도 작다
  (조건 2개 이상 쿼리가 갈래 B 에서 141 → 133 으로 8건 감소)
- **`이불` 은 근거 없이 남았다.** 배수가 신뢰 불가라 *"틀렸다고 말할 수 없어서"* 유지한 것이지
  *"맞다고 확인해서"* 유지한 것이 아니다. 다음 측정에서 검색어 오염을 고치면 달라질 수 있다
- **`status` 는 그대로다.** 등급을 올린 것은 *"출처가 무엇인가"* 에 답한 것이고
  *"서비스에 써도 되는가"* 는 열려 있다
- **설문 155건을 쓰지 않았다.** holdout 이라 사전 판정에 쓸 수 없다. 그래서 이 결정의
  기여도 근거는 전부 합성 평가셋에서 왔고, 합성 평가셋은 실사용과 조건 분포가 다르다
  (`NLR_EVALSET_PREREGISTRATION.md` §12-1)

---

**재현 방법**

```bash
cd EDA
export PYTHONIOENCODING=utf-8
# 39_lexicon_v1_2_apply.ipynb 를 REPORT_ONLY=False 로 실행
# API 호출 0회. v1_1.csv 와 30_lexicon_crosscheck.csv 를 읽기만 하고 v1_2.csv 를 새로 쓴다
#
# 출력:
#   data/scent_knowledge/domain_lexicon_v1_2.csv
#   analysis_outputs/39_lexicon_v1_2_changelog.md
```

**관련 자료**

- `39_lexicon_v1_2_apply.ipynb` — 근거 1~4
- `analysis_outputs/39_lexicon_v1_2_changelog.md` — 행 단위 변경 기록
- `DECISIONS.md` N4 — 배수 판정의 출처
- `DECISIONS.md` N5 — 문턱 1.4 와 "강등하되 교체하지 않는다"
- `DECISIONS.md` N3 근거 3 — 교차언어 문헌의 배제 사례
- `docs/nlr_engineering_notes.md` 10번 — `이불` 검색어 오염
- `docs/nlr_engineering_notes.md` 11번 — 판정표 `NaN` 3건의 재계산
- `spec.md` §10 `2026-09-12` — 문서 쪽 변경

---

## N7. 하드 AND 재검토(A축)를 사전 확장 뒤로 미룬다

- 날짜: 2026-09-12
- 상태: **제안** — 측정은 끝났으나 팀 승인 전

**문제** — `DECISIONS.md` N2 가 정한 **하드 AND 필터**(`core` accord 를 전부 가진 향수만 통과)를
가중 점수로 바꿀지 검토하자는 안건이 있었다. 지금 규칙이 지나치게 엄격해 보였기 때문이다.

그런데 **어느 쪽이 나은지 판정할 데이터가 없었다.** 그래서 평가셋을 먼저 만들고(N8) 측정했더니
**질문의 크기 자체가 예상과 달랐다.**

**검토한 방법**

1. A축 실험을 지금 진행한다 — 하드 / 부분 충족 / 완전 가중을 스윕한다
2. **실사용 입력이 어느 구간에 있는지 먼저 재고, 그 결과로 순서를 정한다**
3. 아무것도 재지 않고 가중 방식으로 바꾼다

**결정** — (2)를 택했고, 측정 결과로 다음을 정했다.

- **A축(N2 재검토)을 사전 확장 뒤로 미룬다.** 지금 실사용 영향이 **4.5%p** 다
- **사전 커버리지를 먼저 올린다.** 실사용 영향이 **40%** 다
- **N2 를 기각하지 않는다.** 사전이 커지면 조건 개수 분포가 오른쪽으로 이동하고
  그때 A축이 다시 커진다. **미루는 것이지 닫는 것이 아니다**
- 설문 155건은 holdout 이므로 **사전 확장의 출처로 쓰지 않는다**

---

### 근거 1 — 지표가 질문에 맞지 않아 차이가 안 보였다

합성 평가셋 600문장으로 하드와 소프트를 `P@5` 로 비교했더니 **차이가 0.001** 이었다
(하드 0.3023 / 소프트 0.3013). 이대로면 어느 쪽도 고를 수 없다.

**조건 개수로 층화하니 갈렸다** (노트북 36).

| 조건 개수 | 쿼리 수 | 후보 중앙 하드 | 후보 중앙 소프트 | 검색 불가 하드 | 검색 불가 소프트 |
|---:|---:|---:|---:|---:|---:|
| 1 | 81 | 59,969 | 59,969 | 0% | 0% |
| 2 | 158 | 10,858 | 73,843 | 0.6% | **0%** |
| 3 | 114 | 1,908 | 86,870 | 7.0% | **0%** |
| 4 | 63 | **174** | 94,952 | 7.9% | **0%** |
| 5 | 39 | **26** | 111,479 | 20.5% | **0%** |
| 6 | 10 | **13.5** | 122,138 | 30.0% | **0%** |

**전체 평균이 두 실패를 서로 가리고 있었다.** 하드는 조건이 많을 때, 소프트는(하드와 똑같이)
조건이 1개일 때 무너진다. 방향이 반대라 평균에서 상쇄된다.

조건이 **1개면 두 방식의 5위 동점이 똑같이 17,736개**다. N2 가 단독 매핑을 금지한 근거가
그대로 재현됐고, **이 실패는 검색 방식으로 고칠 수 없다.**

`후보 수`는 수천 배 차이 나는데 `P@5` 는 같다. 하드가 94,000개를 버려도 그 향수들은
어차피 5위 안에 못 들기 때문이다. **`P@5` 만으로 하드/소프트를 고르면 안 된다.**

### 근거 2 — 실사용은 하드가 무너지는 구간에 거의 없다

합성 평가셋은 갈래 A(조건 4개 이상 31.4%)와 갈래 B(6.0%)의 분포가 정반대라
**어느 쪽이 현실인지 알 수 없었다.** 설문 155건으로 같은 것을 쟀다 (노트북 37).

| | 조건 0~1개 | 조건 2~3개 | **조건 4개 이상** |
|---|---:|---:|---:|
| 갈래 A | 20.4% | 48.3% | **31.4%** |
| 갈래 B | 51.6% | 42.3% | 6.0% |
| **설문 155 (실사용)** | **60.7%** | 29.0% | **10.4%** |

**하드 AND 가 무너지는 구간이 실사용에서 10.4% 뿐이다.**

설문으로 직접 검색을 돌린 결과도 같다.

```
전체 155건   하드 검색 불가 52.9%   소프트 48.4%   차이 4.5%p
```

**그 4.5%p 중 대부분이 조건 0개(48.4%)에서 온다.** 거기서는 두 방식이 똑같이 실패한다.

### 근거 3 — 진짜 병목은 조건을 못 뽑는 것이고, 사전으로 풀린다

설문 155건 중 **75건(48.4%)이 accord 를 하나도 얻지 못했다.** 그 75건을 갈랐다 (노트북 38).

| 판정 | 건수 | 75건 중 | 설문 155건 중 |
|---|---:|---:|---:|
| **번역 가능 — 향을 묘사했다** | **62** | **82.7%** | **40.0%** |
| 미분류 (`"넵"`·`"네"` 등 빈 응답) | 13 | 17.3% | 8.4% |
| **번역 불가 — accord 와 무관** | **0** | **0%** | **0%** |

**가격만 묻고 향은 안 물어본 쿼리가 하나도 없었다.** 가격·건강·개인화를 말한 사람도
향 묘사를 함께 했고, 사전에 그 표현이 없어서 못 잡았을 뿐이다.

원인도 갈렸다 — **95%(71건)가 `scent_preference` 를 아예 못 뽑은 경우**이고
표기 정규화로 건질 수 있는 것은 4건뿐이다. 그런데 **80%(60건)에서
`additional_requirements` 는 채워져 있다.** 재료는 넘어왔는데 사전이 못 받았다.

미분류 13건은 `spec.md` §5.3 이 이미 다루는 경우다(*"향과 무관한 입력은 조건이 하나도
안 잡히므로 감지된다"*). **고칠 대상이 아니다.**

### 근거 4 — 사전은 이미 일하고 있고, 효과를 잴 방법이 생겼다

설문 155건에서 사전이 조건 개수를 **2배 이상** 올렸다.

```
LLM 만            평균 0.61개
+ 사전            평균 1.29개     ← 155건 중 76건(49.0%)에서 사전 표현이 걸림
+ 표기 정규화      평균 1.32개
```

합성 평가셋에서도 같다 — 갈래 B 의 **하드 AND 성립 쿼리가 80 → 141건(+76%)** 이 됐다.

**그런데 여전히 평균 1.32개다.** N2 가 요구하는 최소 2개에 못 미친다.
지금 사전의 매핑 표현은 **11개**이고 `spec.md` §4.3 의 목표는 30~50개다.

**사전을 키우고 노트북 37 을 다시 돌리면 조건 개수가 얼마나 오르는지 바로 나온다.**
사전 확장의 효과를 처음으로 숫자로 잴 수 있게 됐다.

---

**결과**

- 우선순위가 바뀌었다

| 문제 | 실사용 영향 | 수단 |
|---|---:|---|
| **사전 커버리지** | **40%** | 사전 확장 (현재 매핑 표현 11개) |
| 하드 vs 소프트 (A축) | 4.5%p | N2 재검토 |
| 무의미 입력 | 8.4% | `spec.md` §5.3 이 이미 설계 |

- 노트북 36·37·38. 출력 6개. **API 호출 155건**(설문 구조화), 사전·평가 데이터 수정 0
- 재현 게이트 — accord 92개 코퍼스 대조 오차 0,
  노트북 35 의 `target_n` 600건 재현 불일치 0

**Trade-off**

- **어느 쪽 추천이 더 좋은지는 여전히 모른다.** `P@5` 는 두 방식을 구분하지 못했고
  `후보 수` 는 품질 지표가 아니다. 하드가 26개만 남기는 것이 *"정확한 것"* 인지
  *"좋은 걸 버린 것"* 인지 이 측정은 답하지 못한다
- **완화를 켠 하드 AND 를 재지 않았다.** `spec.md` §3 ③ 의 5단계 완화를 적용하면
  하드의 실패가 줄어든다. 그 효과는 미측정이다
- **설문 155건의 조건 분포를 본 상태가 됐다.** 성능 측정에는 쓰지 않았고 라벨도 없지만,
  이후 이 155건으로 실사용 성능을 잴 때 *"입력 분포는 이미 본 상태였다"* 를 함께 보고해야 한다.
  **그리고 여기서 본 표현을 사전에 넣으면 holdout 이 아니게 된다**
- **합성 평가셋이 실사용과 다르다.** 두 갈래가 현실을 양쪽에서 빗나갔다
  (조건 4개 이상 — 갈래 A 31.4% / 설문 10.4% / 갈래 B 6.0%). 갈래 B 가 더 가깝다
- **미분류 13건을 실패율에 넣으면 안 된다.** 설문이라 성의 없는 답이 섞인 것이고
  실제 서비스에서 `"넵"` 이라고 치는 사용자 비율과 같다고 볼 근거가 없다
- **사전을 키울 출처가 제한된다.** 설문을 쓸 수 없으므로 팀 문서·미매칭 로그·`ai_summary`
  에서 가져와야 한다. 미매칭 로그(`spec.md` §4.4)는 아직 만들지 않았다

---

**재현 방법**

```bash
cd EDA
export PYTHONIOENCODING=utf-8
# 36_hard_vs_soft_stratified.ipynb   API 0회. 노트북 35 출력을 층화만 한다
# 37_survey_condition_count.ipynb    API 155회. 체크포인트가 있으면 재호출 없음
# 38_zero_condition_analysis.ipynb   API 0회. 노트북 37 체크포인트를 분류한다
```

**관련 자료**

- `analysis_outputs/36_hard_vs_soft_stratified.csv` — 조건 개수별 층화 7행
- `analysis_outputs/37_condition_distribution.csv` — 갈래 A / B / 설문 분포 비교
- `analysis_outputs/38_zero_condition_types.csv` — 조건 0개 75건의 요구 유형 8종
- `DECISIONS.md` N2 — 이 결정이 미루는 안건
- `DECISIONS.md` N8 — 이 측정이 쓴 평가셋
- `spec.md` §7.2 — 평가셋 역할 분담

---

## N8. 쿼리→향수 채점표를 AI 역생성으로 만든다 — 정답은 향수 하나가 아니라 조건 집합

- 날짜: 2026-09-12
- 상태: **제안** — 구축은 끝났으나 사람 검수 미실시. 팀 승인 전

**문제** — *"지금 만드는 추천이 잘 작동하는가"* 에 답할 데이터가 **하나도 없었다.**

```
golden set 200   조건 추출만 채점한다. 어떤 향수가 정답인지는 없다
설문 155         실사용 문장이지만 라벨이 없다
노트북 22 의 70건  v1 실행에 고정돼 있고 v1 은 12건 중 7건 스키마 실패였다
```

채점표가 없으니 하드 AND 와 가중 점수 중 어느 쪽이 나은지(N7), 사전이 얼마나 기여하는지를
전부 말할 수 없었다.

**검토한 방법**

1. **설문 143건을 라벨링한다** — 실사용 문장이라 가장 정확하다
2. **AI 에게 향수를 주고 "그 향수를 찾는 사람이 쓸 법한 문장" 을 만들게 한다**
3. 사람이 시스템 결과를 평가한다 (pooled judgment)

**결정** — (2)를 택했다. 다만 **원안에서 네 가지를 바꿨다.**

| # | 바꾼 것 | 이유 |
|---|---|---|
| 1 | 정답을 **향수 1개 → 조건을 만족하는 향수 집합** | 상위 3개 accord 기준으로 같은 조합을 가진 향수가 중앙값 **188개**다 |
| 2 | 원본 향수 순위는 **보조 지표.** 랜덤 기대값과 병기 | 그 자체로는 해석되지 않는다 |
| 3 | **갈래 두 벌**을 만들어 순환의 크기를 잰다 | 순환은 없앨 수 없다. 재는 것이 현실적이다 |
| 4 | 생성 문장과 설문의 **분포를 비교**한다 | golden set 이 실사용과 달라 위상이 내려간 전례가 있다 |

- **이름은 "합성 평가셋"** 이다. *"추천 정확도"* 로 부르지 않는다
- 쓸 수 있는 말 — **설계 비교용 상대 지표 · 회귀 테스트 · 하한선**
- **사전 등록을 먼저 쓰고 생성했다** (`docs/plans/NLR_EVALSET_PREREGISTRATION.md`)

---

### 근거 1 — 정답을 향수 하나로 찍을 수 없다

131,930개 전수. accord 92개 재현 게이트 통과.

| 향수를 기술하는 방식 | 그 조합이 유일한 비율 | 같은 조합 공유 (중앙값) |
|---|---:|---:|
| accord 8개 전부 | 48.0% | 2개 |
| **상위 3개 accord** | **2.1%** | **188개** |

사용자는 accord 8개를 다 말하지 않는다. 설문 실측 평균 조건 수가 **2.7개**다.

*"이 향수가 1등인가"* 로 채점하면 **완벽하게 맞는 향수를 줘도 0점**이 된다.
그래서 *"이런 향수를 찾아냈나"* 로 바꿨다 — 상위 5개 중 원본의 상위 3 accord 를
전부 가진 비율(`Condition Precision@5`).

### 근거 2 — 순환은 없앨 수 없으므로 크기를 잰다

AI 가 accord 를 보고 `"빨래 냄새"` 라고 쓰고 우리 사전이 `"빨래"` 를 `soapy` 로 되돌린다.
**시험 문제를 낸 쪽과 푸는 쪽이 같은 지식을 쓴다.**

없애는 대신 **순환 경로가 다른 두 갈래**를 만들고 점수 차이를 봤다.

| 갈래 | LLM 에게 준 것 | 순환 경로 |
|---|---|---|
| **A** | 상위 5개 accord + strength | **검색 인덱스와 순환** |
| **B** | `ai_summary` 서술문 (`up_votes` 상위 pros 6 + cons 3) | **사전 근거와 순환** (N4 가 쓴 데이터) |

**갈래 B 도 순환이 없는 것이 아니다.** 종류가 다를 뿐이고 그렇게 보고한다.

`spec.md` §7.3 조건 5(무작위로 섞은 사전을 통제군으로)와 같은 발상이다.

### 근거 3 — `description` 을 기각하고 `ai_summary` 를 택했다

갈래 B 의 입력으로 `description` 을 쓰려 했으나 실물을 보니 **감상문이 아니라 정보표**였다.

```
<이름> by <브랜드> is a <계열> fragrance for <성별>.
Top notes are Bergamot, Verbena...; middle notes are...; base notes are...
```

후보 풀의 **84.8%** 가 note 목록 문구를 갖는다. note 는 검색에 쓰는 표현이므로
**순환이 끊기지 않는다. 기각.**

`ai_summary` 는 전체로는 9.4% 뿐이지만 **인기 향수 안에서는 다르다.**

| 모집단 | `ai_summary` 보유 |
|---|---:|
| 전체 131,930개 | 12,432 (9.4%) |
| **후보 풀 7,823개** (`people >= 500`) | **7,414 (94.8%)** |

**표집 전에 `ai_summary` 보유로 걸렀다.** 두 갈래가 같은 향수 집합을 써야
`A − B` 를 순환 크기로 읽을 수 있기 때문이다 (양쪽 100개로 일치).

### 근거 4 — 길이는 잡혔고 어휘는 못 잡았다

`spec.md` §7.2 의 기록값을 먼저 재현해 측정 방식이 같은지 확인했다 — **4개 전부 일치.**

| 집단 | 건수 | 평균 길이 | 길이 중앙값 | 전문 용어 | 절 개수(대리) |
|---|---:|---:|---:|---:|---:|
| golden 200 (팀원) | 201 | 29.6자 | 29 | 5.5% | 1.25 |
| **생성 갈래 A** | 300 | 56.7자 | **57** | **0.0%** | 2.00 |
| **생성 갈래 B** | 300 | 58.4자 | **58** | **0.0%** | 2.02 |
| 설문 155 (실사용) | 155 | 70.9자 | **57** | 18.1% | 2.72 |

**golden set 이 29자 / 1.25절로 실사용의 절반도 안 됐던 문제를 넘었다.** 길이 중앙값이 같다.

**그러나 현실의 노이즈가 빠졌다.**

| | 생성 600건 | 설문 155건 |
|---|---:|---:|
| 전문 용어 (잔향·지속력·노트) | **0건** | 28건 (18.1%) |
| 가격 조건 | 2건 (0.3%) | 25건 (**16.1%**) |
| 개인화 (*"나에게 어울리는"*) | **0건** | 10건 (6.5%) |

**전문 용어 0% 는 설계의 결과다.** 생성 규칙 2 가 영어 향 용어를 막았고
갈래 A 에 note 를 주지 않기로 했다. 그 대가로 **사전의 우선순위 1번인 전문 용어 10행이
이 평가셋의 사각지대**가 된다.

그 칸은 **설문 155건이 담당한다.** 다만 설문에는 정답 향수가 없으므로
**같은 지표로 메우는 것이 아니다.** 두 데이터가 서로 다른 질문에 답한다.

```
golden 200   조건 추출 회귀 테스트      라벨 있음 · 팀원 작성 · 짧고 단순
합성 600     검색 회귀 테스트           정답 있음 · AI 작성 · 노이즈 없음
설문 155     실사용 성능 (라벨링 필요)   라벨 없음 · 사람 작성 · 노이즈 있음
```

---

**결과**

- 향수 100개 × 문장 3개 × 갈래 2 = **600문장.** 노트북 32~35
- 생성은 사람이 GPT(`gpt-5.6 Sol`)에 직접 업로드. **API 호출 0회**
- **규칙 위반 0건** — 표본 누락·영어 향 용어·향수 이름·문장 중복 전부 0
- Stage 1 구조화 **600/600 성공, 스키마 통과 100%** (`gpt-5.4-nano`, 프롬프트 v1 무수정)
- `spec.md` §7.1 LLM 1 응답 시간 — **mean 1.52초 / p95 2.01초** (600건).
  기존 기록(1.96 / 3.19초, 200건)보다 빠르지만 **원인을 모른다.**
  길이로는 설명되지 않는다 — golden 200 이 30자로 가장 짧은데 가장 느리다.
  측정 시점·서버 부하·모델 버전을 분리해 재지 않았다
- **순환의 크기 `A − B` = 0.15~0.16** — 갈래 A `P@5` 0.30 / 갈래 B 0.15

**Trade-off**

- **사람 검수에서 갈래 A 의 어색함이 지적됐다** (2026-09-13, 30건). **v1 유지로 결정.**
  원인은 **생성 규칙 2** 다 — 영어 향 용어를 금지했는데 `11_rule_lexicon.csv` 의
  한글 accord 10개가 전부 그 외래어 음차(`우디`·`플로럴`·`시트러스`…)였다.
  여러 단어 accord(`warm spicy`·`white floral`)를 한국어로 직역하면서 어색해졌고,
  그 입력이 갈래 A 의 **15%** 다. 상세는 `analysis_outputs/32_evalset_run_log.md`
- **실사용 성능을 말하지 못한다.** 합성 문장이고 `people >= 500` 인기 향수에 치우쳐 있다
- **순환이 남아 있다.** 갈래 A 는 검색 인덱스와, 갈래 B 는 사전 근거(N4 의 `ai_summary`)와 겹친다
- **원본 향수를 거의 못 찾는다.** 600문장 중 상위 5개에 든 것이 **2건**, 랜덤 기대값은 8건으로
  **랜덤보다 낮다.** 조건을 만족하는 향수가 중앙값 6,098개라 원본이 1~5위일 이유가 없고,
  검색에 note 를 쓰지 않았다. 그래서 주 지표로 삼지 않았다
- **모델을 통제하지 못한다.** 사람이 웹 UI 로 돌려 온도·seed 를 고정할 수 없다.
  실제로 붙여넣은 프롬프트가 저장본과 같은지도 대조하지 않았다 (`32_evalset_run_log.md`)
- **사전의 기여를 재기에 불리하다.** 사전의 값어치는 LLM 상식과 다른 지점
  (`머스크` 의 한국어 드리프트, `foresty` 의 코퍼스 1건)인데 AI 가 만든 문장에는
  그런 경우가 잘 나오지 않는다. 실제로 사전을 넣으면 갈래 A 의 `P@5` 가 **−0.0095 떨어졌다** —
  채점 기준 `C` 에 없는 accord 가 더해지기 때문이며 **사전이 틀린 것이 아니다**
- **`P@5` 로는 하드/소프트를 구분하지 못한다.** 차이가 0.001 이었다 (N7 근거 1)

---

**재현 방법**

```bash
cd EDA
export PYTHONIOENCODING=utf-8
# 32_evalset_build_inputs.ipynb    API 0회. perfumes.jsonl 을 한 번 훑는다 (수 분)
#   → 사람이 GPT 에 업로드해 문장 생성 → Downloads 에 CSV 8개
# 33_evalset_collect_verify.ipynb  API 0회. raw/ 로 복사 후 검증·해시 고정
# 34_evalset_stage1_structuring.ipynb  API 600회. 체크포인트가 있으면 재호출 없음
# 35_evalset_baseline.ipynb        API 0회. 기준선 채점
```

**관련 자료**

- `docs/plans/NLR_EVALSET_PREREGISTRATION.md` — 생성 전에 고정한 규칙과 채점 기준
- `evaluation_data/nlr_evalset/` — 원본 8개(`raw/`) · 정규화본 2개 · `checksums.json`
- `analysis_outputs/32_evalset_run_log.md` — 생성 실행 기록. 미기재 항목 포함
- `analysis_outputs/33_evalset_distribution.csv` — 분포 비교
- `analysis_outputs/35_baseline_scores.csv` — 조건 3 × 검색 2 × 갈래 2 기준선
- `DECISIONS.md` N4 — 갈래 B 의 순환이 여기서 온다
- `DECISIONS.md` N7 — 이 평가셋으로 내린 결론
- `docs/nlr_engineering_notes.md` 5번 — `description` 안의 계열 라벨

---

## N9. 정렬은 향 조건이 정하고 인기도는 동점만 푼다

- 날짜: 2026-09-13
- 상태: **제안** — 측정은 끝났으나 팀 승인 전

**문제** — 장면 쿼리 7개를 실제로 돌려보니 `strength` 합만으로 정렬한 결과가 쓸 수 없었다.

```
이불 같은 포근한 향   1위 White Cotton Hair & Body Mist   평가자 0명 · 평점 0.0
휴양지에서 쓸 향수     1·2·4위가 전부 Victoria's Secret    브랜드 3종
빨래 냄새 나는 향수    1위 평가자 2명, 4위 평가자 2,358명
```

`P@5` 로는 보이지 않았다. 점수가 0.30 이었는데 그 점수가 **평가자 0명짜리 향수를
"맞다" 고 세고 있었다.** 채점 기준 `C` 가 accord 만 보기 때문이다.

`people` 컬럼이 `perfumes.csv` 에 이미 있으므로 데이터 문제가 아니라 정렬 식 문제다.

**검토한 방법**

1. `strength` 합 + 평점 가중
2. `strength` 합 × log(평가자 수)
3. 평가자 N명 이상만 남기고 `strength` 합 — MAP `D3` 이 `people>=500` 을 쓴 전례
4. **정렬 순서를 바꾼다 — 동점일 때만 평가자 수로 깬다**

**결정** — (4)를 주로 하고 (3)을 낮은 문턱으로 함께 쓴다.

- **1순위 `strength` 합 내림차순, 2순위 평가자 수 내림차순, 3순위 id 오름차순**
- **평가자 10명 미만 제외.** MAP 의 500이 아니라 10이다 (근거 3)
- **브랜드당 최대 1개**
- **조건 완화 사다리를 켠다** — 결과 3개 미만이면 AND→OR, 그래도 부족하면 문턱 해제
- (1)과 (2)는 **기각.** 근거 1·2

---

### 근거 1 — 평점 가중은 문제를 정확히 반대로 악화시킨다

평점은 평가자가 적을수록 극단으로 간다.

| 평가자 | 향수 수 | 평점 표준편차 | 평점 4.5 이상 | 평점 5.0 |
|---|---:|---:|---:|---:|
| 1–2명 | 19,004 | **1.08** | **46.1%** | **36.18%** |
| 3–10명 | 28,533 | 0.62 | 23.2% | 5.31% |
| 11–50명 | 31,419 | 0.40 | 7.7% | 0.07% |
| 51–200명 | 20,388 | 0.29 | 2.3% | 0.00% |
| 1,000명+ | 4,100 | 0.23 | 0.8% | 0.00% |

**평점 5.00 인 향수 8,410개 중 99.8%가 평가자 10명 이하다.**

계수를 올릴수록 `빨래` top5 의 평가자 중앙값이 **29 → 4** 로 내려간다. 계수와 무관하게
방향이 틀렸다. `비 오는 숲` 에 적용하면 상위 5개가 전부 평점 5.00 · 평가자 1~5명이 된다.

### 근거 2 — 곱셈은 인기가 향 조건을 이긴다

`strength` 합 × log10(1+평가자 수) 로 정렬했을 때, top5 안에서 가장 약한 `core` accord 의 강도.

| 정렬 | 비 오는 숲 | 빨래 | 이불 | 휴양지 |
|---|---:|---:|---:|---:|
| 현재 (합만) | 100 | 50 | 63 | 99 |
| **곱셈** | 82 | 29 | **13** | 55 |
| 동점만 평가자 수 | 100 | 50 | 63 | 99 |

`이불` 에 곱셈을 걸면 4위가 `soapy=13` 인 향수다. 사용자가 *"이불 같은 향"* 을 물었는데
추천 이유 칸에 *"충족: soapy(strength 13)"* 이라고 써야 한다. **설명이 근거를 스스로 약화시킨다.**

### 근거 3 — MAP 의 `people>=500` 은 그대로 못 쓴다. 문제가 다르다

MAP `D3` 은 코퍼스 13만에서 1,000개를 고르는 문제였다. 우리는 **하드 AND 로 이미 좁혀진
수백 개 풀**에서 5개를 고른다.

| 문턱 | 이불 후보 | 이불 top5 최약 core | 합성 600건 중 후보 5개 이상 |
|---:|---:|---:|---:|
| 0 | 519 | 87 | 58.2% |
| **10** | 265 | 75 | **57.8%** |
| 30 | 162 | 75 | 57.2% |
| 50 | 124 | **36** | 57.0% |
| 500 | 29 | **13** | 51.8% |

**500을 걸면 풀이 29개로 얇아져 최약 core 가 13까지 떨어진다.** 10~30이 결함을 고치면서
커버리지 손실이 0.4~1.0%p 다. 10을 택했다.

### 근거 4 — 동점 깨기는 아무것도 버리지 않는다

완화가 걸려 동점이 수천 개일 때 차이가 극적이다.

```
"시트러스" 한 조건만 남았을 때 — 동점 8,704개

id 순      Orange Tonic(95명) · Cristalle(4,053) · Habit Rouge(4,536) · Terre d'Hermès(27,711)
평가자 순   Light Blue(35,695) · Coco Mademoiselle(32,839) · Terre d'Hermès(27,711) ·
           Club de Nuit Intense(27,223) · Acqua di Gio(24,626)
```

기존 id 순 정렬은 **"오래된 = 유명한" 순서를 우연히 만들고 있었다.** 재현은 되지만 근거가 없다.

합성 600건 전체로 보면 상위 5개에 든 평가자 0명 향수가 **249개 → 165개**(34% 감소),
평가자 중앙값이 **9명 → 15명** 이 된다.

**점수가 같을 때만 개입하므로 향 조건이 여전히 순위를 정한다.** 후보를 버리지 않는다.

### 근거 5 — 완화를 켜면 커버리지가 19%p 오른다

`spec.md` §3 의 완화 사다리가 설계만 되어 있고 꺼져 있었다.

| | 합성 600건에서 결과가 나오는 비율 |
|---|---:|
| 하드 AND 만 | 350/600 = **58.3%** |
| **완화 사다리 켬** | 466/600 = **77.7%** |

조건을 하나라도 뽑으면 **결과가 0개인 경우가 0건**이 된다.

**대가** — 완화로 넘어간 116건은 5위 동점이 **중앙값 3,204개**다. 향 조건이 순위를 거의
정하지 못하고 평가자 수가 정한다. 그래서 응답의 `diagnostics.stage` 로 `AND` 와
`RELAXED_OR` 를 구분해 화면이 알릴 수 있게 했다.

---

**결과**

| | 전 | 후 |
|---|---|---|
| `이불` 1위 | White Cotton (평가자 **0명**) | Hair Salon Grooming (40명) |
| `휴양지` 브랜드 | 3종 (VS 3개) | **5종** |
| `휴양지` 평가자 중앙 | 7명 | **83명** |
| `비 오는 숲` 상위 | 평가자 0명 포함 | Mitsouko (5,249명) 3위 |
| 합성 600건 커버리지 | 58.3% | **77.7%** |
| 상위5 중 평가자 0명 | 249개 | **165개** |

`spec.md` §8 의 **13번(정렬에 인기도가 없다)과 14번(브랜드 독점)이 이것으로 닫힌다.**

**Trade-off**

- **완화를 켜면 "그럴듯한데 근거가 약한" 결과가 19.3% 생긴다.** 겉보기에 정상이라
  `stage` 를 화면에 노출하지 않으면 사용자가 정밀한 추천으로 오해한다
- **평가자 10명 문턱이 롱테일을 버린다.** 무명 향수를 발견하는 경험이 줄어든다.
  프로젝트 목적이 "탐색" 인 것과 상충할 수 있다
- **브랜드 1개는 [제안]이다.** 측정으로 정한 값이 아니라 눈으로 보고 정했다
- **동점 깨기가 인기도 편향을 들여온다.** 유명한 향수가 계속 상위에 남는다.
  조사 문헌이 경고한 popularity bias 의 한 형태이며, catalog coverage 를 재보지 않았다
- **점수 식 자체는 여전히 미검증이다.** `strength` 단순 합이고, 쿼리→향수 점수 식이
  옳은지는 이번에도 답하지 않았다

**재현 방법**

```bash
cd EDA
export PYTHONIOENCODING=utf-8
# delivery/build_delivery.py 를 실행하면 장면 쿼리 7개와 합성 600건을 다시 돌린다
# API 호출 0회. 재현 게이트(accord 92개 perfume_count)가 먼저 통과해야 진행된다
#
# 출력:
#   delivery/for_backend/example_response.json
#   delivery/verification.json
```

**관련 자료**

- `delivery/for_backend/search_rules.md` — 규칙 명세. `[측정]` / `[제안]` 구분
- `delivery/verification.json` — 이 결정을 반영한 뒤 실제로 잰 값
- 팀 저장소 `ai/nlr_engine.py` — 구현. MR `[S15P21E203-201][feat]`
- `DECISIONS.md` N2 — 단독 매핑 금지. 완화 4단계가 이 규칙을 일시적으로 푼다
- `MAP/docs/DECISIONS.md` D3 — `people>=500` 전례. 문제가 달라 그대로 쓰지 않았다

---

## N10. 직역 대응은 단독 매핑을 허용한다 — N2가 비워 둔 임계값을 채운다

- 날짜: 2026-09-14
- 상태: **제안** — 측정은 끝났고 사용자 승인(2026-09-14), 팀 승인 전

**문제** — 사전 v1.8 이 N2("한 표현은 accord 2개 이상. 단독 매핑을 금지한다")를 다섯 곳에서
어겼다. 반영하면 규칙 위반이고, 반영하지 않으면 측정된 개선을 버린다.

```
과육  = fruity     꿀 = sweet       나무 = woody
상큼한 = citrus     풀 냄새 = green
```

다섯 개가 전부 **표현이 accord 이름의 한국어 직역**이다. 사전의 근거 칸도 그렇게 적혀 있다 —
*"팀 문서 Woods 장 핵심 형용사에 '나무 같은'·'우디한' 이 있다. 직역 대응이라 추론 단계가 없다."*

**검토한 방법**

1. 다섯 항목에 accord 를 하나씩 더 붙여 N2 를 지킨다
2. N2 의 "단독 금지" 를 그대로 두고 v1.8 을 포기한다
3. N2 가 **한계 5번에서 비워 둔 임계값**을 이번 측정으로 채운다

**결정** — (3)을 택했다.

- **한 표현은 accord 2개 이상으로 매핑한다.** 다만 **표현이 accord 이름의 직역이고 다른
  accord 를 붙일 근거가 없으면 단독 매핑을 허용한다.**
- **단독 매핑만으로 조건이 잡힌 응답은 그 사실을 표시한다.** 검색 단계를 `SINGLE_BROAD` 로
  두고 `diagnostics.relaxed` 에 이유를 적는다. `status` 는 `OK_RELAXED` 를 유지한다.

(1)을 기각한 이유는 **근거가 없어서**다. 팀 문서 `perfume_14families_korean_descriptors.md` 는
계열별 한국어 형용사 목록이고 두 번째 accord 를 가리키는 내용이 없다. `나무 → woody + dry woods`
로 붙일 수는 있으나 문서의 Dry Woods 장은 `건조한`·`스모키한`·`그을린` 처럼 다른 말을 담고 있다.
없는 매핑을 지어내는 것이 되고 `AGENTS.md` 의 *"정의된 근거 없이 매핑을 만들지 말라"* 에 걸린다.

---

### 근거 1 — N2 의 결정문이 자기 근거보다 넓었다

N2 근거 1의 표를 다시 본다.

| accord | 보유 향수 | 단독 5위 동점 |
|---|---:|---:|
| `citrus` | 59,969 | **17,736** |
| `warm spicy` | 45,319 | 6,281 |
| `fresh` | 33,478 | 830 |
| `earthy` | 19,049 | 533 |
| `aquatic` | 8,872 | 609 |
| `soapy` | 1,720 | **18** |
| `foresty` | 1 | 1 |

`soapy` 단독은 5위 동점이 18개다. **단독으로도 순위가 만들어진다.** N2 본문도
*"7종 중 **5종이** 단독으로는 순위를 만들지 못한다"* 로 적었다. 2종은 만든다.

그리고 N2 는 **한계 5번**에 이렇게 남겨 뒀다.

> **단독 매핑 금지의 임계값을 정하지 않았다.** "동점 50개 이하" 는 읽기 편하게 나눈 서술용
> 구분이고 GO/STOP 기준이 아니다. **어느 동점 규모까지 허용할지는 정하지 않았다.**

즉 근거는 *"넓은 accord 는 단독으로 안 된다"* 인데 결정문은 *"단독을 전부 금지한다"* 로 적혔다.
이 결정은 그 빈칸을 **동점 규모가 아니라 표현의 성격**으로 채운다.

### 근거 2 — 비유와 직역은 단독일 때 잃는 것이 다르다

`빨래 → soapy + fresh` 에서 `fresh` 를 빼면 **정보가 빠진다.** 장면 표현은 여러 accord 를
동시에 불러오기 때문이다. `나무` 는 다르다. **woody 가 전부**여서 뺄 것도 더할 것도 없다.

N2 가 `머스크 → musky` 를 *"틀린 매핑이 아니라 무효한 매핑"* 이라 한 것도 같은 구분이다.
한국어 `머스크` 는 musky 보다 넓은 뜻으로 쓰여서 단독 매핑이 의미를 잘라 먹는다.
`나무` 에는 그 드리프트가 없다.

### 근거 3 — 실제로 혼자 조건이 되는 비율 (합성 600문장)

| 표현 | accord | 잡힌 문장 | 혼자 조건이 됨 | 그때 5위 동점 |
|---|---|---:|---:|---:|
| 나무 | `woody` | 210 | 34 (16.2%) | 중앙 9,483 |
| 상큼한 | `citrus` | 131 | 16 (12.2%) | 중앙 8,704 |
| 과육 | `fruity` | 29 | 3 (10.3%) | 중앙 5,811 |
| 풀 냄새 | `green` | 29 | 4 (13.8%) | 중앙 1,544 |
| 꿀 | `sweet` | 12 | **0 (0%)** | — |

**대부분 다른 표현과 함께 잡혀 조건이 좁아진다.** 혼자가 되는 것은 10~16% 다.
조건이 accord 하나뿐인 문장은 전체에서 82건 → 91건으로 9건 늘었고,
그때의 5위 동점 중앙값은 **8,704개로 v1.2 와 같았다.**

혼자일 때는 N2 의 걱정대로 인기도가 순위를 정한다. 다만 결과가 제비뽑기로 보이지는 않는다.

```
"생화와 상큼한 과일을 같이 떠올리게 하는 향"  ->  조건 citrus 하나
  1. Light Blue           평가자 35,695
  2. Coco Mademoiselle    평가자 32,839
  3. Terre d'Hermès       평가자 27,711
```

**항목이 없으면(v1.2) 같은 문장이 결과 0개였다.** "가장 유명한 시트러스 향수들" 과
"이해하지 못했습니다" 중에서 고르는 문제다.

---

**결과**

사전 v1.2 → v1.8. 향수 데이터·검색 코드·평가셋을 고정하고 사전만 바꿔 쟀다.

| | v1.2 (44행) | v1.8 (53행) |
|---|---:|---:|
| NDCG@5 | 0.4110 | **0.4688** (+14.1%) |
| 조건충족률 | 0.5206 | **0.5908** |
| 조건을 뽑은 문장 — LLM 있음 | 466 / 600 (77.7%) | **520 / 600 (86.7%)** |
| 조건을 뽑은 문장 — **LLM 없음** | 214 / 600 (35.7%) | **453 / 600 (75.5%)** |

**마지막 줄이 실제 체감이다.** 서버에 아직 LLM 구조화 호출이 없다.

검색 단계 표시도 함께 고쳤다. 조건이 하나면 AND 와 OR 이 같은 집합인데 예전에는
`RELAXED_OR` 로 떨어져 *"조건을 넓혀 찾았습니다"* 가 떴다. 넓힌 적이 없으므로 거짓이었다.

| `stage` | 전 | 후 |
|---|---:|---:|
| `AND` | 391 | 391 |
| `SINGLE_BROAD` | — | **91** |
| `RELAXED_OR` | 129 | **38** (진짜 완화만) |
| `NO_CONDITION` | 80 | 80 |

`status` 분포는 바뀌지 않는다(`OK 391 / OK_RELAXED 129 / NO_CONDITION 80`).
**연동 형식이 안 바뀌고** NDCG@5 도 0.4688 로 동일하다 — 라벨만 정직해졌다.

**Trade-off**

- **혼자 조건이 되는 10~16% 에서는 향 조건이 순위를 정하지 못한다.** 인기도가 정한다.
  `SINGLE_BROAD` 표시로 알리지만 **화면에서 무엇을 보여줄지는 아직 정하지 않았다**
- **직역인지 아닌지를 사람이 판단한다.** 기계적 기준이 아니다. `상큼한`=citrus 는 직역이지만
  `깨끗한`=soapy 는 아니다(비누·물 두 방향). 경계가 흐린 표현에서 다툼이 생길 수 있다
- **N2 의 원래 걱정을 없앤 것이 아니라 조건부로 받아들인 것이다.** `citrus` 단독의 동점
  17,736개는 그대로다. 표시를 달았을 뿐이다
- **동점 규모 임계값은 여전히 정하지 않았다.** N2 한계 5번이 그대로 남는다.
  이 결정은 "표현의 성격" 으로 우회했지 임계값을 정하지 않았다

---

**한계 — 이 근거로 말할 수 있는 것과 없는 것**

1. **합성 600문장이다.** 우리가 만든 문제지라 실사용자 말투와 다르다. 설문 155건에서는
   조건을 못 뽑는 비율이 44% 로 훨씬 높다
2. **"직역" 의 정의를 문서로 고정하지 않았다.** 다섯 개는 명확하지만 다음 항목에서
   같은 판단이 반복 가능한지 확인하지 않았다
3. **`SINGLE_BROAD` 일 때 사용자가 어떻게 느끼는지 재지 않았다.** "유명한 시트러스 향수" 가
   납득되는지 불만인지는 실사용 기록이 있어야 안다
4. **혼자 조건이 되는 비율 10~16% 는 이 평가셋 기준이다.** 실사용에서는 문장이 짧아
   더 높을 수 있다

---

**재현 방법**

```bash
cd EDA
export PYTHONIOENCODING=utf-8
python 41_lexicon_ndcg.py \
  ../../S15P21E203/ai/data/domain_lexicon_v1_2.csv \
  ../../S15P21E203/ai/data/domain_lexicon_v1_8.csv
# API 호출 0회. 사전만 바꾸고 나머지는 고정한다
```

**관련 자료**

- `DECISIONS.md` N2 — 이 결정이 채우는 빈칸(한계 5번)
- `DECISIONS.md` N9 — 정렬 규칙. `SINGLE_BROAD` 구간에서 동점 깨기가 순위를 정한다
- `EDA/data/scent_knowledge/domain_lexicon_v1_8.csv` — 사전
- `EDA/analysis_outputs/47_lexicon_v1_8_changelog.md` — v1.7 → v1.8 변경 내역
- 팀 저장소 `ai/nlr_engine.py` — `SINGLE_BROAD` 구현
- 팀 저장소 `ai/docs/search_rules.md` — 규칙 명세

---

## N11. 표현을 accord 가중 조합으로 바꾸지 않는다 — 세 번 재봤고 개선이 없었다

- 날짜: 2026-09-15
- 상태: **제안** — 측정은 끝났고 사용자 승인(2026-09-15), 팀 승인 전

**문제** — 254 로 ①단계 LLM 구조화가 붙으면서 `avoid` 에 처음으로 값이 들어온다.
회피 처리 규칙은 `spec.md` §8 11번이 *"회피가 이름 동일성만 쓴다고 **가정했다**"* 로
미결로 남겨 뒀다. 가정을 결정으로 바꿔야 한다.

그 과정에서 더 근본적인 질문이 나왔다. **향은 여러 accord 의 조합으로 인상이 정해지는데
사전은 표현 하나에 accord 를 1~2개만 연결한다.** 그러면 회피할 때 accord 하나를 빼는 것이
조합을 깨뜨린다. 표현을 accord 의 **가중 조합**으로 정의하고 회피 시 해당 향의 가중치를
줄이는 방식이 맞지 않느냐는 제안이 나왔다.

레퍼런스 조사(`자연어 향수 추천의 회피 로직 설계`, 2026-09-15)도 같은 방향을 권했다 —
*"component 를 삭제하는 대신 concept 를 회피한다"*.

**검토한 방법**

1. 사전 구조를 손본다 — `relation_type` 에 `IDENTITY` 를 더해 N10 직역 5행을 재라벨하고,
   엔진 상수 `KOREAN_ACCORD` 음차를 사전으로 옮긴다
2. accord 를 늘리고 하드 AND 를 가중 점수로 바꾼다 — 배수가 지지하는 accord 를 모두 넣고
   IDF 로 비중을 준다
3. core 는 AND 로 두고 나머지는 가점으로만 쓴다 — 사전의 `optional` 을 살린다
4. **현행을 유지한다** — 하드 AND, `core` 만 사용

**결정** — (4)를 택했다. 1·2·3 을 모두 구현해 재봤고 **어느 것도 개선을 보이지 못했다.**

- **사전 구조를 바꾸지 않는다.** `relation_type` 에 값을 더하지 않고 음차도 옮기지 않는다
- **하드 AND 를 유지한다.** 가중 점수로 바꾸지 않는다
- **`optional` 을 계속 쓰지 않는다.** 가점 항을 넣지 않는다

**이것은 "방향이 틀렸다" 가 아니라 "지금 확인할 수단이 없다" 는 판단이다.** 아래 한계 2번.

---

### 근거 1 — 사전 구조만 바꾸면 아무것도 안 바뀐다

엔진이 읽는 사전 컬럼은 8개(`expression` · `aliases` · `entry_id` · `candidate_name` ·
`required` · `match_condition` · `rationale` · `evidence_tier`)뿐이고 `relation_type` 은
그중에 없다. 변형 사전 둘을 만들어 600문장을 다시 돌렸다.

| 사전 | 조건을 뽑은 문장 | NDCG@5 | 조건충족률 | 회피 도달 |
|---|---:|---:|---:|---:|
| v1.8 | 520/600 | 0.4688 | 0.5908 | 8/59 |
| A (IDENTITY 재라벨 5행) | 520/600 | 0.4688 | 0.5908 | 8/59 |
| B (A + 음차 9행 이전) | 520/600 | 0.4688 | 0.5908 | 8/59 |

**세 사전이 완전히 같다.** B 가 안 바뀌는 이유는 엔진이 `KOREAN_ACCORD` 상수와 사전을
둘 다 훑기 때문이다 — 중복이지 경쟁이 아니었다. 사전을 옮겨도 커버리지가 안 변한다.

### 근거 2 — 하드 AND 는 좁혀서 나쁜 게 아니라 좁혀서 맞았다

배수가 지지하는 accord(`신호=True` · `순환≠True`)를 모두 넣고 AND 를 걸면 후보가 죽는다.

| 표현 | 사전 | 확장 | 현재 후보 | 확장 AND 후보 |
|---|---:|---:|---:|---:|
| 빨래 | 2 | 7 | 518 | **0** |
| 휴양지 | 2 | 7 | 784 | **0** |
| 깨끗한 | 3 | 6 | 32 | **0** |
| 촉촉한 | 2 | 7 | 1,789 | 2 |
| 비 오는 숲 | 2 | 5 | 2,591 | 25 |

**사전이 2개에 머문 이유가 데이터로 확인된다.** 근거가 있어도 넣으면 검색이 죽는다.

그래서 AND 를 버리고 IDF 가중 점수로 바꿨더니 후보는 3~4만으로 살아났지만 **결과가
명백히 나빠졌다.**

```
빨래     현재 1위 Laundry(soapy=100)  ->  1위 Snow Palace(soapy 없음, aldehydic=100)
                                          Laundry 는 5위로 밀림.  상위 5 겹침 1/5
휴양지   현재 coconut=100 tropical=100 들  ->  marine·salty 가 올라옴.  겹침 0/5
깨끗한 0/5 · 비 오는 숲 0/5 · 촉촉한 1/5 · 이불 2/5
```

**"반드시 있어야 하는 조건" 이 사라지자 조연 accord 들이 합쳐서 주연을 이겼다.**
`빨래` 의 가중치는 `soapy` 25% 인데 나머지 6개가 75% 였다.

### 근거 3 — 가점을 붙일 자리가 거의 없고, 크기를 정할 근거도 없다

`core` 는 AND 로 두고 `optional` + 확장 accord 를 가점으로만 쓰는 방식을 두 강도로 쟀다.

| | NDCG@5 | 조건충족률 | 가점이 붙은 문장 |
|---|---:|---:|---:|
| 현재 | 0.4406 | 0.6853 | — |
| H1 동점일 때만 | 0.4399 | 0.6846 | 49/600 |
| H2 점수에 더함 | 0.4388 | 0.6834 | 49/600 |

**둘 다 소폭 하락했다.** H1 은 동점이 드물어 개입할 자리가 거의 없었고(표현별 상위 5
겹침 5/5), H2 는 바꾸긴 하지만 `빨래` 에서 `soapy=40` 짜리가 3위로 올라오는 식이었다.

> 이 표의 0.4406 은 `41_lexicon_ndcg.py` 의 0.4688 과 다르다. 세 방식을 같은 조건에서
> 비교하려고 `search()` 를 다시 구현하면서 **완화 사다리를 넣지 않았다.**
> 세 줄끼리는 비교되지만 41번 값과 직접 비교하면 안 된다.

H2 의 가점 최대치는 100점이라 `core` accord 하나 분량이다. 줄이면 완화되겠지만
**그 계수를 정할 근거가 없다.** 정하는 순간 결과는 방법이 아니라 그 숫자가 만든 것이 된다.

### 근거 4 — 가중치로 쓸 숫자가 없다

두 후보를 검토했고 **둘 다 이 용도에는 맞지 않았다.**

**배수(lift, 30번 측정)** — 사전에 `core` 로 들어간 27칸 중 쓸 수 있는 것이 10칸이다.

```
지지  10칸   깨끗한 soapy/aquatic/fresh · 빨래 soapy · 달달 caramel
             이불 powdery · 촉촉한 aquatic/green · 휴양지 coconut
순환   4칸   달달 sweet · 빨래 fresh · 휴양지 tropical · 머스크 musky
             검색어가 accord 이름과 같아 부풀려진 값. "관계 없음" 이 아니라 "못 잼"
반증   2칸   머스크 soapy · 이불 soapy
미측정 11칸  과육 · 꿀 · 나무 · 상큼한 · 풀 냄새 · 분내 · 하얀 꽃 · 비 오는 숲
```

**조합을 이루는 두 accord 가 모두 지지를 받는 표현은 세 개뿐이다** — 깨끗한 · 촉촉한 ·
비 오는 숲(mossy·earthy 는 미측정이라 실제로는 둘). `빨래` 는 `soapy` 만, `달달` 은
`caramel` 만, `휴양지` 는 `coconut` 만 쓸 수 있다. **가중 조합을 하려면 두 값이 다
있어야 하는데 핵심 표현들이 한쪽만 있다.**

그리고 `ai_summary` 는 131,930개 중 12,432개(9.4%)에만 붙고, 그 집단의 평가자 수
중앙값이 628 명이다(없는 쪽은 9명). 30번 본문이 *"롱테일에는 적용을 보증할 수 없다"* 로
직접 적어 뒀다. 실제로 `이불 → caramel` 배수 7.36 을 가중치로 넣었더니 상위 5개의
`caramel` 이 **전부 0** 이었다.

**IDF(우리 데이터 129,161개)** — 구멍이 없고 편향도 없다. 20칸이 전부 채워진다.
그러나 **의미를 모른다.** 드물다는 것과 그 표현과 관련 있다는 것은 다르다.

```
이불    soapy 86% · powdery 14%      IDF 는 soapy 를 주인공으로 만든다
                                     그런데 30번 측정은 이불 -> soapy 를 반증으로 판정했다
분내    musky 61% · powdery 39%      분 냄새인데 musky 가 더 높다
```

**두 숫자가 답하는 질문이 다르다.** 배수는 *"이 매핑이 옳은가"*(사전에 넣을지),
IDF 는 *"이 accord 가 변별력 있는가"*(순위를 매길 때)다. 어느 쪽도 *"이 표현에서 이
accord 가 얼마나 기여하는가"* 를 재지 않았다.

---

**결과**

세 방식 모두 채택하지 않았다. 현행 유지로 `NDCG@5 0.4688 · 조건충족률 0.5908` 이
그대로다.

**측정 과정에서 따로 남길 발견 셋**

1. **`ozonic` 이 네 표현에 공통으로 신호가 잡힌다** — 빨래 · 깨끗한 · 촉촉한 · 비 오는 숲.
   "맑은" 계열의 공통 축으로 보이는데 **사전에 한 번도 들어 있지 않다**
2. **반증 판정을 받은 매핑 2행이 아직 `core` 다** — `머스크 → soapy` · `이불 → soapy`.
   30번이 *"해당군에 지지도 없음"* 으로 판정했는데 필수 조건으로 남아 있다.
   같은 판정을 받은 `호텔 → soapy` · `차가운 → ozonic` · `차가운 → fresh` 는
   `optional` + `NO_MAPPING` 으로 강등돼 실제로 안 쓰인다(N5 의 *"강등하되 교체하지
   않는다"*). **왜 둘만 남았는지는 확인하지 못했다** — 의도인지 누락인지 모른다
3. **엔진이 `optional` 을 아예 버린다** (`if row["required"] != "core": continue`).
   `search_rules.md` 는 *"optional 은 점수용"* 이라고 적어 뒀지만 구현이 없다

**Trade-off**

- **회피가 거의 동작하지 않는 상태가 유지된다.** 회피 문구 59개 중 accord 에 닿는 것이
  8개다. `too sweet` · `유치한` · `끈적` 같은 말은 그대로 무시된다
- **사전이 표현당 accord 1~2개에 머문다.** 근거가 있는 `ozonic` 등이 못 들어간다.
  들어가면 하드 AND 가 후보를 0으로 만들기 때문이다
- **조합으로 인상이 정해진다는 도메인 사실을 모델이 표현하지 못한다.** `빨래` 를
  `soapy + fresh` 두 개로 근사하는 상태가 계속된다
- **`optional` 컬럼이 계속 죽은 데이터로 남는다**

---

**한계 — 이 근거로 말할 수 있는 것과 없는 것**

1. **"개선이 없다" 까지만 말할 수 있고 "나쁘다" 는 말할 수 없다.** H1·H2 의 하락은
   −0.0007 ~ −0.0018 이다. 이 크기로 방향을 단정하지 않는다
2. **NDCG 가 이 변화를 평가할 수 있는 지표가 아니다.** 정답 키 `C` 는 조건 accord 만
   담고 가점 accord 는 채점에 안 들어간다. *"Snow Palace 가 빨래스러운가"* 를 이 지표가
   답하지 못한다. **레퍼런스 조사가 사람 판정을 요구한 지점이다**
3. **가중치 후보를 둘만 봤다.** 배수와 IDF 외에 다른 근거(예: 새로 측정한 기여도)를
   쓰면 결과가 달라질 수 있다
4. **확장 대상이 5개 표현뿐이었다.** 나머지는 측정이 없거나 직역이라 확장 대상이 아니다
5. **단일 실행이다.** 세 스크립트 모두 결정론적이라 반복 실행에 의미는 없으나,
   사전이나 평가셋이 바뀌면 다시 재야 한다

**다시 볼 조건** — 아래 중 하나가 생기면 이 결정을 다시 연다.

- 회피 품질을 사람이 판정한 평가셋이 생겼을 때 (over-suppression / under-suppression)
- 표현별 accord 기여도를 직접 측정했을 때 (배수·IDF 가 아닌 것)
- 미매칭 로그가 쌓여 실사용 회피 표현의 분포를 알게 됐을 때

---

**재현 방법**

```bash
cd EDA
export PYTHONIOENCODING=utf-8
./venv/Scripts/python.exe 44_avoid_lexicon_variants.py      # 사전 변형 A·B
./venv/Scripts/python.exe 46_weighted_concept_expansion.py  # 확장 + AND 제거 + IDF 가중
./venv/Scripts/python.exe 47_core_and_weighted_bonus.py     # core AND + 가점
# API 호출 0회. 원본 사전을 덮어쓰지 않는다 — 변형은 임시 폴더에만 만든다
```

**관련 자료**

- `DECISIONS.md` N2 — 하드 AND 와 단독 매핑 금지. 근거 2가 그 결정을 뒷받침한다
- `DECISIONS.md` N4 — 배수(lift) 측정. 순환·반증 판정의 출처
- `DECISIONS.md` N9 — 정렬 규칙. 가점을 넣는 것은 이 결정을 다시 여는 일이다
- `DECISIONS.md` N10 — 직역 5개 판정. 근거 1의 재라벨 대상
- `analysis_outputs/30_ai_summary_signal_report.md` — 순환·반증·커버리지 편향
- `analysis_outputs/30_expression_accord_lift.csv` — 표현×accord 배수 781행
- `EDA/43_avoid_rule_measurement.py` — 회피가 지금 얼마나 도는가(59개 중 8개)
- `EDA/45_avoid_matching_modes.py` — 매칭 방식 비교. 별건이나 같은 맥락
- 레퍼런스 조사 `자연어 향수 추천의 회피 로직 설계` (2026-09-15, 팀 외부 자료)

---

## N12. 회피도 사전을 경유한다 — 다만 하드 제외는 사전 자신이 부정한다

- 날짜: 2026-09-16
- 상태: **제안** — 측정은 끝났고 사용자 승인(2026-09-16), 팀 승인 전 · **미구현**

**문제** — `spec.md` §8 11번이 회피 규칙을 미결로 남겨 뒀고, N11 이 "가중 조합으로는 안
된다" 까지만 답했다. 남은 질문이 둘이었다.

```
Q1  회피도 사전을 태울 것인가
Q2  태워서 잡은 accord 를 어떻게 뺄 것인가
```

지금 엔진은 회피를 `_normalize_accord` 로만 푼다 — accord 영어 이름과 (거의) 완전히
같을 때만 닿고, **사전 경유 경로가 프로덕션에 아예 없다.**

**검토한 방법**

1. 현행 유지 — accord 이름 직접 지목만 받는다
2. 사전을 태우고 잡힌 accord 를 **하드 제외**한다 (지금 `search()` 의 회피 의미)
3. 사전을 태우되 **직접 지목만 하드, 사전 경유분은 점수 감점**한다
4. 회피 전용 매핑 열을 새로 만든다

**결정** — (3)을 택했다. **아직 구현하지 않았다.**

- **Q1: 회피도 사전을 경유한다.** 사용자 결정(2026-09-16)
- **Q2: 사전 경유분을 하드로 빼지 않는다.** 점수에서 감점한다
- **(4)는 기각.** 새 열이 필요 없다 — `relation_type` 이 이미 그 정보를 담는다. 근거 3
- 계수 λ 는 **튜닝하지 않는다.** N11 의 가점 계수와 같은 이유

---

### 근거 1 — Q1 은 도달을 71% 늘리고 guardrail 을 깨지 않는다

사전 v1.14 · 600문장 · `57_avoid_lexicon_routing.py`.

> **⚠ 71% 는 합성 600 기준이다. 실사용에서는 +13.6% 다** (2026-09-17 · 측정 기록 21번).
> 설문 143건의 회피 항목 65개로 같은 정의로 재니 **22/65(33.8%) -> 25/65(38.5%)** 였다.
> 설문 쪽 도달률이 애초에 높아서다 — 사용자가 `citrus` · `floral` · `Rose` 처럼 **영어
> accord 이름을 그대로 쓴 경우가 많았다.** 사전 v1_8 과 v1_14 가 같은 결과를 냈다.
>
> **결정을 뒤집지는 않는다.** 다만 팀 승인 전이므로 두 수를 나란히 두고 판단해야 한다.

| | 현재 | core 경유 |
|---|---:|---:|
| 회피 도달 | 7/59 | **12/59** |
| NDCG@5 | 0.471540 | 0.470684 |
| status 분포 | OK 397 · OK_RELAXED 128 · NO_CONDITION 75 · NO_RESULT 0 | **동일** |
| 검색 단계 분포 | — | **동일** |

결과가 0개가 된 문장도, 완화 사다리를 새로 타는 문장도 없다.

`optional` 까지 경유하면 도달이 19/59 로 더 오르지만 **새로 잡히는 7개 중 4개가 오탐**
이고 전부 `흔한` 항목에서 나온다.

```
X  ordinary citrus · 평범한 과일 향 · 평범한 꽃향 · 흔한 디저트 향  ->  aquatic · marine · salty
```

50번 측정이 말한 것은 *"흔하다고 평가된 향수들이 aquatic·marine·salty 인 경향"* 이고
이건 **어떤 향수가 흔하다고 불리는가** 에 대한 집단 성질이지 `흔한` 의 구성적 의미가
아니다. `평범한 꽃향` 에서 `평범한` 은 `꽃향` 을 수식하는 말이다. 그래서 `core` 만 쓴다.

### 근거 2 — 하드 제외는 사용자가 좋다고 한 조건을 지운다

`58_avoid_hard_vs_soft.py` · 복제한 `search()` 가 600문장에서 원본과 **불일치 0건**임을
먼저 확인하고 쟀다(47번이 완화 사다리를 빠뜨려 비교 불가가 됐던 전례 때문이다).

| | A 현재 | B 경유+하드 | C 경유+소프트 |
|---|---:|---:|---:|
| 회피 도달 | 7/59 | 12/59 | 12/59 |
| NDCG@5 | 0.471540 | 0.470684 | 0.471160 |
| vs A | — | −0.000856 | −0.000379 |

숫자보다 문장이 결정적이다.

```
"달콤한 과일 향을 좋아하는데 단순한 사탕 냄새는 싫어"   회피 ['candy']

A 현재      조건 [caramel, citrus, fruity, sweet]   후보   309
B 경유+하드   조건 [citrus, fruity]                  후보 3,539   <- 선호가 지워졌다
C 경유+소프트  조건 [caramel, citrus, fruity, sweet]   후보   309   <- 살아남았다
```

`spec.md` §8 11번이 걱정한 그대로다 — *"회피가 사전 항목 전체를 쓰면 `빨래` 의 `soapy`
까지 지워져 선호가 통째로 사라진다."*

### 근거 3 — 새 열이 필요 없다. `relation_type` 이 이미 답을 갖고 있다

레퍼런스 조사의 회피 권한 표를 우리 데이터에 적용하면 이렇다.

```
엔진이 싣는 ACCORD 행 36개
   EVOKES           19    조사 표: 회피 hard  X
   ASSOCIATED_WITH  17    조사 표: 회피 hard  X
   IDENTITY          0
   ALIAS_OF          0
```

**하드 회피 자격이 있는 행이 한 줄도 없다.** B 가 나빴던 것이 우연이 아니고, C 의 규칙
(직접 지목은 하드 · 경유는 소프트)이 임의 구분이 아니라 이 표와 같은 결과다 —
`_normalize_accord` 가 잡는 것은 사용자가 accord 이름을 직접 말한 것(사실상 IDENTITY)이고
사전을 경유하는 것은 전부 EVOKES/ASSOCIATED_WITH 다.

다만 **`relation_type` 값이 지금 부정확하다.** N10 이 직역으로 판정한 5행
(`과육`·`꿀`·`나무`·`상큼한`·`풀 냄새`)이 전부 `EVOKES` 로 붙어 있다. `나무 → woody` 는
직역이지 연상이 아니다. 열을 만드는 것이 아니라 **있는 열을 바로잡고 엔진이 읽게 하는
것**이 남은 일이다(지금 엔진은 `relation_type` 을 아예 안 읽는다 — N11 변형 A).

---

**결과**

- Q1·Q2 가 결정됐고 **구현은 하지 않았다.** 팀 저장소 `ai/nlr_engine.py` 변경이 필요하다
- 회피 도달은 켜면 7 → 12회(59회 중 20%)가 된다

**Trade-off**

- **C 가 A 보다 낫다는 것은 증명하지 못했다.** NDCG 는 −0.000379 이고, 정답 키에 회피가
  없어 이 지표가 회피의 이득을 잴 수 없다(N11 한계 2번). **"B 는 확실히 나쁘고 C 는 그
  해악이 없다"** 까지가 정확한 표현이다
- **λ=1.0 은 근거 없는 값이다.** 회피를 평가 못 하는 지표로 튜닝하면 회피를 덜 하는
  방향(λ→0)이 항상 이긴다. 그래서 튜닝하지 않는다
- **표본이 12문장이고 결정적인 예는 1문장이다.** 델타가 N11 이 *"이 크기로 방향을 단정
  하지 않는다"* 고 한 크기와 같다
- **"B 가 나쁘다" 도 제품 판단이 섞여 있다.** 선호가 회피를 이겨야 한다는 전제를 깔고
  있는데, 우리 accord 어휘가 "달콤한 과일" 과 "사탕" 을 구분하지 못하는 이상 어느 쪽을
  이기게 할지는 사람이 정할 문제다
- **효과의 천장이 낮다.** 측정 기록 14번대로 회피가 걸리는 문장은 600 중 12건(2%)이고,
  조건을 아예 못 뽑는 문장이 75건(12.5%)이다

**다시 볼 조건**

- 회피 품질을 사람이 판정한 평가셋이 생겼을 때 (C vs A 를 처음으로 가릴 수 있다)
- `relation_type` 이 바로잡히고 엔진이 그것을 읽게 됐을 때
- 측정 기록 14번의 칸 배정 결함이 고쳐졌을 때 — 회피 문구의 53% 가 ①단계에서 `avoid`
  가 아닌 칸으로 가고 있어 사전 경유의 효과가 그만큼 가려져 있다

**재현 방법**

```bash
cd EDA
export PYTHONIOENCODING=utf-8
./venv/Scripts/python.exe 57_avoid_lexicon_routing.py   # Q1 — 현재 vs core 경유
./venv/Scripts/python.exe 58_avoid_hard_vs_soft.py      # Q2 — A/B/C. 복제본 검증이 선행된다
# API 호출 0회. 팀 저장소를 수정하지 않는다
```

**관련 자료**

- `DECISIONS.md` N2 — 하드 AND 와 단독 매핑 금지
- `DECISIONS.md` N11 — 가중 조합 기각. 계수를 정할 근거가 없다는 판단의 출처
- `docs/nlr_engineering_notes.md` 13번 — 회피 도달 수치의 두 정의
- `docs/nlr_engineering_notes.md` 14번 — 파이프라인 손실. 이 결정의 효과 천장을 설명한다
- `57_avoid_lexicon_routing.py` · `58_avoid_hard_vs_soft.py`
- 레퍼런스 조사 `자연어 향수 추천의 회피 로직 설계` (2026-09-15, 팀 외부 자료)

---

## N13. 구조화는 LLM 과 사전을 둘 다 쓴다 — 형태소 분석기도 임베딩도 지금은 넣지 않는다

- 날짜: 2026-09-16
- 상태: **제안** — 측정 완료, 사용자 결정(2026-09-16), 팀 승인 전 · **코드 변경 없음(현행 유지)**
- **2026-09-18 재검토 — 결론 유지, 다만 이 결정이 답한 질문의 범위를 좁혀 적는다**

> **⚠ 이 결정은 「어간 매칭」 용도만 기각했다.**
>
> Q2 가 *"②단계 매칭을 형태소 분석기로 바꿀 것인가"* 였고, 잰 것은 **원형 매칭으로 더
> 많이 잡는** 방향이다. 근거 3 의 위험(`달콤하지` → `달콤` · 부정 오탐 3.4% → 14.8%)도
> 그 방향에서 나온다.
>
> **반대 방향은 이 결정이 답하지 않았다** — 낱말 경계 판정으로 **덜 잡는** 용도다.
> 2026-09-18 에 그 용도로 다시 쟀고(측정 기록 28번) **또 기각했다.** 다만 이유가 다르다.
>
> ```
> N13 의 기각 이유    이득이 작고(+0.0025) 새 오탐이 붙는다
> 28번의 기각 이유    긴 문장 오탐 2건을 더 막지만 합성어에서 3~4건을 잃는다
>                    `초록빛` · `면이불` 이 한 낱말로 분석돼 그 안의 `초록` · `이불` 이
>                    독립 형태소가 아니게 된다. 세 분석기가 같은 곳에서 잃는다
> ```
>
> **근거 3 의 "셋이 갈리지 않는다" 도 어간 매칭 기준이다.** 경계 판정에서는 갈린다 —
> `꿀꿀하다` · `해변가` 를 kiwi 는 쪼개고(못 막음) mecab · pecab 은 안 쪼갠다(막음).
>
> 이 결정이 적어 둔 *"되돌리기 쉬운 결정이다. 언제든 다시 연다"* 가 실제로 쓰였다.
> **실사용 로그로 오탐 빈도가 보이면 또 열 수 있다.**

**문제** — 자연어 추천 로직을 검토하면서 첫 번째로 만난 질문이다.

```
사용자 문장
  ① LLM 구조화    llm_stage1.py · gpt-5.4-nano    표현 조각을 뽑는다
  ② 사전 -> accord understand()                    표현을 accord 로 바꾼다
```

14번 측정이 "②가 병목(16.3%만 통과)" 이라고 지목했고, **한국어 자연어 특화 모델이 ①단계의
LLM 보다 구조화를 잘 하는지** 확인하려는 것이 출발점이었다. 즉 질문이 둘이었다.

```
Q1  LLM 과 사전 중 하나만 쓸 수 있는가
Q2  ②단계 매칭을 형태소 분석기나 임베딩으로 바꿀 것인가
```

**검토한 방법**

1. **LLM 만** — ②단계 사전 매칭을 제거한다
2. **형태소 분석기 + 사전만** — ①단계 LLM 을 제거한다. 지연 1.5초와 외부 의존이 사라진다
3. 둘 다 유지 + ②를 형태소 분석기(mecab)로 교체
4. 둘 다 유지 + ②를 임베딩으로 보강 (⑤단계 estimator)
5. **둘 다 유지 · ②는 부분 문자열 그대로** — 현행

**결정** — (5)를 택했다. **코드를 바꾸지 않는다.**

- **Q1: 둘 다 쓴다.** 두 평가셋에서 방향이 같다. 근거 1·2
- **Q2: 지금은 바꾸지 않는다.** 분석기는 이득이 작고 새 위험이 붙는다(근거 3).
  임베딩은 배포 조건에서 불가능하다(근거 4)
- **되돌리기 쉬운 결정이다.** `_lexicon_lookup` 하나를 바꾸는 일이라 언제든 다시 연다

---

### 근거 1 — 둘 다가 어느 한쪽보다 낫다. 두 평가셋에서 방향이 같다

`65_llm_vs_analyzer.py`(합성 600) · `66_survey_holdout.py`(설문 155) · 사전 v1.14 ·
API 호출 0회. 체크포인트에 저장된 ①단계 출력을 쓴다.

| 방식 | 합성 600 조건추출 | 합성 NDCG@5 | 설문 155 조건추출 |
|---|---:|---:|---:|
| ① LLM 만 (② 없음) | 71.3% | 0.398531 | **38.1%** |
| ② 형태소+사전만 (① 없음) | 81.3% | 0.328575 | **51.6%** |
| **둘 다 (현행)** | **87.5%** | **0.471540** | **55.5%** |

합성에서 둘 다가 LLM 단독보다 **+0.073**, 사전 단독보다 **+0.149** 다.
설문에서 **+17.4%p**, **+5.8%p** 다.

**두 방식의 성격이 반대다.** LLM 은 적게 뽑지만 정확하고(합성 NDCG 0.399 vs 0.329),
사전은 많이 뽑지만 정확도가 낮다(설문 51.6% vs 38.1%). 서로의 약점을 메운다.

### 근거 2 — 둘은 서로 다른 것을 본다. 하나를 빼면 그 몫이 사라진다

설문 155건에서 ②가 ①의 조건을 얼마나 재현하는지 쟀다. **LLM 이 옳다는 가정이 아니라
대체 가능성을 본 것이다.**

```
② 부분문자열   재현율 61.5%  ·  정밀도 33.7%  ·  완전일치 10 / 155
② mecab       재현율 61.5%  ·  정밀도 32.0%  ·  완전일치 10 / 155
```

**155건 중 두 방식이 같은 답을 낸 것이 10건뿐이다.** ②는 ①이 찾는 것의 61.5% 만 찾고,
대신 ①이 못 찾는 것을 2배쯤 더 찾는다. 대체 관계가 아니라 보완 관계다.

### 근거 3 — 형태소 분석기는 이득이 작고 새 위험이 붙는다

`62_analyzer_candidates.py` · `64_negation_handling.py`. 후보는 kiwipiepy ·
python-mecab-ko · pecab 셋이고 PyPI 실물로 확인했다(konlpy·koalanlp 는 JVM,
khaiii 는 배포 없음, bareunpy 는 라이선스 서버, soylemma 는 문장을 못 쪼갠다).

**정확도로는 셋이 갈리지 않는다.** 격차가 600문장 중 2문장이고 지표마다 1등이 다르다.

| | 조건추출 | NDCG@5 | OK | 부정 탐지 |
|---|---:|---:|---:|---:|
| kiwi | 539 | 0.473010 | 420 | 92.0% |
| mecab | 538 | **0.474068** | **421** | 92.0% |
| pecab | **540** | 0.472016 | 419 | 92.0% |

부정 처리를 붙여도 셋이 **소수점까지 같은 +0.000883** 을 얻었다. 갈릴 축이 없다.

**얻는 것이 작다.** 셋 중 배포 비용이 가장 낮은 mecab 기준으로도 이렇다.

```
합성 NDCG         +0.002528        N11 이 "이 크기로 방향을 단정하지 않는다" 고 한 구간
설문 조건추출      +1.3%p (2문장)
LLM 죽었을 때 폴백 +4.5%p (합성) · +1.9%p (설문)
비용              0.21ms · +10MB · 의존성 1개
```

**새 위험이 붙는다.** 원형 매칭은 사전 키를 어간으로 바꾸므로, 14번이 경고한
*"지금 오탐이 0인 것은 사전에 어간이 없어서다 — 설계가 막은 것이 아니라 운이다"* 가 깨진다.

```
부정 조각 88개 중 accord 를 잡은 것
   부분문자열   3 (3.4%)
   kiwi        11 (12.5%)
   mecab       13 (14.8%)
   pecab       13 (14.8%)

"달콤하지 않은 과일 향"
   부분 문자열  `달콤하지` 안에 별칭 `달콤한` 이 없다        안 걸린다
   원형 매칭    달콤한 -> (달콤,) · 달콤하지 -> (달콤,)     **걸린다 -> caramel·sweet**
```

부정 범위 규칙(`-지 않다` 는 앞 용언을 부정)을 붙여도 13건 중 **2건밖에 못 막는다.**

그리고 사전 별칭이 한 글자 키로 줄어드는 문제가 분석기마다 다르다.

```
별칭 `말캉`(표현 `과육`)
   mecab · pecab  -> 키 ('말',)   `말고` 의 `말` 에 걸려 fruity 오탐 7문장
   kiwi           -> 쪼개지 않는다  우연히 안전하다
```

**이건 분석기 문제가 아니라 사전 문제다.** 별칭 하나를 고치면 셋이 같아진다. 분석기를
고르는 기준으로 쓸 수 없다.

### 근거 4 — 임베딩은 성능이 아니라 배포 조건에서 막힌다

`63_embedding_estimator.py` · 모델 4개(ko-sroberta · KR-SBERT · multilingual-MiniLM ·
multilingual-e5-base) · 설계 둘(미매칭 표현 -> 사전 표현 최근접 / -> 92개 accord 직접).

**성능은 분석기보다 한 자릿수 크다.**

| | 최고 NDCG | vs 기준선 |
|---|---:|---:|
| mecab (분석기 최고) | 0.474068 | +0.002529 |
| ko-sroberta (설계2) | 0.491371 | +0.019832 |
| **mling-e5-base (설계1)** | **0.506255** | **+0.034716** |

**그런데 배포가 불가능하다.** 컨테이너 512MB 권장 · 현재 서버 162MB 인데,

```
mecab        172MB    들어간다
ko-sroberta 1,050MB   2배 초과
e5-base     2,022MB   4배 초과
torch 스택만 디스크 793MB + 가중치 423~1,083MB
아웃바운드가 gms.ssafy.io 하나로 묶여 있어 런타임 가중치 내려받기가 막힌다
```

그리고 **가장 높은 NDCG 지점이 `OK` 를 134건 잃는다**(397 -> 263). 평균 조건이
2.52 -> 4.64 로 늘어 하드 AND 가 깨지고, "조건을 넓혀 찾았습니다" 가 뜨는 요청이 56% 가 된다.

### 근거 5 — 더 큰 문제가 남아 있다

실사용 설문 155건에서 `NO_CONDITION` 이 **69/155 = 44.5%** 다(합성은 12.5%).
가장 좋은 조합으로 바꿔도 41.9% 이고, **분석기 선택으로는 3%p 도 못 줄인다.**

> **2026-09-16 정정(측정 기록 19번)** — 이 155건 중 12건은 향수 요청이 아니라 동의
> 표현(`네`·`넵`·`예`)이었다. 분모에서 빼면 **57/143 = 39.9%** 다. 위 숫자는 그때
> 그 정의로 잰 값이라 그대로 둔다. **이 절의 결론은 바뀌지 않는다** — 4할에 가깝고
> 분석기로 못 줄인다는 판단은 그대로다.

미매칭 표현이 문장당 2.18회(총 338회) 남는다. 여기를 푸는 것이 먼저다.

---

**결과**

- **코드 변경 없음.** ①단계 LLM + ②단계 사전(부분 문자열) 유지
- `torch` · `sentence-transformers` · `kiwipiepy` · `python-mecab-ko` · `pecab` 은
  **개인 저장소 venv 에만 설치했다.** 팀 저장소 `requirements.txt` 는 4개 그대로다
- 우선순위가 이렇게 남는다 — 14번의 표에 이번 측정을 더한 것이다

| | 무엇 | 크기 | 성격 |
|---|---|---|---|
| 1 | 칸 배정 결함 2개 | `sweet` 8회 + 부정 61건 | **버그에 가깝다. 비용이 작다** |
| 2 | ⑤단계 미매칭 추정 | 실사용 문장당 2.18회 | 설계됨·미구현 |
| 3 | 회피 소프트 감점 (N12) | 실사용 13건 | 결정됨·미구현 |
| 4 | 사전 확장 | 부분적 | 수익 체감 |

**Trade-off**

- **실사용 품질을 못 쟀다.** 설문 155건에 정답 라벨이 없어 NDCG 를 못 낸다. 설문으로 잰
  것은 조건 추출률이고, 합성에서 LLM 이 이긴 것은 NDCG 다. **서로 다른 지표라 직접
  충돌하지 않지만, 그래서 "실사용에서 어느 쪽이 정확한가" 는 여전히 미해결이다**
- **합성 600 은 정답 accord 집합에서 생성된 문장이라 LLM 에 유리하다.** LLM 이
  `scent_preference` 에 영어 accord 이름을 뱉으면 생성 과정을 역으로 밟는 셈이다.
  기여도 +0.149 를 실사용으로 옮기면 안 된다
- **분석기를 안 넣는 것이 "분석기가 나쁘다" 는 판정은 아니다.** 폴백 품질 개선
  (+4.5%p)을 지금은 포기한 것이다. LLM 이 죽으면 그만큼 손해를 본다
- **임베딩 기각은 배포 조건에 묶인 것이지 성능 판정이 아니다.** 컨테이너 상한이 올라가면
  결론이 달라질 수 있다. 512MB 는 배포 인계 문서에 `[추정]` 으로 적힌 값이다
- **`NO_CONDITION` 44.5% 를 아직 못 푼다.** 이 결정은 그 문제를 건드리지 않는다
- **리눅스에서 실제로 설치해 보지 못했다.** 휠 존재 여부까지만 확인했다. 이 환경에
  Docker 가 없다

**다시 볼 조건** — 아래 중 하나가 생기면 이 결정을 다시 연다.

- 컨테이너 메모리 상한이 인프라와 재협의되어 올라갔을 때 (임베딩·kiwi 가 살아난다)
- 설문 155건이 라벨링되어 실사용 NDCG 를 잴 수 있게 됐을 때
- `NO_CONDITION` 44.5% 가 해결되어 다음 병목이 ②단계로 내려왔을 때
- 사전 별칭 `말캉` 이 고쳐지고 부정 처리 범위 설계가 정해졌을 때 (분석기 위험이 줄어든다)
- GMS 장애가 실제로 나서 폴백 품질이 문제가 됐을 때

---

**재현 방법**

```bash
cd EDA
export PYTHONIOENCODING=utf-8
./venv/Scripts/python.exe -W ignore 62_analyzer_candidates.py   # 분석기 3개 · status · 메모리
./venv/Scripts/python.exe -W ignore 63_embedding_estimator.py   # 임베딩 4개 · 설계 1·2
./venv/Scripts/python.exe -W ignore 64_negation_handling.py     # 부정 처리 · 한 글자 키
./venv/Scripts/python.exe -W ignore 65_llm_vs_analyzer.py       # ①·② 기여 분해 (합성 600)
./venv/Scripts/python.exe -W ignore 66_survey_holdout.py        # 실사용 설문 155
# GMS 호출 0회. 팀 저장소를 수정하지 않는다
# 분석기·임베딩은 개인 저장소 venv 에만 설치했다
```

**관련 자료**

- `DECISIONS.md` N11 — 가중 조합 기각. "델타 크기로 방향을 단정하지 않는다" 의 출처
- `DECISIONS.md` N12 — 회피 소프트 감점. 우선순위 3번
- `docs/nlr_engineering_notes.md` 14번 — 파이프라인 손실. 이 결정의 출발점
- `docs/nlr_engineering_notes.md` 15번 — 분석기 비교 (근거 3의 출처)
- `docs/nlr_engineering_notes.md` 16번 — 임베딩 비교 (근거 4의 출처)
- `docs/nlr_engineering_notes.md` §7 — golden 200 과 survey 155 의 역할 구분
- `docs/plans/NLR_DEPLOY_HANDOFF.md` — 배포 제약(의존성 4개 · 512MB · 아웃바운드)
- `62`~`66` 스크립트. **`61_analyzer_comparison.py` 의 속도표는 결함이 있으니 인용하지 말 것**
  (pecab 캐시 · kiwi 지연 적재 — 15번에 기록)

---

## N14. 노트 브리지를 엔진에 연결한다 — 확률만 쓰지 않고 배수 문턱을 함께 건다

- 날짜: 2026-09-16
- 상태: **제안** — 측정 완료, 사용자 결정(2026-09-16), **팀 리뷰 중**
  · **코드 변경 있음** — 커밋 `5926709` · MR `[S15P21E203-296][feat]`

**문제** — ①단계 LLM 이 `scent_preference` 에 accord 이름이 아니라 **노트(향 원료) 이름**을
넣는 일이 잦은데, 엔진의 `_normalize_accord()` 는 accord 이름만 해석한다. 정확 일치 ·
소문자화 · 복수형 제거 · `e→y` 추측이 전부라 `mint` · `lemon` 은 통째로 버려진다.

```
합성 600 에서 scent_preference 의 값이 버려진 횟수   256회 · 191문장 (31.8%)
  그중 노트 이름이라 브리지로 풀리는 것              111회 (43.4%)
  문자열 규칙 확장으로 풀리는 것                      32회 (12.5%)
  둘 다 안 되는 것 (flower · clean)                 113회 (44.1%)
```

그리고 **그것을 풀 수 있는 표가 이미 저장소에 있었다.**
`analysis_outputs/10_note_accord_bridge.csv` 5,806행 · 2026-08-20 생성.
10번 노트북이 만들어 놓고 **엔진이 한 번도 쓰지 않았다.**

노트 하나가 여러 accord 에 붙어 있어(`Absinthe → aromatic 0.91 · woody 0.69 · fresh spicy 0.49`)
**어느 것을 쓸지가 결정 사항이었다.**

**검토한 방법**

1. **D1 `top1`** — 노트당 확률 최상위 1개
2. **D2 `p50`** — `p_accord_given_note >= 0.5` 인 것 전부
3. **D3 `p50+lift`** — `p >= 0.5` **그리고** `lift >= 1.4`
4. **D4 `top1+lift`** — D3 에 노트당 최상위 1개를 배수와 무관하게 더함
5. **연결하지 않는다** — ⑤단계 미매칭 추정(LLM estimator)으로 같은 자리를 공격한다

**결정** — (3) D3 를 택했다.

- **`_normalize_accord()` 가 못 푼 값에만 브리지를 본다.** 기존 경로는 그대로 둔다
- **문턱을 하드코딩하지 않는다.** `load_index(bridge_csv=, bridge_p_min=0.5, bridge_lift_min=1.4)`
  로 인자를 받는다. 전부 기본값이 있어 **기존 호출부를 고치지 않아도 된다**
- **브리지 CSV 에 `p` · `lift` 숫자를 담고 엔진이 거른다.** 미리 걸러서 넣지 않는다 —
  향 사전이 63행을 통째로 싣고 엔진이 `required=core` 35행만 쓰는 것과 같은 방식이다
- **CSV 가 없으면 경고만 남기고 이전과 동일하게 동작한다.** 코드 선배포를 막지 않는다
- **추천 근거(`evidence`)는 붙이지 않는다** → 아래 한계 2번
- (1) **기각** — D3 보다 확실히 낮다. 근거 1
- (2) **기각** — NDCG 는 D3 와 안 갈리지만 조건을 더 쓰고, 얹는 조건의 절반 이상이
  N4 가 정한 신호 문턱 아래다. 근거 2
- (4) **기각** — 755건 중 **1건**에서만 D3 와 다르다. 규칙을 하나 더 둘 값이 없다
- (5) **보류** — 후보 B 는 여전히 유효하다. 다만 요청마다 LLM 호출이 붙고 아직 미측정이다.
  브리지는 **추가 비용 0** 이라 먼저 한다. 근거 5

**결과** — 팀 저장소에 적용하고 **엔진 안에서 다시 쟀다.** 측정 스크립트의 결과와
소수점 6자리까지 일치한다.

| 합성 600 · 사전 `v1_8`(팀 기본값) | 패치 전 | 패치 후 |
|---|---:|---:|
| NDCG@5 | 0.468842 | **0.495198 (+0.026356)** |
| 조건을 뽑은 문장 | 520 · 86.7% | **529 · 88.2%** |
| `AND` | 391 | **412** |
| `SINGLE_BROAD` | 91 | **75** |
| `NO_CONDITION` | 80 | **71** |
| `NO_RESULT` | 0 | 0 |

배포 산출물은 `ai/data/note_accord_bridge.csv` **2,925행 · 97KB** 하나가 는다.

---

### 근거 1 — 브리지를 넣는 것 자체는 표본 흔들림과 갈린다

응답별 NDCG 를 짝지어 빼고 20,000회 복원추출했다(seed 20260916 · 사전 v1.14 · 합성 600).

| 비교 | 차이 | 95% 구간 |
|---|---:|---|
| 현행 → D1 | +0.011157 | `[+0.004963, +0.017971]` |
| 현행 → D2 | +0.022906 | `[+0.013858, +0.032464]` |
| **현행 → D3** | **+0.025357** | **`[+0.016437, +0.035016]`** |
| **D1 → D3** | **+0.014200** | **`[+0.007491, +0.021470]`** |
| D2 → D3 | +0.002451 | `[-0.001899, +0.006812]` **0 을 포함** |

**D1 보다 D3 가 낫다는 것은 갈린다**(응답 단위로도 D3 우세 41건 대 D1 우세 14건).
**D2 와 D3 의 NDCG 우열은 갈리지 않는다.** 그래서 D3 를 고른 근거는 NDCG 가 아니다.

### 근거 2 — 확률만 쓰면 기저율이 조건으로 들어온다

D2 가 D1 위에 얹는 것을 보면 **노트 종류와 무관하게 `woody` 가 붙는다.**

```
mint  → aromatic + citrus·fresh spicy·green·woody
plum  → fruity   + powdery·sweet·woody
peony → floral   + fresh·fruity·rose·woody
```

`p ÷ lift` 로 역산한 accord 기저율(전체 향수 중 보유 비율)이 이유다 [측정].

```
woody 0.617 · powdery 0.477 · sweet 0.460 · citrus 0.455 · aromatic 0.414
accord 74개 중 기저율 0.3 이상이 11개
```

**이 값은 브리지 표에서 역산한 것이라 현재 향수 데이터와 다르다** [2026-09-17 리뷰 지적].
직접 세면 `woody` 는 129,161개 중 **0.6305**, 평가자 10명 이상 69,376개 중 0.6447 이다.
브리지가 2026-08-20 스냅샷으로 만들어져 분모가 다르다. **어느 쪽이든 60% 를 넘어
결론은 같다** — `p` 만 보면 `woody` 가 아무 노트에나 붙는다.

**전체의 63.1% 가 `woody` 라 어떤 노트든 `p >= 0.5` 가 저절로 선다**(직접 계수).
신호가 아니다.

합성 600 의 **발화 횟수로 가중**하면 이렇다.

```
D1 이 만드는 조건     발화 117회 · lift < 1.4 가   2회 ( 1.7%)
D2 가 더 얹는 조건    발화 327회 · lift < 1.4 가 186회 (56.9%)

가장 낮은 것들 — 전부 → woody · lift 가 1.0 미만
  peony 0.87 · melon 0.87 · white flowers 0.90(발화 8회) · mango 0.93
```

**`peony → woody` lift 0.87 은 "작약이 든 향수는 평균보다 덜 woody 하다" 는 뜻이다.**
그런데 D2 는 사용자가 작약을 말하면 `woody` 를 조건으로 넣는다.

문턱 1.4 는 **N4 의 통제군이 정한 값을 그대로 썼다** — 표현과 무관한 무작위 집단
500·1,000·2,500 에서 배수가 1.17·1.38·1.14 로 1.4 를 넘지 못했다.

**`lift >= 1.4` 를 걸면 조건이 2.98 → 2.79 로 줄었는데 `AND` 가 408 → 417 로 늘었다.**
조건을 빼면 보통 품질이 내려가는데 반대로 갔다 — **뺀 조건이 정보가 아니었다.**

### 근거 3 — 1.4 는 평지 안에서 유일하게 근거가 있는 값이다

두 문턱을 각각 훑었다(`72_bridge_lift_threshold.py` · 합성 600 · GMS 호출 0회).

```
lift 0 ~ 1.6   서로 갈리지 않는다 — 1.4 대비 부트스트랩 구간이 전부 0 을 포함
lift 1.7       [-0.005464, -0.000396]   0 을 안 넘는다. 확실히 나쁘다
lift 2.0       [-0.009482, -0.001162]
lift 2.5       [-0.019827, -0.007075]   노트가 755개 → 436개로 준다

p 0.4 ~ 0.5   NDCG 차이 0.00004. 사실상 같다
p 0.6 · 0.7   내려간다
```

**NDCG 로는 0~1.6 안에서 문턱을 고를 수 없다.** 그 구간에서 1.2·1.3 이 `AND` 419 로
1.4 의 417 보다 2건 높지만 검정하지 않았고, 표를 눈으로 보고 고르면 근거가 아니라 취향이다.
**1.4 는 이 프로젝트가 이미 세운 선이고 나빠지는 지점(1.7)에서 세 칸 떨어져 있다.**

### 근거 4 — N2 가 걱정한 단독 매핑은 오히려 줄어든다

N2 가 단독 매핑을 금지했고 N10 이 직역에만 예외를 뒀다. *"노트 하나가 accord 하나를
만들면 N2 가 막은 형태가 된다"* 가 걱정이었다. **재보니 반대다** [측정 · 19번].

```
합성 600 의 SINGLE_BROAD (조건이 accord 하나뿐)
  현행 89건  →  D3 73건        16건 줄었다

단독이었다가 벗어난 문장          17건
단독이 아니었다가 단독이 된 문장    1건
  "한여름에 쓰기 좋은 생생한 오렌지 향을 찾고 있어…"
  현행 조건 [] (추천 불가)  →  D3 조건 [citrus]
  조건이 줄어든 것이 아니라 없던 것이 생겼다
```

브리지는 노트 하나에서 평균 **2.56개** accord 를 만든다. 조건이 하나뿐이던 문장이
둘 이상이 되어 정상 `AND` 로 올라간다.

**N10 의 표시 의무는 자동으로 붙는다.** 엔진이 `if len(cols) == 1` 로 **조건 개수만** 보고
`SINGLE_BROAD` 를 붙인다(`nlr_engine.py`). 조건이 사전에서 왔든 브리지에서 왔든
따지지 않으므로 별도 작업이 없다.

**별건으로 남는 것** — *"N2 의 원칙을 향 사전 밖 경로로 확장할 것인가"*. 확장한다면
브리지가 아니라 **현행 엔진이 이미 만들고 있는 단독 조건 27건**(합성 600 ·
`citrus` 7 · `rose` 5 · `leather` 4 …)이 먼저 대상이 된다. 브리지와 무관하게 남는다.

### 근거 5 — 이 세션에서 잰 것 중 유일하게 배포 가능한 개선이다

```
형태소 분석기 mecab   +0.0025    서버 메모리 +10MB              N13 — 이득이 작아 미도입
임베딩 ko-sroberta    +0.0198    서버 메모리 +1,050MB           N13 — 배포 불가
임베딩 e5-base        +0.0347    +2,022MB · OK −134             N13 — 배포 불가
⑤단계 미매칭 추정      미측정      요청마다 LLM 호출 추가          후보 B
노트 브리지 D3         +0.0254    CSV 97KB 를 기동 시 한 번 읽는다
```

권장 메모리가 512MB 이고 아웃바운드가 `gms.ssafy.io` 뿐이라는 제약(`NLR_DEPLOY_HANDOFF.md`)
아래에서 **추가 비용이 사실상 0 인 유일한 선택지다.**

---

**한계 — 이 근거로 말할 수 있는 것과 없는 것**

1. **D2 보다 낫다고 말할 수 없다.** NDCG 구간이 0 을 포함한다. D3 를 고른 근거는
   조건 수(2.98 → 2.79) · `AND`(408 → 417) · N4 기준 준수이지 NDCG 가 아니다
2. **추천 근거를 붙이지 못했다.** 브리지가 만든 accord 는 응답의 `matched[]` 에서
   `from` · `tier` · `rationale` 이 `null` 로 나간다. `recommend()` 가 원래 그렇게
   처리하도록 짜여 있어 스키마는 그대로지만 **지금까지 `tier` 가 실제로 `null` 이 된
   적은 없다.** 근거를 붙이려면 `tier` 에 값이 하나 더 필요한데
   `docs/nlr_api_contract.md` 가 `VERIFIED`/`TEAM`/`LLM` 세 값으로 적어 뒀다.
   **계약 변경이라 팀이 정할 일이다**
3. **실사용 설문 143건에서 `AND` 가 57 → 56 으로 1건 준다.** 모든 브리지 규칙에서 같다.
   정답 라벨이 없어 품질 하락인지 판정할 수 없다
4. **두 문턱을 동시에 흔들지 않았다.** `lift` 를 훑을 때 `p` 는 0.5 고정, 반대도 마찬가지다.
   **`p` 쪽은 부트스트랩도 안 했다**
4-1. **배포 CSV 에서는 `p` 문턱이 한 행도 거르지 않는다** [측정 · 2026-09-17].
   파일을 만들 때 이미 `p >= 0.5` 로 걸러 넣어 `p` 최솟값이 정확히 `0.5000` 이다.
   실제로 거르는 것은 `lift` 뿐이다(2,925행 중 987행). **`BRIDGE_P_MIN` 은 표를
   다시 만들 때를 위한 방어다.** `p` 스윕은 원본 표(5,806행)로만 재현된다
5. **브리지 CSV 의 생성 절차를 확인하지 않았다.** `10_query_feature_bridge.ipynb` 가 만든
   표이고 노트 이름 정규화 방식과 최소 빈도 컷을 모른다. **노트당 상위 5개만 담긴 표라
   문턱을 0 으로 내려도 6번째 accord 는 안 나온다**
6. **노트 이름 매칭은 소문자 정확 일치뿐이다.** `white flower`(단수)는 못 잡고
   `white flowers`(복수)만 잡는다. 남은 145회에 그런 것이 섞여 있다
7. **`p` 는 조향 관행이지 사용자 의도가 아니다.** *"민트가 든 향수는 aromatic 인 경우가
   많다"* 와 *"사용자가 민트라고 말하면 aromatic 을 원한다"* 는 다르다. `lift` 로
   기저율을 걷어내도 이 간극은 남는다. 검색이라는 목적에서 건너뛴 것이다
8. **사전 버전이 어긋난 상태에서 적용했다.** 팀 기본값은 `v1_8`, 최근 분석은 `v1_14` 다.
   이 결정은 사전을 바꾸지 않는다 — **별도 이슈로 나눴다**(2026-09-16 사용자 결정).
   사전만 v1_14 로 바꾼 효과는 `+0.0027` 이고 **95% 구간 `[-0.000146, +0.005997]` 로
   0 을 포함한다**

**재현 방법**

```bash
cd EDA
export PYTHONIOENCODING=utf-8
# GMS 호출 0회. 저장된 ①단계 출력만 읽는다. 파일을 만들지 않는다
./venv/Scripts/python.exe -W ignore 67_field_assignment_defects.py   # 규칙 넷 비교
./venv/Scripts/python.exe -W ignore 72_bridge_lift_threshold.py      # 문턱 스윕·부트스트랩
```

팀 저장소 적용은 `delivery/note_bridge/` 에 패치 · CSV · 적용 문서가 있다.
되돌리려면 `git apply -R` 후 `ai/data/note_accord_bridge.csv` 를 지운다.
**데이터 파일만 지워도 기능은 꺼지고 엔진은 정상 동작한다.**

**관련 자료**

- `DECISIONS.md` N2 — 단독 매핑 금지. 근거 4 가 답하는 대상
- `DECISIONS.md` N4 — 통제군 배수 1.4. **문턱의 출처**
- `DECISIONS.md` N10 — 직역의 단독 매핑 허용과 `SINGLE_BROAD` 표시 의무
- `DECISIONS.md` N13 — 분석기·임베딩 미도입. 근거 5 의 비교 대상
- `docs/nlr_engineering_notes.md` 14번 — 파이프라인 손실. 이 발견의 출발점
- `docs/nlr_engineering_notes.md` 18번 — 규칙 넷 비교와 기저율 (근거 1·2 의 출처)
- `docs/nlr_engineering_notes.md` 19번 — 설문 무효 응답 판정 (근거 4 의 출처)
- `67_field_assignment_defects.py` · `72_bridge_lift_threshold.py`
- `analysis_outputs/10_note_accord_bridge.csv` — 원본 표 5,806행
- `delivery/note_bridge/` — 패치 · 배포용 CSV · 적용 문서
- 팀 저장소 커밋 `5926709` · 브랜치 `feature/ai/S15P21E203-296-note-accord-bridge`

---

## N15. ⑤단계 미매칭 추정을 켠다 — `spec.md` 1장에 예외를 두고 캐시로 흔들림을 막는다

- 날짜: 2026-09-17
- 상태: **제안** — 측정 완료, 사용자 결정(2026-09-17), 팀 승인 전
  · **코드 변경 있음** — 2026-09-17 팀 저장소 병합(MR !172)
- **⛔ 2026-09-18 부분 뒤집힘 — 「파일 캐시」를 뺀다. 추정을 켜는 결정 자체는 유지**

> **캐시를 파일에서 되살리는 것이 오염을 영구화한다** [측정 2026-09-18 · 기록 26번].
>
> ```
> "빨레 냄새 나는 향수 찾아줘"        <- `빨래` 의 오타
>   ⑤단계 추정 -> rubber · plastic   ("빨레 = 고무 같은 재질" 이라고 지어냈다)
>   조건 ['plastic','rubber'] -> 1위 Garage
>   그 줄이 미매칭 로그에 남아 **프로세스를 다시 띄워도 같은 결과**
> ```
>
> **LLM 의 일시적 오류가 파일을 거치면서 시스템의 지속적 규칙이 된다.** 재현성은 올라가지만
> 틀린 답의 재현성이다.
>
> **아래 「검토한 방법」의 (3)에서 (2)에 가까운 쪽으로 옮긴다** — 추정은 켜 두고 캐시는
> **한 프로세스 안에서만** 유지한다. `cached_estimator` 가 `load_estimates()` 를
> 부르지 않는다.
>
> **잃는 것이 작다.** 근거 3 이 캐시를 넣은 이유가 *"같은 표현에 같은 답"* 인데, 캐시 키가
> ①단계가 뽑은 표현이고 그 표현이 같은 문장에서도 매번 달라진다 —
> `additional_requirements` 일치율 **9.2%**(기록 17·23번). 재시작 뒤에 키가 맞을 확률이
> 10% 남짓이라 **파일로 이어 붙일 실익이 원래 작았다.**
>
> **기록은 그대로 쌓인다.** `record()`(쓰기)와 `load_estimates()`(읽기)가 분리돼 있어
> 읽기만 끊으면 미매칭 표현 기록은 남는다. 사전 확장 근거를 잃지 않는다.
>
> **딸려 나온 것** — `load_estimates()` 의 호출처가 0 이 된다. `S15P21E203-306` 이
> *"어느 코드도 안 쓴다"* 를 근거로 파일을 지운 선례가 있어 리뷰 판단이 필요하다.
>
> 코드는 만들어 뒀고 Jira 이슈를 기다린다(`scratchpad/cache_work.patch`).

**문제** — 노트 브리지(N14)를 넣어도 **실사용 설문 143건 중 56건(39.2%)이 조건을
하나도 못 뽑는다.** 그 요청은 추천이 아니라 *"조건을 찾지 못했습니다"* 를 받는다.

엔진에는 **자리가 이미 있다.** `recommend(..., estimator=)` 와 `_apply_estimates()` 가
`tier="LLM"` · `rationale=None` · `llm_reasoning` 분리까지 갖춰져 있고 **함수만 없었다.**

**그런데 이 방식은 `spec.md` 1장과 정면으로 부딪힌다.**

> 매핑은 사전이 하고 LLM 은 구조화만 한다.
> LLM 이 `달큰한 → caramel` 이라고 했을 때 **왜 caramel 인지 물어볼 데가 없다.**

`NLR_UNMATCHED_LOG_DESIGN.md` 도 「LLM 추정 호출」을 **미결**로 남겨 뒀다.
**원칙에 예외를 둘 것인가가 이 결정이다.**

**검토한 방법**

1. **현행 유지** — 조건을 못 뽑으면 추천하지 않는다
2. **추정을 켜되 캐시 없이** — 요청마다 LLM 에 물어본다
3. **추정 + 파일 캐시** — 미매칭 로그를 캐시로 쓴다
4. **추정 + 백엔드가 캐시를 준다** — DB 에서 꺼내 AI 에 넘긴다
5. **추정 + AI 가 DB 를 직접 읽는다**

**결정** — (3)을 택했다. **`spec.md` 1장에 예외를 둔다.**

- **사전이 실패했을 때만 LLM 이 매핑한다.** 엔진이 `not understood["core"]` 일 때만 부른다.
  조건이 하나라도 잡히면 예전과 똑같이 사전만 쓴다
- **표현당 accord 는 최대 3개**(측정 기록 20번의 E3). `ESTIMATOR_TOP_N = 3`
- **호출은 병렬로 한다.** `ESTIMATE_MAX_WORKERS = 8`. 순차로는 예산을 넘었다. 근거 2
- **캐시는 미매칭 로그 파일 자체다.** 별도 저장소를 만들지 않는다. 근거 3
- **호출 실패(`None`)를 캐시에 넣지 않는다.** 넣으면 네트워크가 한 번 끊긴 표현이
  영영 *"향이 아님"* 으로 굳는다. 근거 4
- **빈 판정(`[]`)은 기록하고 캐시한다.** *"LLM 이 향이 아니라고 답했다"* 와 *"아직 묻지
  않았다"* 를 구분한다. 전체 표현의 38% 가 여기 해당한다. 근거 4
- **`NLR_ESTIMATOR` 로 끌 수 있다.** GMS 키가 없으면 자동으로 꺼진다
- **결과에 `tier="LLM"` 이 붙고 `rationale` 은 비운다.** 사전의 근거와 섞지 않는다
- (1) **기각** — 실사용 5명 중 2명이 아무 추천도 못 받는다
- (2) **기각** — 같은 문장을 두 번 물으면 **5문장 중 5문장이 달랐다.** 근거 3
- (4)(5) **기각** — 타이밍·아웃바운드 제약에 걸린다. 근거 5

**결과** — 개인 저장소에서 검증했다. **팀 저장소에는 아직 안 올렸다.**

| 합성 600 · 사전 v1_8 · 브리지 켬 | 현행 | **추정 켬(E3)** |
|---|---:|---:|
| 조건을 뽑은 문장 | 529 · 88.2% | **598 · 99.7%** |
| NDCG@5 | 0.495198 | **0.541795 (+0.046597)** |
| `AND` | 412 | 447 |
| `RELAXED_OR` | 42 | **74** |

| 설문 143 | 현행 | **추정 켬(E3)** |
|---|---:|---:|
| 조건을 뽑은 문장 | 87 · 60.8% | **122 · 85.3%** |
| 추천 불가 | 56건 | **21건** |

바뀐 파일 — `nlr_engine.py`(병렬 · 실패/빈 판정 구분) · `llm_stage1.py`(`estimate_accords`
· `make_estimator`) · `server.py`(연결 · 스위치) · `unmatched_log.py`(실패 캐시 금지 ·
빈 판정 복구) · `data/estimator_prompt_v1.txt`(신규).

---

### 근거 1 — 효과는 크다. 다만 NDCG 를 그대로 믿으면 안 된다

짝지은 부트스트랩 20,000회(합성 600) — **현행 → E3 `+0.046597` · 95% 구간
`[+0.035112, +0.058909]`** 로 0 을 제외한다. E1·E2 보다 나은 것도 갈린다.

**그런데 합성 600 은 향수에서 문장을 역생성한 데이터다**(`spec.md` §7.2).
문장을 쓴 것과 같은 종류의 모델이 그 문장을 읽는 것이라 **NDCG 가 부풀려졌을 수 있다**
(측정 기록 20번).

**부풀려지지 않은 지표로 판단한다.** 조건 추출률은 정답 라벨이 필요 없다.

```
실사용 설문 143건   60.8% -> 85.3%     추천 불가 56건 -> 21건
```

**"추천이 더 정확해진다" 가 아니라 "추천을 아예 못 하던 요청이 준다" 가 이 결정의 근거다.**

### 근거 2 — 순차 호출은 예산을 넘었고 병렬이 풀었다

`_apply_estimates()` 가 표현을 하나씩 순차로 불렀다. 미매칭은 문장당 중앙 2~3개 ·
최대 7개다.

| | 문장당 지연 | ①단계 1.5s 포함 | 8s 예산 |
|---|---:|---:|---|
| 순차 | 6.05s | 7.55s | **실사용 2.1% 초과** |
| **병렬(8)** | **1.64s** | **3.14s** | 여유 |

표현 7개짜리 문장은 **10.9s → 1.9s(83% 절감)** 다 [측정 · 실제 GMS 호출].

**병렬이 결과를 바꾸지 않는 것을 확인했다.** 체크포인트를 고정 estimator 로 써서
순차와 병렬을 돌린 결과가 **설문 143건 전부 일치**했다. `ThreadPoolExecutor.map` 이
입력 순서를 지키므로 `core` 도 `evidence` 도 순차와 같다.

GMS 속도 제한은 **분당 30,000요청 · 1억 8천만 토큰**이다 [측정 · 응답 헤더
`X-Ratelimit-Limit-Requests`]. 동시 7개는 한도의 **1.4%** 다.

### 근거 3 — 캐시가 없으면 같은 문장에 매번 다른 추천이 나온다

**같은 문장 · 같은 프롬프트 · 같은 설정(병렬)으로 두 번 불렀다** [측정].

```
5문장 중 **5문장이 달랐다**

"향이 별로 안강했으면…"   1차 [earthy, fresh, woody]
                        2차 [aldehydic, earthy, fresh, soapy, sweet, woody]
```

**사용자가 새로고침하면 다른 향수가 나온다.** `spec.md` 1장이 막으려던 것이 이것이다.

`temperature=0` · `seed` 를 넣어도 **없어지지 않았다**(표현 3개 × 4회 표본에서 여전히
갈림). 표본이 작아 설정 간 우열은 판정하지 않았다.

**캐시가 이것을 없앤다** [측정].

| | 캐시 없음 | **캐시 있음** |
|---|---:|---:|
| 두 번 물어 같은 결과 | 0/5 | **5/5** |
| 2차 응답 지연 | 1.5~2.8s | **0.01s** (GMS 를 안 부른다) |
| 재시작 뒤 | — | 로그에서 복구해 **같은 답** |

`unmatched_log.cached_estimator()` 가 이미 그렇게 구현돼 있었다 — *"캐시는 별도 파일이
아니라 이 로그 자체다. 기동 시 한 번 읽어 메모리에 둔다."* 새로 만들지 않았다.

### 근거 4 — 실패와 "향이 아님" 을 구분해야 한다

캐시를 파일로 살리면서 **두 가지 구멍이 드러났다.**

```
① 호출 실패를 캐시에 넣고 있었다
   `estimate(expression) or {}` 가 `None`(실패)을 `{}`(빈 답)으로 만들었다
   → 네트워크가 한 번 끊긴 표현이 **영영 "향이 아님" 으로 굳는다**
   지금까지는 저장이 안 돼 재시작하면 사라졌지만, 캐시를 파일로 살리면 영구화된다

② 빈 판정이 저장되지 않았다
   `record()` 가 `llm_accords: null` 로 쓰고 `load_estimates()` 가 건너뛰었다
   → **표현 369개 중 140개(38%)가 재시작마다 다시 물어진다**
   `무겁지 않게` · `겨울에 코트 입고 뿌리기 좋음` 같은 것들이다
```

**둘 다 고쳤다.** `None` 은 캐시에 안 넣고, `[]` 는 `tier="LLM"` 과 함께 기록해
`load_estimates()` 가 `None` 과 구분해 읽는다.

**DB 테이블도 이 구분을 지켜야 한다** — `NLR_PREDEPLOY_DECISIONS.md` 안건 ③.

```
llm_accords = ['woody','green']   LLM 이 accord 를 골랐다
llm_accords = [] (빈 배열)         LLM 이 "향이 아니다" 라고 판정했다
llm_accords = NULL                아직 묻지 않았다 (추정이 꺼졌거나 호출 실패)
```

### 근거 5 — 캐시를 백엔드나 DB 에서 받는 길은 제약에 걸린다

```
(4) 백엔드가 요청마다 준다
    **백엔드는 어떤 표현이 미매칭이 될지 요청 전에 모른다.** 미매칭은 AI 가 ①단계
    구조화와 사전 매칭을 해봐야 나온다. 전체를 매 요청 보내거나 왕복을 두 번 해야 한다

(5) AI 가 기동 시 DB·백엔드에서 받는다
    **아웃바운드가 `https://gms.ssafy.io` 하나만 허용된다**
    (`NLR_DEPLOY_HANDOFF.md` · 2026-09-15 인계 조건)
    DB 를 직접 읽으면 의존성이 4개 → 5개가 되고 *"DB 없이 독립적으로 돈다"* 가 깨진다
    compose 에서 `backend` 가 `ai` 를 `depends_on` 이라 방향을 뒤집으면 순환이 된다
```

**파일 캐시는 이 제약들을 다 피한다.** 나가는 통신이 없고, 백엔드와 무관하게 뜨고,
요청 전에 이미 채워져 있다.

**DB 는 분석용으로 따로 쌓는다.** 역할이 다르다 — 캐시는 *"같은 답을 빨리"*,
DB 는 *"사전을 키울 근거 모으기"* 다.

---

**한계 — 이 근거로 말할 수 있는 것과 없는 것**

1. **실사용 정확도를 잴 수 없다.** 설문 143건에 정답 라벨이 없다(`spec.md` §8 3번).
   조건을 뽑게 됐다는 것과 **그 조건이 맞다는 것은 다르다**
2. **합성 600 의 NDCG 가 얼마나 부풀려졌는지 정량화하지 못했다.** 방향만 말할 수 있다
3. **일관성은 캐시 안에서만 보장된다.** 처음 묻는 표현은 여전히 LLM 이 그때그때 답한다.
   같은 뜻의 다른 표현(`성숙한 분위기` vs `고전적이고 우아한 분위기`)은 여전히 갈린다
4. **AI 서버를 여러 대로 늘리면 깨진다.** 대마다 파일 캐시가 달라 사용자가 어느 서버에
   붙느냐로 답이 갈린다. 지금은 1대다(2026-09-17 사용자 확인). 늘릴 때 다시 본다
5. **프롬프트가 v1 한 판뿐이다.** 69번이 ①단계를 v1~v4 로 비교한 것과 달리 비교군이 없다
6. **E3 는 `RELAXED_OR` 를 42 → 74 로 늘린다.** 조건이 늘어 하드 AND 가 더 자주 깨진다.
   사용자에게 *"조건을 넓혀 찾았습니다"* 가 더 자주 뜬다
7. **캐시가 틀린 추정을 고정한다.** `추운 날 어울리는 → fresh` 처럼 의심스러운 답도
   계속 쓰인다. 흔들리면 가끔 맞지만 고정하면 항상 틀린다.
   **미매칭 로그를 사람이 검토해 사전으로 올리는 것이 그 교정 경로다**
8. **비용이 요청마다 붙을 수 있다.** 캐시에 없는 표현만이지만 실사용은 39.2% 가
   조건 0개다. 초기에는 캐시가 비어 있어 호출이 몰린다
9. **`temperature` 를 건드리지 않았다.** 기본값 그대로다. 표본 4회로는 효과를 증명하지
   못했고, 캐시가 흔들림을 이미 막아 우선순위가 낮다

**다시 볼 조건**

- AI 서버를 2대 이상으로 늘릴 때 (한계 4)
- 설문 143건에 정답 라벨이 생겨 실사용 정확도를 잴 수 있을 때 (한계 1)
- 미매칭 로그가 쌓여 사전으로 올릴 표현이 나올 때 — 그만큼 LLM 호출이 준다

**재현 방법**

```bash
cd EDA
export PYTHONIOENCODING=utf-8
# GMS 를 호출한다. 체크포인트가 있으면 재사용한다
./venv/Scripts/python.exe -W ignore 74_unmatched_estimator.py
```

병렬 동일성 · 실제 지연 · 캐시 효과 검증은 스크립트로 남기지 않았다. 측정 기록 20번의
체크포인트(`analysis_outputs/74_estimator_checkpoint.csv` 369행)와 바뀐 엔진으로 다시
계산할 수 있다.

**관련 자료**

- `spec.md` 1장 — *"매핑은 사전이 하고 LLM 은 구조화만 한다"*. **이 결정이 두는 예외의 대상**
- `spec.md` §7.2 — 합성 600 이 역생성 데이터라는 서술 (근거 1의 한계)
- `spec.md` §8 3번 — 설문 라벨링. 실사용 정확도를 잴 전제
- `DECISIONS.md` N14 — 노트 브리지. 이 결정 이전에 `NO_CONDITION` 을 줄인 작업
- `docs/nlr_engineering_notes.md` 20번 — 천장·E1/E2/E3 비교·부트스트랩·지연 (근거 1·2)
- `docs/plans/NLR_UNMATCHED_LOG_DESIGN.md` — 「LLM 추정 호출」 미결을 이 결정이 닫는다
- `docs/plans/NLR_PREDEPLOY_DECISIONS.md` 안건 ③ — DB 테이블 명세
- `docs/plans/NLR_DEPLOY_HANDOFF.md` — 아웃바운드 제약 (근거 5)
- `74_unmatched_estimator.py` · `analysis_outputs/74_estimator_prompt_v1.txt`
- 팀 `ai/unmatched_log.py` 의 `cached_estimator()` · `load_estimates()`

---

## N16. 짧은 표면형에 문맥 조건을 건다 — 사전 행을 쪼개서 거는 것이 핵심이다

- 날짜: 2026-09-18
- 상태: **제안** — 측정 완료, 사용자 결정(2026-09-18), 팀 승인 전
  · **코드 변경 없음 · 사전 데이터만** — 팀 저장소 `S15P21E203-307` 푸시됨, MR 대기

**문제** — 사전 매칭이 `if form in haystack` 이라 **표면형이 더 큰 낱말 안에 박혀도
걸린다.** 향과 무관한 문장 11건을 넣었더니 11건 다 향수를 돌려줬다
[측정 2026-09-18 · 3회 반복해도 같았다 · 기록 25번].

```
"꿀꿀하다"  -> '꿀'   -> sweet           -> 향수 5개
"잼민이"    -> '잼'   -> sweet           -> 향수 5개
"세탁기"    -> '세탁'  -> fresh, soapy    -> 향수 5개
"초록불"    -> '초록'  -> green           -> 향수 5개
"분내다"    -> '분내'  -> musky, powdery  -> 향수 5개
```

측정 기록 14번이 이 위험을 적어 뒀다 — *"지금 오탐이 0인 것은 사전에 어간이 없어서다.
설계가 막은 것이 아니라 운이다."* **1글자·2글자 명사에서는 그 운이 따르지 않았다.**

**검토한 방법**

1. **아무것도 안 한다** — 실사용 빈도를 모르니 로그가 쌓일 때까지 기다린다
2. **표면형을 사전에서 뺀다** — `꿀` · `숲` · `잼` 을 지우고 긴 형태만 남긴다
3. **낱말 경계를 본다** — 표면형 뒤가 조사·공백·문장끝일 때만 인정한다
4. **형태소 분석기로 경계를 판정한다** — N13 을 다시 연다
5. **문맥 조건을 건다** — `match_condition` 에 향 낱말을 요구한다

**결정** — (5)를 택했다. **`match_condition` 이 행 단위라 행을 쪼개서 건다.**

```
kr.source.honey_syrup        당밀 · 시럽   -> sweet   조건 없음 (그대로 산다)
kr.source.honey_syrup.short  꿀 · 잼      -> sweet   query_contains:향,냄새,느낌
```

대상은 표면형 9개다 — `꿀` · `잼` · `빨래` · `세탁` · `이불` · `잔디` · `초록` ·
`분내` · `산림`. 사전이 63행 -> 72행이 된다.

---

### 근거 1 — 나머지 넷이 전부 더 비쌌다 [측정]

```
방법                        오탐 막음   설문 143 손실   합성 600 손실
표면형을 뺀다 (78번)            5/11        4건          14건
낱말 경계를 본다 (79번)         8/11        2건           9건
형태소 분석기 (82번)           7/14        0건         3~4건
**문맥 조건 (채택)**          14/14        0건           0건
```

(2)·(3)이 비싼 이유가 같다. **한국어가 교착어라서다.** `숲향` · `숲속에` · `숲의` 처럼
뒤에 무엇이든 붙는데 부분 문자열 매칭이 그것을 잡던 유일한 수단이었다. 실제로 잃은 것이
`숲향 나는 향수 추천좀` · `나무통에 숙성한 술을 떠올리게 하는 향` 같은 문장이다.

(4)는 N13 을 다시 연 것이다. **긴 문장 오탐을 2건 더 막지만** 합성어(`초록빛` ·
`면이불`)에서 3~4건을 잃고 의존성 +1 · 메모리 +10MB 가 붙는다(기록 28번).

### 근거 2 — 행을 쪼개지 않으면 향 표현 22개가 함께 죽는다 [측정]

`match_condition` 은 행 단위다. 대상이 든 행에 그냥 조건을 걸었더니 같은 행의 별칭이
전부 제약됐다.

```
살릴 것 13/35 생존   그냥 걸었을 때
살릴 것 35/35 생존   행을 쪼갠 뒤
```

죽은 것이 `편백` · `고목` · `목재` · `톱밥` · `당밀` · `시럽` · `분가루` · `아기분` ·
`침대` · `침구` · `생풀` · `수액` · `잎사귀` … **전부 사전이 의도적으로 넣은 향 표면형**
이다. 같은 행에 있다는 이유만으로 제약된다.

**같은 accord 가 두 `entry_id` 로 나뉘는 것이 이 결정의 대가다.** 표면형 단위로 조건을
걸 방법이 이것뿐이다.

### 근거 3 — 조건 낱말을 짐작에서 측정으로 바꿨다 [측정]

처음에 일곱을 짐작으로 적었다가 넷을 뺐다.

```
낱말    설문 143   합성 600   향 무관 18   판정
향       96%       99%        0%         쓴다
냄새     15%       23%        0%         쓴다
느낌     24%       40%        0%         쓴다
노트      6%        0%        6%         뺀다 — '노트북' 에 걸린다
향수     70%        1%        0%         뺀다 — '향' 의 부분집합. 743건 중 0건 추가
나는     24%        2%        6%         뺀다 — "나는 초록불에 건넜어" 통과
같은     10%       26%        6%         뺀다 — "나무젓가락 같은 거" 통과
```

**`노트` 가 `노트북` 에 걸린 것이 시사적이다.** 조건 낱말도 부분 문자열로 검사되므로
고치려는 문제와 똑같은 함정에 빠질 수 있다.

### 근거 4 — 대상 9개는 어휘 조사를 엔진으로 검증해 정했다 [측정]

처음엔 작성자가 떠올린 문장에서 역산한 10개였다. 그것이 근거가 약해 GPT 어휘 조사로
표면형 36개의 향 무관 낱말을 받고 **그 낱말을 그대로 엔진에 넣어** 가렸다(기록 27번).

```
처음 10개와 비교   둘 다 6 · 새로 들어옴 6 · 빠짐 4
```

**처음 고른 10개 중 4개는 근거가 없었고 6개를 놓치고 있었다.** 새로 찾은 것이 `세탁기` ·
`세탁소` · `분내다`(화내다) · `나무라다` · `산림청` · `이불킥` 이다.

그중 셋은 다시 뺐다. `우디` 는 `KOREAN_ACCORD` 경로라 사전 조건이 닿지 않고, `나무` 와
`과즙` 은 같은 행의 `편백나무` · `상큼한` 을 살리려고 포기했다.

### 근거 5 — 비용이 0 이면 편익 불확실성이 결정을 바꾸지 않는다

```
넣는다      오탐 막힘 (빈도 미상)   ·  잃는 것 0건 (설문 143 + 합성 600 · NDCG 델타 0)
안 넣는다    오탐 그대로 (빈도 미상)  ·  잃는 것 없음
```

**기댓값이 음수가 될 수 없다.** 되돌리기도 CSV 칸을 비우는 일이다.

처음에는 *"편익을 못 쟀으니 보류"* 로 판단했는데 **`spec.md` §7.2 를 잘못 적용한
것이었다.** 그 조항은 사전 **확장**의 출처를 제한한다. 이 변경은 **제약**이고 둘은 위험
방향이 반대다.

```
확장   근거 없이 넣으면 -> 틀린 매핑이 생긴다   -> 출처를 엄격히 따진다
제약   근거 없이 걸면  -> 정상 매칭을 잃는다    -> 743건으로 쟀고 0건이다
```

---

**한계 — 이 근거로 말할 수 있는 것과 없는 것**

- **긴 문장 오탐은 못 막는다.** `"꿀꿀한 기분일 때 쓸 향수"` 는 `향` 이 있어 통과한다.
  조건이 가르는 것은 *"오탐이냐"* 가 아니라 *"향 낱말이 있냐"* 다
- **합성어·신조어도 못 막는다.** `초록불` · `잼민이` · `이불킥` 은 이 방법으로도
  형태소 경계 판정으로도 막히지 않는다(기록 28번)
- **NDCG 가 안 움직인 것은 이득이 없다는 뜻이 아니라 평가셋이 못 잰다는 뜻이다.**
  합성 600·설문 143 이 전부 향수 요청이라 오탐이 날 입력이 표본에 없다.
  **해롭지 않다는 것까지만 말할 수 있다**
- **오탐의 실사용 빈도를 모른다.** 실사용 설문 143건에 사례가 0건인데 그 설문은
  *"향수 추천 문장을 써 달라"* 고 받은 자료다. `NLR_DB_TABLE_REQUEST.md` 의 표가
  쌓여야 안다

**되돌리는 법** — `.short` 행 9개를 지우고 원래 행의 `aliases` 에 표면형을 되돌린다.
또는 `80_lexicon_v1_15_short_form_condition.py` 의 `TARGET` 을 비우고 다시 돌린다.

**관련 자료**

- `docs/nlr_engineering_notes.md` 25번 — 문제의 출처(오탐 11/11)
- `docs/nlr_engineering_notes.md` 27번 — 이 결정의 측정 전체
- `docs/nlr_engineering_notes.md` 28번 — 기각한 대안(형태소 분석기)
- `DECISIONS.md` N13 — 형태소 분석기. 2026-09-18 에 범위를 좁혀 적었다
- `spec.md` §7.2 — 사전 확장의 출처 제한. **확장에만 적용된다**(근거 5)
- `analysis_outputs/80_lexicon_v1_15_changelog.md` — 변경 기록
- `80_lexicon_v1_15_short_form_condition.py` · `81_misfire_words.py` · `78` · `79` · `82`
- 팀 저장소 `S15P21E203-307` — 커밋 6baa4b4 · f352a31

---

## N17. 오타 검문소를 지금은 넣지 않는다 — 편집거리는 오타와 창의적 표현을 못 가른다

- 날짜: 2026-09-18
- 상태: **N18 로 대체됨** (2026-09-19) — 기각 근거가 한쪽만 잰 것이었다. 본문은 그대로 둔다
  · 원래 상태: 보류 — 측정 완료, 사용자 결정(2026-09-18) · 코드 변경 없음

**문제** — 사용자가 정상적으로 요청했는데 **근거까지 지어낸 엉뚱한 결과**가 나가는
경우가 있다 [측정 2026-09-18 · 기록 25·26번].

```
"빨레 냄새 나는 향수 찾아줘"        <- `빨래` 의 오타
  ⑤단계 추정 -> rubber · plastic   ("빨레 = 고무 같은 재질" 이라고 지어냈다)
  3회 모두 다른 향수가 나왔다 — 셋 다 가죽·타이어·휘발유였다
```

`ㅁㄴㅇㄹ` 에는 빈 목록을 내면서 `빨레` 에는 이러는 이유는 **의미 없는 글자는 의미
없다고 판단하지만, 그럴듯한 한국어 오타는 "모르는 단어" 로 보고 해석하려 들기 때문**이다.

설계 방향은 ⑤단계 **앞에** 검문소를 세우는 것이었다. 사전이 조건을 하나도 못 뽑았을 때
*"이 표현이 사전에 있는 말의 오타인가"* 를 먼저 보고, 오타면 교정해 사전 경로로 보내고
아니면 ⑤단계 LLM 으로 보낸다. **LLM 을 결정권자가 아니라 후보 제안자로 강등시킨다.**

**검토한 방법**

1. **표현 전체를 사전 표면형과 편집거리 비교** — 거리 1이고 후보가 하나면 교정
2. **표현 안의 낱말 단위로 편집거리 비교** — 낱말 하나씩 교정
3. **낱말 단위 + 국어 낱말 목록** — *"국어에 없는 낱말"* 만 교정 대상으로 삼는다
4. **아무것도 안 한다** — 실사용 로그로 빈도가 보일 때까지 기다린다

**결정** — (4)를 택했다. **(1)(2)(3) 을 다 재봤고 셋 다 목표 사례를 못 잡는다.**

- **(1) 기각** — `빨레 냄새` 는 사전에서 **자모 거리 3** 이라 교정 대상이 아니다.
  게다가 *"가까운 말이 없으면 ⑤단계로 안 보낸다"* 로 하면 **⑤단계 입력의 98.1%가
  차단된다.** N15 가 얻은 것이 통째로 되돌아간다. 근거 2
- **(2) 기각** — `빨레 → 빨래` 는 잡지만 **실제 입력의 10.3%를 망가뜨린다.**
  `향긋한 풀 느낌` → `꿀 느낌` · `너무 무겁지 않게` → `나무 무겁지 않게`. 근거 3
- **(3) 기각** — 오교정 40건 중 28건을 막아 주지만 **`빨레` 도 같이 막힌다.**
  `빨레` 가 국어 사전에 등재된 낱말(`NNG`)이기 때문이다. 근거 4
- **음절이 아니라 자모로 재야 한다는 것은 확인했다.** 다시 열 때 쓴다. 근거 1

**결과** — 코드를 바꾸지 않았다. 사전도 그대로다.

---

### 근거 1 — 음절 단위는 볼 것도 없이 탈락이다

`빨레`/`빨래` 는 글자로 한 글자 차이, 자모로 `ㅐ`/`ㅔ` 한 개 차이다. 어느 쪽으로 재느냐가
나머지 전부를 바꾼다 [측정 2026-09-18 · 사전 v1_15 표면형 275개].

| | 음절 거리 1 | 자모 거리 1 |
|---|---:|---:|
| 사전 표면형끼리 충돌하는 쌍 | 28쌍 | **0쌍** |
| 실사용 설문 낱말 2,727개 중 교정 대상 | 204개 낱말 · 688회 | 12개 낱말 · 82회 |

음절 기준은 `향수`→`향긋`, `너무`→`나무`, `향`→`꿀/숲/잼/탑` 을 만든다.

### 근거 2 — 표현 전체로 재면 ⑤단계가 죽는다

검문소의 실제 입력은 **⑤단계까지 내려온 표현**이다. 그 목록으로 쟀다
[`analysis_outputs/74_estimator_checkpoint.csv` 369개 · 글자 수 중앙값 11].

```
사전 표면형과의 자모 최소거리
  거리 0        1개 ( 0.3%)
  거리 1        3개 ( 0.8%)   달콤함·달큰함·자연스럽고 — 전부 어미 변형이다
  거리 2        3개 ( 0.8%)
  거리 3 이상  362개 (98.1%)  <- "가까운 말 없음"
```

`빨레 냄새` 는 거리 3, `화장품 같은 향` 은 거리 11, `무겁지 않게` 도 멀다.
**편집거리는 오타와 창의적 표현을 가르지 못한다. 둘 다 사전에서 멀다.**

### 근거 3 — 낱말 단위는 목표를 맞히지만 10%를 부순다

표현 전체가 아니라 표현 안의 낱말을 자모 거리 1로 고치면 `빨레 냄새` → `빨래 냄새` 가
되고 `화장품 같은 향` · `무겁지 않게` 는 안 건드린다. **그런데 369개 중 38개(10.3%)가
교정 대상이 되고 대부분이 오교정이다.**

```
향긋한 풀 느낌          -> 향긋한 꿀 느낌      풀 → 꿀
시원한 물 느낌          -> 시원한 꿀 느낌      물 → 꿀
겨울에 코트 입고 뿌리기 좋음 -> 노트 입고         코트 → 노트
너무 무겁지 않게         -> 나무 무겁지 않게     너무 → 나무 (5건)
부드러운 단향           -> 부드러운 잔향       단향 → 잔향
술 향이 진하고 깊음       -> 모호              술 → 꿀 / 숲
```

### 근거 4 — `빨레` 가 국어 사전에 있다

나쁜 교정은 전부 진짜 한국어 낱말(`너무` `풀` `물` `술` `코트`)이고 `빨레` 는 아니라고
보았다. **틀렸다.** kiwi 형태소 사전 **529,335개 형태**를 전부 훑어 확인했다
[측정 2026-09-18 · `kiwipiepy` 0.23.2].

```
빨레   사전에 있다 (NNG)  -> 교정 안 됨   ❌ 목표 사례를 놓친다
숯    사전에 있다        -> 교정 안 됨   ✅ 숯→숲 오교정이 자동으로 막힌다
단향   사전에 없다        -> 잔향 으로 교정  ❌ 단향(檀香)은 실제 향 용어다
뷁쿱   사전에 없다        -> 가까운 말 없음  ✅
```

목록을 걸면 오교정 40건 중 28건이 막히지만 **남는 교정 12건 중 쓸 만한 것은 어미 변형
6종뿐이고, `빨레` 는 통과한다.**

**형태소 태그로는 가릴 수 없다.** 분석기가 아무 글자 조합이나 `NNG` 로 찍는다 —
`뷁쿱/NNG` · `츄릅삥/NNG`. 미등록 태그가 붙지 않는다.

---

**한계 — 이 근거로 말할 수 있는 것과 없는 것**

- **오타의 실사용 빈도를 모른다.** ⑤단계 입력 369개에 오타로 보이는 것이 **0건**이다.
  `빨레` 는 지난 세션이 만든 시험 문장이다. **빈도를 모르는 채로 검문소를 짓는 중이었다**
- **369개는 설문·합성에서 나온 것이다.** 실제 검색창 입력은 더 짧고 오타가 많을 수 있다
- **국어 낱말 목록의 품질을 따로 검증하지 않았다.** kiwi 사전 하나만 봤다.
  다른 목록이면 `빨레` 가 없을 수도 있다
- **`단향` 같은 향 전문 용어가 국어 목록에 없다.** 교정을 켜면 이 계열이 위험하다.
  사전에 표면형으로 추가하면 막히지만 그 목록을 따로 관리해야 한다

**다시 볼 조건** — **실사용 로그가 쌓여 오타 빈도가 보일 때.**

`nlr_unmatched_expressions` 에 표현이 쌓이면 다음을 세어 판단한다.

```
오타로 보이는 표현의 비율        0에 가까우면 검문소를 짓지 않는다
그중 사전 표면형과 자모 거리 1     교정으로 건질 수 있는 몫
⑤단계가 지어낸 설명이 붙은 비율    tier='LLM' 이고 조건이 엉뚱한 것
```

**320(DB 표 4개)이 배포돼야 이 데이터가 생긴다.** 배포 여부가 아직 확인되지 않았다.

**재현 방법** — 측정 스크립트는 저장소에 남기지 않았다(41번 이후 관례).
`74_estimator_checkpoint.csv` 와 `domain_lexicon_v1_15.csv` 로 다시 계산할 수 있다.
낱말 목록은 `kiwipiepy` 의 `Kiwi().morpheme(i)` 를 0부터 훑어 뽑는다(EDA venv 에 있다).

**관련 자료**

- `docs/nlr_engineering_notes.md` 29번 — 이 결정의 측정 전체
- `docs/nlr_engineering_notes.md` 25번 — `빨레` 사례의 출처
- `docs/nlr_engineering_notes.md` 26번 — 파일 캐시가 이 오염을 굳히던 문제
- `DECISIONS.md` N15 — ⑤단계 추정. 이 검문소가 그 앞에 서려던 것이다
- `DECISIONS.md` N13 — 형태소 분석기. 근거 4가 그 판단에 한 줄을 더한다
- `docs/plans/NLR_NEXT_SESSION.md` 2장 — 이 설계를 적어 둔 곳
- `analysis_outputs/74_estimator_checkpoint.csv` — 근거 2·3·4 의 입력

---

## N18. 오타 검문소를 만든다 — N17 을 뒤집는다. 편집거리로 못 가른 것은 국어 낱말 목록이 가른다

- 날짜: 2026-09-19
- 상태: **제안** — 측정 완료, 사용자 결정(2026-09-19), 팀 승인 전
  · **코드 변경 있음** — 팀 저장소 `S15P21E203-332` 커밋 `431c3b4`
- **N17 을 뒤집는다.** N17 의 기각 근거가 한쪽만 재고 내린 결론이었다

**문제** — N17 이 검문소를 접은 근거가 **`빨레` 를 못 잡는다** 하나였다. 그런데 N17 은
*"멀쩡한 입력을 얼마나 망가뜨리나"*(오교정률)만 재고 *"실제 오타를 얼마나 잡나"*
(포착률)는 재지 않았다. **교환비를 따지려면 두 수가 다 있어야 한다.**

그리고 목표는 *"오타로 인한 환각을 줄이기"* 인데 동기가 된 사례 하나를 합격 기준처럼
썼다. N17 이 기각한 ③(낱말 단위 + 국어 목록)은 실측 오교정이 **369개 중 2건(0.5%)**
이었다.

**서비스를 공개하기 전에는 실사용 데이터가 없다.** *"데이터가 쌓이면 판단하자"* 는
공개하고 나서 문제를 확인하자는 말이 된다. 예방이 목적이면 순서가 거꾸로다.

**검토한 방법** — N17 의 셋에 포착률을 붙여 다시 봤다.

1. **표현 전체를 사전과 편집거리 비교** — N17 근거 2 그대로 기각
2. **낱말 단위 편집거리만** — N17 근거 3 그대로 기각(실제 입력의 10.3% 를 망가뜨린다)
3. **낱말 단위 + 국어 낱말 목록** — **포착률을 재보니 93.0%** 다
4. **아무것도 안 한다** — N17 의 결정

**결정** — (3)을 택했다. **⑤단계 앞에 검문소를 세운다.**

```
검문소는 사전이 조건을 하나도 못 뽑았을 때만 돈다
   ↓  표현 안의 낱말을 하나씩 본다
사전 표면형이다 / 보호 목록에 있다 / 1글자다   -> 그대로
   ↓  사전 표면형과 자모 거리 1 후보를 찾는다
후보 1개   -> 교정하고 사전 경로를 다시 태운다
후보 여럿  -> **빈 결과** (지어내지 않는다)
후보 없음  -> 그대로 ⑤단계 LLM 으로
```

- **자모 단위로 잰다.** 음절 단위는 사전 표면형끼리 충돌이 28쌍이고 자모는 0쌍이다. 근거 1
- **거리는 1 · 2글자 이상.** 거리 2 는 충돌이 0 -> 12쌍이 된다. 1글자는 `꿀`·`숲`·`잼`·
  `탑` 이 서로 이웃이라 뺀다
- **보호 목록 628개를 데이터 파일로 싣는다.** 국어 사전 53만 개가 필요 없다. 근거 3
- **향 전문 용어는 손으로 예외를 둔다.** `단향` 과 그 조사 형태. 근거 4
- **목록 파일이 없으면 검문소를 끈다.** 경고만 남긴다 — 목록 없이 교정하면 10.3% 가
  망가지므로 끄는 편이 안전하다
- **응답 계약을 바꾸지 않는다.** 검문소가 한 일은 `corrections.jsonl` 에만 남는다
- (4) **기각** — 공개 전 예방이 목적인데 데이터를 기다리면 목적을 못 이룬다

**결과** — 실제 엔진 코드로 잰 값이다.

| | 값 |
|---|---:|
| 포착률 (생성 오타 3,775개) | **93.0%** |
| 엉뚱한 사전 항목으로 교정 | **0.0%** |
| 놓침 (오타 결과가 국어 낱말) | 4.6% |
| 모호 (후보 여럿) | 2.4% |
| ⑤단계 실제 표현 369개 오교정 | **0건** |
| 검문소 지연 | 중앙 0.779ms · 최대 3.835ms |

369개를 `recommend()` 로 돌리면 그대로 361 · 교정 8 · 모호 0 이고, **LLM 호출이
367 -> 362 로 준다.** 교정 8건은 전부 어미 변형이다.

---

### 근거 1 — 음절이 아니라 자모다

`빨레`/`빨래` 는 글자로 한 글자 차이, 자모로 `ㅐ`/`ㅔ` 한 개 차이다.

| | 음절 거리 1 | 자모 거리 1 |
|---|---:|---:|
| 사전 표면형끼리 충돌하는 쌍 | 28쌍 | **0쌍** |
| 설문 낱말 2,727개 중 교정 대상 | 204개 · 688회 | 12개 · 82회 |

음절 기준은 `향수`→`향긋`, `너무`→`나무`, `향`→`꿀/숲/잼/탑` 을 만든다.

### 근거 2 — 편집거리만으로는 안 되고, 국어 목록이 그 구멍을 메운다

낱말 단위 교정만 하면 `빨레향` 은 잡지만 실제 표현의 **10.3%** 가 망가진다 —
`향긋한 풀 느낌`→`꿀 느낌` · `너무 무겁지 않게`→`나무 무겁지 않게` ·
`겨울에 코트 입고`→`노트 입고`.

**나쁜 교정은 전부 진짜 한국어 낱말이다.** 국어 목록으로 그것들을 지키면 오교정이
40건 -> 0건이 된다(369개 기준).

### 근거 3 — 국어 사전 53만 개가 아니라 614개면 된다

검문소가 물어보는 낱말은 사전 표면형에서 자모 한 개 떨어진 것뿐이다. 나머지는 애초에
후보가 안 나온다.

```
kiwi 형태소 사전 전체     529,334개 -> 포착률 93.1%
사전 표면형과 자모 거리 1      614개 -> 포착률 93.0%   (4.4KB)
```

**AI 서버에 형태소 분석기를 넣지 않아도 된다.** N13 이 분석기를 기각한 이유
(의존성 +1 · 메모리 +10MB)가 그대로 유효하다. 목록은 EDA 의
`83_protected_words_v1_15.py` 가 만들고 사전 버전마다 다시 돌린다.

### 근거 4 — 향 전문 용어는 자동 보호가 안 된다

`단향`(檀香, 백단향)이 국어 사전에 없어 `잔향` 으로 바뀌었다(369개 중 4건).
향 용어 29개 중 위험한 것은 이것 하나였다 — `침향`·`사향`·`몰약`·`정향`은 국어 사전에
있고 `통카`·`베티버`·`패출리`는 표면형과 멀다.

손으로 예외를 두되 **조사 형태까지 함께 생성한다.** 한국어가 교착어라 `단향` 만 적으면
`단향이` 가 따로 걸린다(614 -> 628개).

`단향` 을 사전 표면형으로 올리는 것이 더 나은 해법이지만 그것은 **사전 확장**이라
`spec.md` §7.2 의 출처 제한을 따라야 하는 별개 결정이다.

### 근거 5 — 브랜드명은 위험하지 않았다

검문소의 규칙이 *"국어 사전에 없는 낱말만 교정"* 이라 브랜드명과 외래어가 전부 후보가
된다. 설문에서 그 부류 52개를 뽑아 쟀더니 **사전 표면형과 자모 거리 1 인 것이 0개**였다
(`조말론`·`톰포드`·`딥티크`·`베르가못`·`오우드`·`화이트머스크` 등).

---

**한계 — 이 근거로 말할 수 있는 것과 없는 것**

1. **`빨레 냄새` 는 여전히 못 잡는다.** `빨레` 가 국어 사전에 등재된 낱말이라 보호 목록이
   지킨다. 보호 목록은 `풀`·`너무`·`코트` 를 지키느라 `빨레` 도 같이 지킨다.
   **`빨레향`(붙여쓴 형태)은 잡힌다** — 그건 국어 낱말이 아니다. 이 맞바꿈이 4.6% 다
2. **93.0% 는 생성 오타 기준이다.** 실제 오타 분포와 다를 수 있고, 자판 오타가
   3,775개 중 3,213개라 가중치가 쏠려 있다. 유형을 고르게 보면 88% 안팎이다
3. **오타의 실사용 빈도를 모른다.** 설문 155문장에서 실제 오타가 사실상 0건이다.
   **검문소가 얼마나 자주 일할지 모른 채 만든 것이다.** 비용이 4.6KB 라 감수했다
4. **향 용어 29개는 내가 고른 것이다.** 빠진 부류가 있을 수 있다. 실사용
   `corrections.jsonl` 로 확인해야 한다
5. **모호 처리(빈 결과)를 실사용으로 확인하지 못했다.** 369개에서 0건이라 생성 오타로만
   확인했다
6. **보호 목록이 사전 버전에 묶인다.** v1.16 이 되면 생성 스크립트를 다시 돌려야 한다.
   잊으면 조용히 나빠진다 — 새 표면형의 오타를 못 잡거나 새로 위험해진 낱말을 못 막는다

**다시 볼 조건**

- **`corrections.jsonl` 이 쌓였을 때.** 오교정이 보이면 보호 목록에 더하거나 규칙을 좁힌다
- **사전 버전이 올라갈 때.** 목록을 다시 만든다 (한계 6)
- **`빨레` 유형의 빈도가 보일 때.** 4.6% 가 실제로 아프면 다른 신호를 찾아야 한다

**재현 방법**

```bash
cd EDA
./venv/Scripts/python.exe -W ignore 83_protected_words_v1_15.py
```

포착률·오교정은 스크립트로 남기지 않았다(41번 이후 관례). `74_estimator_checkpoint.csv`
와 `nlr_engine` 의 `typo_gate`·`_typo_candidates` 로 다시 계산할 수 있다.

**관련 자료**

- `docs/nlr_engineering_notes.md` **30번** — 이 결정의 측정 전체
- `docs/nlr_engineering_notes.md` 29번 — N17 의 측정. 무엇을 안 쟀는지 30번이 적었다
- `DECISIONS.md` **N17** — 이 결정이 뒤집는 것
- `DECISIONS.md` N15 — ⑤단계 추정. 검문소가 그 앞에 선다
- `DECISIONS.md` N13 — 형태소 분석기. 근거 3 이 그 판단을 유지시킨다
- `spec.md` §7.2 — 사전 확장의 출처 제한. `단향` 을 사전에 올리려면 여기를 따른다
- `83_protected_words_v1_15.py` — 보호 목록 생성
- 팀 저장소 `S15P21E203-332` — 커밋 `431c3b4`
