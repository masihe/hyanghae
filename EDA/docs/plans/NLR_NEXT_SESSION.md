# 자연어 추천 — 다음 세션 인수인계

| | |
|---|---|
| 작성 | 2026-09-18 (전면 개정) · 2026-09-20 갱신 — 343·346 병합 · 347 기각 후 **근거가 무너짐** · **확장 NDCG 신설** |
| 담당 | 김용석 (AI) |
| 저장소 | 팀 `C:\Users\SSAFY\Desktop\S15P21E203` 의 **`ai/` 만** · 개인 `C:\Users\SSAFY\Desktop\hyanghae` 는 자유 |
| 이 문서 | **다음 세션이 시작할 때 읽는다.** 일이 끝나면 갱신하거나 지운다 |

> **347 은 MR !207 로 올라갔고 리뷰 둘을 반영했다.**
> ① 347 리뷰(성별·계절·낮밤) · ② ⑤단계 되살림(보류) · ③ 320 배포(인프라) ·
> ④ **노트 정렬을 다시 연다**(35번이 기각 근거를 무너뜨렸다 · 새 Jira 번호 필요)

---
## 0. 남은 일은 다섯이다 — 이 표가 이 문서의 목차다

```
①  347 성별·계절·낮밤        1-11장 **MR !207 · 리뷰 1회 반영** 확장 NDCG +0.0184 (유의)
②  노트 정렬 다시 열기         1-10장 **새 Jira 번호가 필요**   확장 NDCG +0.0087 (유의)
③  ⑤단계 되살림              4장    **보류**              백엔드 협의가 필요하다
④  320 배포 여부 확인          2장    **인프라 담당**         이것 하나가 여럿을 막고 있다
⑤  미뤄도 되는 것 셋           4장    나중에                C1 파이프라인 · 주입 방어
```

**①은 리뷰만 남았다.** ②는 바로 시작할 수 있다 — 측정이 끝나 있고 남은 것은
배포 데이터 +38% 를 받아들일지의 판단뿐이다 (N9 정정 추기).

### ① 347 성별·계절·낮밤 — **MR !207 · 리뷰 1회 반영** (1-11장 · 35번)

```
브랜치  feature/ai/S15P21E203-347-context-conditions   원격에 있다
커밋    28be934 배포 데이터 3칸 추가 · 6128aec 엔진이 후보를 좁힌다
```

**측정과 구현이 모두 끝났고 MR 만 만들면 된다.** 자세한 것은 1-11장.

```
확장 NDCG   0.632047 -> 0.650458   +0.018411 [+0.014092, +0.022960] 유의
accord     0.640157 -> 0.643859   **기존 지표도 함께 오른다**
주 지표     설문 gender 1.80 -> 5.00 · season 2.11 -> 5.00 · daypart 4.43 -> 5.00
결과 0건    13 / 9 / 3  **그대로**
배포 파일   4.97 -> 5.27MB (+6.0%)
```

**리뷰에 물은 것 셋** — 배포 순서 · `CONTEXT_MIN=3` 값 · 성별을 투표로 볼지 공식으로 볼지.

---

### ② 노트 정렬을 다시 연다 — **새 Jira 번호가 필요하다** (1-10장 · N9 정정 추기)

**원본 59칸 중 6칸만 쓰고 있었다.** 그리고 ①단계가 뽑는 것을 엔진이 버린다.

```python
# nlr_engine.py:69
STRUCTURED_KEYS = ("scent_preference", "avoid", "additional_requirements")
```

```
                     설문 143   합성 600   원본 칸
context.season        19.6%     18.2%    winter·spring·summer·autumn
context.gender        19.6%      7.3%    gender · perceived_* 5칸
performance.longevity 14.7%      0.5%    longevity_avg     <- 33·68번이 이미 봤다
performance.intensity 13.3%     11.0%    sillage_avg       <- 위와 같음
context.daypart        7.0%      7.7%    day · night
```

**기각했다가 근거가 무너진 건이다.** 확장 NDCG 로 재면 부호가 반대다.

```
accord 만으로   -0.006384 유의하게 나쁨   <- 이것을 근거로 기각했다
확장으로       **+0.008713 유의하게 좋음**
```

**그리고 347 이 병합되면 손실이 사라진다.**

```
노트 단독              accord 0.635962   -0.0042
성별·계절·낮밤 (=347)   accord 0.643544   +0.0034
**둘 다**              accord 0.640146   **±0.000**   확장 NDCG 는 +0.027983
```

**남은 판단은 배포 데이터 +38% 하나다.** 347 의 +6.0% 위에 노트를 얹으면
4.97 -> 7.21MB 가 된다 (번호 인코딩 · 어휘표 0.02MB 별도).

```
할 일   Jira 새 이슈 · build_data.py 에 노트 칸 · load_index 적재 ·
       _rank 에 노트 보유를 최상위 키로  <- **N9 를 바꾸는 유일한 조각**
```

⚠ **`performance` 둘은 아직 아무도 안 열었다.** ①단계가 뽑는 나머지 두 칸이다
(설문 longevity 14.7% · intensity 13.3%). 33번·68번이 이미 봤고 세 조각(v4 프롬프트 ·
정렬 2차 키 · 배포 데이터)이 함께 가야 한다. 그 기록을 먼저 읽는다.

---

### 2026-09-19~20 에 닫힌 것 일곱 — 다시 열지 않는다 (**347 은 예외다. 아래 ⚠**)

```
오탐 검증 156문장      1-5장 · 32번 · N19.  Wilson [74.1%,100%] -> [89.7%,99.7%]
형태소 분석기          1-6장 · 28번 추기.   **기각.** 실질 이득 4/51
⑤단계 문장 맥락 주기   4장 ① · 33번.       **네 각도로 다 쟀다.** 아래 ①
설문 공개 저장소       6장.                **그대로 두기로 정했다.** 응답자 식별 불가
S15P21E203-343       1-7장.              **병합됨** develop 3efb547 · 추천 이유 표현
S15P21E203-346       1-8장 · 34번 추기 둘.  **병합됨** develop 598e1eb · 한국어 노트 별칭
노트 보유 정렬         1-9장 · 34번 추기 셋째. **기각 — 그런데 근거가 무너졌다**(1-10장)
                     ⚠ Jira 347 은 이 안이 아니라 **성별·계절·낮밤**으로 다시 썼다
```

**346 은 `미모사` 로 검색하면 미모사가 없는 향수가 나오던 문제다.** NDCG +0.025303
[+0.0146, +0.0364] · 결과 0건 67 -> 55. **다만 설문에서는 2.54 -> 2.55 로 거의 안
움직인다** — 자세한 것과 남긴 한계 넷은 1-8장에 있다.

---

### ③ ⑤단계 되살림 — **2026-09-20 보류.** 잴 것은 다 쟀다

**보류 사유** — 계약이 바뀌면 백엔드와 이야기해야 하는데 그날 불가능했다.

**⚠ 다만 두 결정의 계약 영향이 다르다. 다시 열 때 분리해서 본다.**

```
v2 프롬프트 채택        **응답 계약이 안 바뀐다.** `estimator` callable 시그니처만
                     바뀌고 그건 `ai/` 안의 내부 계약이다 (33번 비용 측정).
                     -> **백엔드 협의 없이 갈 수 있는 쪽이다**

원문 단위로 한 번만 호출   `evidence` 의 `from`(어느 표현에서 왔는지)을 못 적어
                     **응답 계약이 바뀐다.** -> 백엔드 협의가 필요하다
```

보류 결정 시점에는 둘을 묶어서 봤다. 나중에 *"계약 안 바뀌는 쪽만 먼저 할까"* 를
다시 볼 수 있다.

**문제** — 사전이 조건을 막아도 ⑤단계 LLM 이 되살린다 [32번 · N19].

```
사전 조건이 막아야 하는 칸을 막는가       50/51  98.0%   <- v1.15~v1.17 이 적어 온 수
사전이 막은 문장이 되살아나는가          19/50  38.0%   (⑤단계 34% + ①단계 경로 4%)
오탐 문장 102개 중 끝까지 막힌 것        31/102 30.4%   <- **사용자가 겪는 수**
```

```
시럽 없는 커피로 주세요   ->  coffee        1위 Black Opium Intense
세탁소에 코트 맡겼어      ->  fresh·soapy   1위 Laundry
잔디밭에 앉아서 쉬었어    ->  fresh·green   1위 Cowboy
```

**사전을 더 올려도 이 38% 는 안 줄어든다.** 오히려 사전이 막을수록 ⑤단계로 넘어가는
문장이 늘어 되살아날 기회가 는다.

#### 막는 방법을 바꾸는 것으로는 안 풀린다 — 표본 둘이 같은 말을 한다

```
사전 조건으로 막은 것을 ⑤단계가 되살림    19/50  38.0%  [25.9%, 51.8%]
경계 판정으로 막은 것을 ⑤단계가 되살림     9/13  69.2%  [42.4%, 87.3%]
```

그리고 **막으면 더 나빠지기도 한다** — `초록물고기 분위기 같은 향수` 가 사전 조건일 때
`green` 이었는데 경계 판정으로 조건이 0개가 되자 ⑤단계가 `물고기` 를 바다로 읽어
`aquatic·green·marine` 이 됐다. **⑤단계를 고칠 때 이것을 함께 봐야 한다.**

#### 후보 셋의 상태

```
⑤단계에 문장 맥락 주기   **네 각도로 다 쟀다(33번 + 추기 3).** 아래에 결과가 있다
                    남은 것은 측정이 아니라 **설계 결정**이다
⑤단계 앞 향 요청 판별   **통째로 미정.** 판별을 무엇으로 할지 정해진 것이 없다.
                    오타 검문소 자리가 비어 있다 — 150회 전부 아무 일도 안 했다
형태소 분석기          **기각**(1-6장). ⑤단계 되살림이 풀리면 다시 연다
```

#### 문장 맥락 주기 — 잰 것 전부

```
비용        코드 6곳 · 응답 형식은 안 바뀐다 · 프롬프트 +4.4~11% ·
           캐시 키를 원문 단위로 (설문 2.6% · 합성 10.1% 손실)
표현 단위    오탐 -13.0%p · 커버리지 +12.7%p
파이프라인    오탐 -13.7%p · 커버리지  +8.0%p · 정상 쪽에서 죽은 문장 0개
NDCG       **변화 없음** [-0.0071, +0.0067].  ⑤단계를 켜는 것 자체는 +0.0398
정체        **v2 는 표현이 아니라 원문에 답한다.** 같은 원문 안 표현끼리 accord
           자카드가 v1 0.150 -> v2 0.473 이다
```

**커버리지가 오른 이유가 "표현을 더 잘 이해해서" 가 아니다.** 원문의 accord 를 모든
표현에 복사해서 오른 것이다 — **정확도가 아니라 단위가 바뀌었다.**

#### 그래서 정할 것 둘

```
1  이 단위 변화를 받아들일 것인가
   받아들이면   오탐 -13%p · 커버리지 +8~13%p · NDCG 그대로. 값은 확실하다
   잃는 것     표현 단위 의미. `값비싸 보이는 느낌` 에 whiskey 가 붙는다.
              v1 이 빈 목록 내던 142개 중 **48%** 가 답하기 시작하고
              거기에 부정 표현 6개와 가격 표현 1개가 섞인다

2  받아들인다면 ⑤단계를 표현마다 부를 이유가 남았나
   원문에 답할 거라면 **한 번만 부르면 호출이 1/3 로 준다**
   다만 `evidence` 의 `from`(어느 표현에서 왔는지)을 못 적어 응답 계약이 바뀐다
```

**둘 다 측정이 아니라 판단이다.** 자료는 4장 ① 과 33번에 있다.
**2026-09-20 에 둘 다 보류했다** — 위의 사유 참고.

#### 남은 미지수 — 측정으로 메울 수 있는 것

```
①단계 표현이 사전 조건을 통과시키는 경로   되살림 19건 중 2건. **⑤단계를 고쳐도 안 막힌다.**
                                  빈도를 모른다 (33번 추기의 정정)
표본이 작다                        표현 96 · 파이프라인 42 · NDCG 1회분.
                                  NDCG 는 개별 쿼리가 0.67 까지 움직인다
```

### ④ 320 배포 여부

배포가 안 됐으면 `nlr_*` 표에 아무것도 안 쌓이고, 그러면 **아래가 전부 막힌 채로
남는다.**

```
⚠ 미매칭 로그 기준이 바뀌었다  **343 병합 전후로 다르다**(1-7장). 배포가 343 뒤라면
                          처음부터 한 기준이다. 앞이면 집계 쪽에 경계를 알려야 한다
오타 검문소의 실제 오교정      corrections.jsonl 과 nlr_* 표가 쌓여야 안다 (N18 다시 볼 조건)
사전 조건이 적절한지          조건을 건 표면형이 v1.17 기준 **29개**다 (32번이 엔진으로 셌다)
여섯 칸의 실사용 분포         32번이 잰 것은 **칸 안의 비율**이지 칸의 빈도가 아니다
①단계 경로의 빈도            ①단계 표현이 사전 조건을 통과시키는 경로 (33번 추기)
한 낱말 입력 빈도            조건을 건 표면형은 한 낱말로 입력하면 죽는다. 설문엔 0건이다
형태소 분석기를 다시 열지      **기각했지만**(1-6장) ⑤단계 되살림이 풀리면 12~13개가
                          그대로 실질 이득이 된다. 그때 다시 연다
```

**확인은 인프라 담당에게 물어보는 것이다.** 로컬 확인법은 2장에 있다.

### ⑤ 미뤄도 되는 것 셋

C1(한 낱말 입력) 파이프라인 측정 · 프롬프트 주입 방어 · 평가 체계 개편이다.
4장에 각각 왜 미뤘는지 적혀 있다.

**평가 체계 개편이 347 기각과 맞물린다.** NDCG 의 정답 조건 `C` 가 accord 라서
**노트 효과를 구조적으로 못 본다.** 347 을 기각할 때 두 지표(노트 일치 · NDCG)가 서로
다른 것을 재서 합칠 수 없었고 결국 사람이 골랐다. 정답 조건에 노트가 들어가면 그런
판단을 수치로 할 수 있다 — 1-9장의 "다시 볼 조건" 참고.

---

## 1. 끝난 것 — 병합된 여덟

```
S15P21E203-326   ⑤단계 캐시 파일 복원 오류 수정      develop 08fcd55 · 커밋 734aa76
                 cached_estimator 가 기동 시 load_estimates() 를 부르지 않는다.
                 수정 전 코드와 같은 확인 여섯 개 중 하나만 실패 -> 통과로 뒤집혔다
                 **⚠ 오타 환각 자체는 안 고쳤다.** 파일에서 되살아나는 것만 막았다

백엔드 후속        nlr_* 표에 여섯 칸 저장            develop 0dc97d9
                 리뷰 의견 셋을 냈고 셋 다 반영됐다
                 **내 의견 ③이 틀렸다** — V23 의 주석을 고치면 Flyway 체크섬이 깨져
                 이미 적용한 DB 가 전부 기동을 못 한다. V24 로 덧붙인 쪽이 맞다

S15P21E203-330   계약서 result_count 기준 명시      develop 7e97e11 · 커밋 7b4e3a3
                 "AI 가 찾은 개수이고 사용자가 본 개수가 아니다" 를 적었다.
                 예시의 resultCount 5 를 1 로 고쳤다 — 그 예시의 results 가 하나뿐이라
                 자기모순이었고 백엔드 fixture 가 그대로 베껴 갔었다

S15P21E203-332   ⑤단계 앞 오타 검문소                develop 4f8eba5 — **1-1장**
S15P21E203-334   사전 v1.16 · 침대·침구 오탐          develop 13639da — **1-2장**
S15P21E203-335   사전 v1.17 · 껍질·시럽 오탐          develop ad0792c — **1-4장**
S15P21E203-343   추천 이유 표현을 입력 표면형으로       develop 3efb547 — **1-7장**
S15P21E203-346   한국어 노트 이름을 accord 로          develop 598e1eb — **1-8장**
```

**`develop` 전체 백엔드 테스트를 돌려 확인했다 — 786개 통과 · 실패 0** (330 병합 시점).
V24 가 추천 밖 통합 테스트에도 영향이 없다.

---

## 1-1. 오타 검문소 `S15P21E203-332` — 병합됨. 다음 사람이 알아야 할 것

**한 번 접었다가(N17) 다시 열었다(N18).** 접은 근거가 포착률을 안 재고 내린 것이었다.
측정 기록 **29·30번**과 `DECISIONS.md` **N17·N18** 에 전부 있다.

```
커밋   431c3b4  feat: ⑤단계 앞에 오타 검문소 추가
      fb61c8b  fix: 검문소 판정이 조용히 사라지는 경로에 경고 추가 (리뷰 반영)
파일   ai/nlr_engine.py · ai/server.py · ai/unmatched_log.py
      ai/data/protected_words_v1_15.txt (628줄 · 4.6KB) — **지금은 v1_17 이다**
```

### 무엇을 하나

```
검문소는 사전이 조건을 하나도 못 뽑았을 때만 돈다  (⑤단계 앞)
   ↓  표현 안의 낱말을 하나씩 본다
사전 표면형 / 보호 목록에 있음 / 1글자   -> 그대로
   ↓  사전 표면형과 **자모** 거리 1 후보를 찾는다
후보 1개   -> 교정하고 사전 경로를 다시 태운다
후보 여럿  -> 빈 결과 (지어내지 않는다)
후보 없음  -> 그대로 ⑤단계 LLM 으로
```

```
포착률 (생성 오타 3,775개)     93.0%    엉뚱한 사전 항목으로 교정 0.0%
⑤단계 실제 표현 369개          그대로 361 · 교정 8 · 오교정 0 · LLM 호출 367 -> 362
검문소 지연                   중앙 0.779ms
```

### ⚠ 세 가지

```
보호 목록은 사전 버전에 묶인다  사전을 올릴 때마다 생성 스크립트를 다시 돌려야 한다.
                          334·335 에서 두 번 돌렸고 **표면형이 안 바뀌어 내용은 같았다**.
                          **안 하면 조용히 나빠진다** — 새 표면형의 오타를 못 잡거나
                          새로 위험해진 낱말을 못 막는다. 절차는 7장에 목록으로 있다

빨레 냄새는 여전히 안 잡힌다   `빨레` 가 국어 사전에 있어 보호 목록이 지킨다. 보호 목록은
                          `풀`·`너무`·`코트` 를 지키느라 `빨레` 도 같이 지킨다.
                          **`빨레향`(붙여쓴 형태)은 잡힌다.** 이 맞바꿈이 놓침 4.6% 다

오타의 실사용 빈도를 모른다    설문 155문장에 실제 오타가 사실상 0건이었다(브랜드명과
                          외래어뿐). 검문소가 얼마나 자주 일할지 모른 채 만들었다.
                          `ai/logs/corrections.jsonl` 이 쌓이면 보인다 — 2장이 막고 있다
```

### 리뷰에서 나온 것 — 아직 안 한 것 하나

```
영문 표면형 17개가 typo_targets 에 들어가 있다 (EDP·clean·cute…). 한글 낱말과는
거리 1이 될 수 없어 **무해하지만 영원히 매칭되지 않는 죽은 항목**이다.
빼는 것은 동작이 같고 검증을 다시 돌려야 해서 두었다. 리뷰어도 넘어갔다
```

---
## 1-2. 사전 v1.16 `S15P21E203-334` — 침대·침구 오탐. 병합됨

`77번이 확인한 사전 오탐 다섯 중 `침대` 가 v1.15 이후에도 남아 있었다.` 를 고쳤다.
측정 기록 **31번** · changelog `analysis_outputs/84_lexicon_v1_16_changelog.md`.

```
커밋   f106782  fix: 사전 v1.16 으로 침대·침구 오탐 차단   MR !195 · develop 13639da
만든 것 EDA/84_lexicon_v1_16_bedding_condition.py   (80번의 apply_condition 재사용)
       EDA/84_protected_words_v1_16.py             (83번의 v1.16 판)
       사전 72행 -> 74행
```

### 왜 남아 있었나

**누락이라기보다 분류가 갈린 것이다.** 78번이 C2 로 `침대`·`침구`·`이불` 을 함께
제안했는데 80번(v1.15)이 `이불` 만 가져갔고 `MISFIRE` 목록에 `침대` 가 없어 시험조차
하지 않았다. 기록 27 은 `침대`·`침구` 를 *"행 조건에 딸려 죽던 향 표현 22개"* 안에 넣어
**살릴 것** 으로 봤다. 같은 행의 `이불` 은 **막을 것** 으로 보고 쪼갰다.

### 판정 (v1.15 와 같은 기준)

```
막아야 하는 것  5/5     침대에서 뒹굴기만 했다 · 침대보 · 침구 정리 · 받침대 · 침대칸
살려야 하는 것  5/5     침대 냄새 같은 향수 · 침대에 누운 것 같은 포근한 향 …
평가셋 손실    0건      설문 143 · 합성 600
NDCG@5 차이   +0.000000
```

### ⚠ 다음 사람이 알아야 할 것

```
lexiconVersion 가 바뀌었다   응답과 nlr_recommendations.lexicon_version 이 v1_15 -> v1_16
                          이 됐고, **같은 날 335 로 v1_17 이 됐다.** 집계하는 쪽은 값이
                          셋으로 갈린다는 걸 알아야 한다 — 7장의 "모아서 올린다" 참고

보호 목록도 다시 만들었다     N18 한계 6 의 절차다. **내용은 v1.15 와 같았다**(sha256 일치) —
                          표면형 집합이 안 바뀌었기 때문이다. 다음에 표면형이 바뀌면 달라진다

남은 구멍 하나            kr.scene.bedding 의 **musky 행**에는 `침대`·`침구` 가 조건 없이
                          남는다. 그 행이 optional 이라 80번의 쪼개기가 안 건드렸고 엔진이
                          optional 을 버리므로 지금은 안 샌다. **기록 22(optional 살리기)를
                          고치면** `침대에서 뒹굴기만 했다` 가 optional=['musky'] 를 낸다.
                          core 가 비어 향수는 안 나가지만 다른 조건이 함께 잡힌 문장에서는
                          점수에 섞인다. **둘을 같이 볼 때 이 행도 쪼개야 한다**

한 낱말 `침대` 입력이 죽는다  `이불`·`꿀`·`잼`·`빨래` 는 v1.15부터 이미 그렇고
                          한 낱말 입력은 설문 143건에 0건이다
```

---

## 1-3. 미뤄 둔 것 셋을 쟀다 — 측정 기록 31번

부록과 기록 22·23 이 **"비용을 재지 않았다"** 고 명시한 채 미뤄 둔 것들이다.
**셋 다 지금은 이득이 없다.** 부록의 해당 네 행을 갱신했다.

```
optional 을 살리면   NDCG -0.000236 · 95% 구간 [-0.00226, +0.00180] 로 0 을 포함하고
                  **내린 쿼리가 더 많다**(오름 6 · 내림 9). 기록 22 의 추정이 맞았다.
                  다만 optional 이 생기는 문장이 합성 4.7% · **설문 18.1%** 라
                  평가셋이 이 변경을 잴 자료가 아니다. **설문 라벨이 생기면 다시 연다**

회피 커버리지       사전·브리지를 태우면 33.8% -> 41.5% 로 오르지만 **avoid 가 하드 제외**라
                  올릴수록 위험하다 — `sweet` 하나가 카탈로그의 46.9%, `woody` 가 63.0% 를
                  지운다. **먼저 정할 것은 커버리지가 아니라 강도(하드 제외 vs 감점)다.**
                  반대로 N12 가 걱정한 `머스크` 는 +1.0%p 뿐이었다(`soapy` 가 1.3%)

캐시 키 원문화      잃는 것(표현 단위 재사용)이 **설문 2.6% · 합성 10.1%** 로 작다.
                  이득(같은 원문 재요청 빈도)은 여전히 로그가 있어야 한다
```

**부수적으로 오탐 노출면을 실사용 데이터 없이 재는 방법이 생겼다.** 국어 낱말 23만 개로
표면형이 품은 실재 낱말을 센다. 이 측정으로 위의 `침대`·`침구` 를 찾았다.
**노출면이 크다고 오탐은 아니다** — `나무`(624개)는 대부분 다른 나무라 `woody` 가 맞고,
v1.15 가 `나무` 를 뺀 판단이 옳았다는 것이 이 측정으로 확인된다.

---
## 1-4. 사전 v1.17 `S15P21E203-335` — 껍질·시럽 오탐. 병합됨

측정 기록 **31번**이 국어 낱말 23만 개로 오탐 노출면을 재면서 찾은 둘이다.
changelog `analysis_outputs/85_lexicon_v1_17_changelog.md`.

```
상태   병합됨 · MR !196 · develop ad0792c
커밋   e970b8e  fix: 사전 v1.17 로 껍질·시럽 오탐 차단
만든 것 EDA/85_lexicon_v1_17_peel_syrup_condition.py   (80번의 apply_condition 재사용)
      EDA/85_protected_words_v1_17.py
      사전 74행 -> 76행
```

```
조개껍질 주웠어 · 달걀 껍질 까느라 힘들었어 · 눈껍질이 무겁다   ->  citrus -> 향수 5개
귀찬시럽다 · 거치장시럽다                                ->  sweet  -> 향수 5개
```

**`시럽` 쪽은 방언 어미 `-시럽다`(=`-스럽다`)다.** 국어 낱말 목록에 49개가 있고
`껍질` 은 11개다. 둘 다 향 표현과 한 행에 있어 v1.15 방식대로 `.short` 로 쪼갰다 —
`상큼한`·`새콤`·`과즙`·`톡 쏘는`·`당밀` 은 조건 없이 그대로 산다.

### 판정 (v1.15·v1.16 과 같은 기준)

```
막아야 하는 것  5/5     ·  살려야 하는 것  6/6
평가셋 손실    0건      설문 143 · 합성 600
NDCG@5 차이   +0.000000
```

팀 저장소 코드로 15건을 확인했고 **v1.15·v1.16 의 수정도 그대로 살아 있다.**
검문소 회귀 경로 다섯도 통과했다.

### ⚠ 남은 것

```
과즙                80번이 NOT_BLOCKED 에 남긴 `과즙기` 오탐이 그대로다.
                   행을 쪼개면 막을 수 있지만 한 번에 하나씩 바꾸려고 뒀다
나무껍질이 벗겨졌다     `껍질` 은 막혔지만 `나무` 가 woody 를 낸다. `나무` 는 품은 낱말
                   624개가 거의 다 다른 나무라 대상이 아니다(31번)
보호 목록            절차대로 다시 만들었고 v1.16 과 **같은 파일**이었다(sha256 일치)
```

---

## 1-5. 오탐 검증을 156문장으로 넓혔다 — 전 세션의 ③. **저장소 변경 없음**

측정 기록 **32번** · `DECISIONS.md` **N19**. 팀 저장소를 건드리지 않았다. 사전도 그대로다.

```
남긴 것  EDA/analysis_outputs/86_misfire_sentences.csv             156행
        EDA/analysis_outputs/86_misfire_word_classification.csv    303행
비용     GMS ①단계 306회 (52문장 × 3회 + 50문장 × 3회)
```

### 어떻게 만들었나

**낱말은 기계가 고르고 문장만 사람이 썼다.** 작성자가 막힐 문장만 골라 쓰면 차단률이
부푼다 — v1.15 때 처음 고른 10개 중 4개가 근거가 없었던 전례가 있다(27번).

```
국어 낱말 23만 개에서 조건 걸린 표면형 29개가 품은 낱말을 뽑았다   297개
  misfire  향 무관 낱말에 박혔다        47개
  same     표면형과 뜻이 같다            61개
  rare     방언·고어·전문어·고유명사      189개 (63.6%)
```

문장은 **두 축 여섯 칸**으로 나눴다. 축은 *낱말 안에 박혔나* 와 *조건 낱말이 있나* 다.

```
                         조건 낱말 없음              조건 낱말 있음
낱말 안에 박힘        A1  조개껍질 주웠어         A2  잔디밭 냄새 나는 데서 놀았어
제대로 쓰임·향 무관    B1  방을 깨끗한 상태로       B2  깨끗한 침구 사고 싶은데
향 요청              C1  이불                  C2  이불 향 나는 향수
```

표면형 그룹 9개 × 6칸 × 3문장 = 156. **180 이 아니다** — `깨끗한` 계열 16개는 품은
낱말이 0개라 A 칸을 만들 수 없어 네 칸뿐이다. 살려야 하는 칸(C2)은 지어내지 않고
설문 143 · 합성 600 에서 가져왔다(`산림`·`분내`·`시럽` 만 0건이라 손으로 썼다).

### 결과

```
사전 조건이 막아야 하는 칸(A1·B1)     50/51  98.0%   Wilson [89.7%, 99.7%]   종전 11/11 [74.1%,100%]
사전 조건이 못 막는 칸(A2·B2)         51/51 100.0%   Wilson [93.0%, 100%]
그 문장이 파이프라인에서도 나가는가     52/52 100.0%   ①단계·⑤단계가 하나도 안 막았다
**사전이 막은 것을 ⑤단계가 되살림**   19/50  38.0%   Wilson [25.9%, 51.8%]
살려야 하는 칸(C2)                  27/27 100.0%
오탐 문장 102개 중 끝까지 막힌 것      31/102 30.4%   Wilson [22.3%, 39.9%]
```

**목표였던 구간 좁히기는 됐다.** 그리고 **안 보이던 것이 나왔다 — 0장 ①** 이다.

### ⚠ 다음 사람이 알아야 할 것

```
판정 기준이 바뀐다      v1.18 부터는 사전 경로만이 아니라 **사전이 막은 문장을
                    파이프라인으로도 돌린다**. GMS 비용 때문에 막은 것만 돌린다 (N19)

조건 근거가 약한 넷     `이불`·`잔디`·`침구`·`분내` 는 misfire 낱말이 **0개**다.
                    품은 낱말이 전부 진짜 이불·진짜 잔디다. **빼자는 말이 아니라
                    "낱말에 박히는 오탐" 근거가 없다는 뜻이다** — B 칸 근거는 남아 있다

시럽 조건의 근거      misfire 10개가 전부 방언 어미 `-시럽다`(=`-스럽다`)다.
                    v1.17 이 막으려던 `귀찬시럽`·`거치장시럽` 이 그것이다

조건 없는 표면형이 샌다  `바닷가에서 조개껍질 주웠어` 는 `껍질` 이 막혀도 `바닷가`
                    (표현 `휴양지`·조건 없음)가 coconut·tropical 을 낸다.
                    **문장을 고치지 않고 남겼다** — 고치면 이 경로가 안 보인다

오타 검문소는 0 이었다  150회 전부 `그대로`. 전부 국어 낱말이라 보호 목록이 지킨다.
                    N18 한계 1 과 같은 맞바꿈이고 이 문제에 기여가 없다

**77번이 안 돈다**    326 이 `cached_estimator()` 의 `path` 인자를 없앴는데
                    `77_abnormal_input.py` 가 두 곳에서 쓴다 (`TypeError`).
                    개인 저장소라 팀엔 영향 없다. **요청 범위 밖이라 안 고쳤다**
```

---

## 1-6. 형태소 분석기를 51문장으로 다시 쟀다 — **기각. 실질 이득이 4문장이다**

**측정 기록 28번의 추기**에 전부 있다. 저장소 변경 없음 · GMS 0회.

28번의 기각 근거가 *"얻는 것이 긴 문장 오탐 **2건**"* 이었다. 1-5장이 같은 유형을
51문장으로 만들어 그 근거가 무너졌다.

```
변형                      A2·B2 막음   C2 살아남음   평가셋 손실
문맥 조건 (v1.17) — 현행       0/51      27/27         0
경계 판정 + 문맥 [mecab]      12/51      27/27         4
경계 판정 + 문맥 [pecab]      13/51      27/27         3
경계 판정 + 문맥 [kiwi]       19/51      24/27         3
```

### 알게 된 것 셋

```
손실은 `초록` 하나다     13개 표면형 중 12개가 손실 0 이다. 손실 문장이 전부 `초록빛`
                    이다. 28번이 "합성어에서 3~4건" 이라 적은 것이 **낱말 하나**였다.
                    `초록` 을 빼면 pecab 은 12/51 · 손실 0 · C2 27/27 이 된다.
                    **다만 결과를 본 뒤의 사후 조정이라 그대로 채택할 수 없다**

kiwi 의 19 는 부풀었다  kiwi 만 C2 3개를 죽이는데 전부 `분내` 다. kiwi 형태소 사전에
                    `분내` 가 없어 **표면형이 통째로 죽는다.** A2·B2 에서 `분내` 4개를
                    막은 것도 정당한 차단이 아니다

39개는 못 닿는다       못 막는 것의 대부분이 B2 칸이다 — 표면형이 **제대로 쓰인** 향
                    무관 문장이다. `이불에서 쉰 냄새` 의 `이불` 은 진짜 이불이라
                    독립 형태소가 맞다. **경계 판정이 원리적으로 닿을 수 없다**
```

### 그래서 막은 13개를 파이프라인으로 돌렸다 — 여기서 결론이 났다

경계 판정이 막는 13문장(mecab 12 + pecab 만 잡는 1)을 전체 파이프라인 3회로 돌렸다
[pecab · GMS ①단계 39회].

```
끝까지 막힘      4/13  30.8%   Wilson 95% [12.7%, 57.6%]
⑤단계가 되살림   9/13  69.2%   Wilson 95% [42.4%, 87.3%]
비교 — 1-5장의 사전 조건 되살림  38.0%  [25.9%, 51.8%]

사전 경로에서 막는다     12~13 / 51   (23.5~25.5%)
끝까지 막는다            4 / 51       ( 7.8%)   <- 실질 이득
```

**의존성 +1 · 메모리 +10MB 를 주고 4문장을 산다.** 28번 본문의 *"같은 값을 더 비싸게
살 이유가 없다"* 가 그대로 유효하다.

### 막으면 더 엉뚱해지기도 한다 — 이것이 결정적이다

```
꿀꿀한 기분일 때 쓸 향수    사전 sweet        ->  animalic·bitter        1위 Leather & Artemisia
세탁실에서 곰팡이 냄새가 나  사전 fresh·soapy  ->  animalic·earthy·mossy  1위 Panache
초록물고기 분위기 같은 향수  사전 green        ->  aquatic·green·marine   1위 Arashiyama
```

조건이 0개가 되면 ⑤단계가 문장 전체를 보고 상상한다. `물고기` 를 바다로 읽었다.
흔들림도 생긴다 — 13개 중 4개가 회차마다 갈린다(1/3 · 2/3). 1-5장의 52개는 전부
3/3 으로 고정이었다. **문맥 조건은 최소한 결과를 더 나쁘게 만들지는 않는다.**

### 다시 볼 조건 — ⑤단계가 먼저다

이 기각은 *"경계 판정이 쓸모없다"* 가 아니라 *"⑤단계가 되살리는 한 사전 경로에서
막는 것은 이득이 적다"* 이다. **⑤단계 되살림이 풀리면 12~13개가 그대로 실질 이득이
되므로 그때 다시 연다.** 0장 ① 과 같은 조건이다.

### ⚠ 다음 사람이 알아야 할 것

```
82번을 고치지 않았다   스크래치패드에서 함수만 불러 썼다. 재현 방법은 28번 추기에 있다 —
                   사전 v1.17 · 대상 13개 · A2·B2 51문장 · MUST_KEEP 35 + C2 27

깨끗한 계열은 대상 밖   활용형이라 형태소 목록에 그 형태가 없어 **무조건 차단**된다.
                   넣어 보니 평가셋 손실이 **35건**이 됐다
                   (`시트러스 향처럼 시원하고 깔끔한 향` 이 죽는다)

NDCG 를 안 쟀다      82번 판정 기준의 셋째 항목인데 **기각으로 정해져 더 잴 이유가 없다**

파이프라인은 갈아끼워 잰다  팀 엔진에 경계 판정이 없다. `nlr_engine._lexicon_lookup` 을
                   실행 중에만 경계 판정 버전으로 바꿨다. **팀 파일은 안 건드렸다**
```

---

## 1-7. 추천 이유에 안 쓴 낱말이 뜨던 문제 `S15P21E203-343` — 병합됨

**배포된 서비스에서 팀원이 발견했다.** `숲속의 비 맞은 진흙` 을 넣으니 추천 이유에
`'나무' → woody` 가 떴다. 사용자는 `나무` 를 쓰지 않았다.

```
상태   병합됨 · MR !202 · develop 3efb547
커밋   7454247  fix: 추천 이유에 사전 항목 이름 대신 입력 표면형을 낸다
파일   ai/nlr_engine.py · ai/docs/nlr_api_contract.md
```

### 원인 — 계약서와 구현이 어긋나 있었다

`숲` 은 사전 항목 **`나무`** 의 별칭 12개 중 하나다. `_lexicon_lookup` 이 표면형으로
찾은 뒤 `evidence` 에는 **항목 대표 이름**을 적었다.

```
ai/docs/nlr_api_contract.md              matched[].from 은 "사용자가 쓴 표현"
backend RecommendationReasonResponse     @param phrase 사용자가 쓴 표현입니다
frontend api.ts                          { phrase: '숲속', evidence: '주니퍼' }
```

**백엔드도 프론트도 계약서대로 만들었다. AI 쪽 구현만 달랐다.**

```
사전의 표면형-표현 쌍 279개 중 불일치 229개 (82%)
평가셋 743문장에서 실제로 어긋난 매칭 599회 · 57가지
  97회 상큼->상큼한 · 94회 달콤한->달달 · 44회 숲->나무 · 27회 살냄새->로션 같은
```

**계약서와 백엔드 fixture 의 예시가 `빨래` 인데 하필 표면형과 항목 이름이 같아서**
지금까지 어긋남이 안 보였다. 14번이 *"오탐이 0인 것은 운이다"* 라고 적은 것과 같은 구조다.

### 고친 것

```
_lexicon_lookup   from 에 실제로 걸린 표면형을 넣는다
                  한 항목에 여럿 걸리면 **가장 긴 것** · 같으면 원문에 먼저 나온 것
_unmatched        대표만 보던 것을 matched_forms 까지 보게 한다
계약서            from 설명에 "항목 이름이 아니라 표면형" 과 다중 표면형 규칙을 명시
```

### 검증 (GMS 0회)

| 평가셋 | core | avoid | 화면 문구 | 미매칭 추가 | 미매칭 제거 |
|---|---:|---:|---:|---:|---:|
| 설문 143 | 0건 | 0건 | 46건 (32%) | 0 | 12 |
| 합성 600 | 0건 | 0건 | 302건 (50%) | 0 | 249 |

백엔드 전체 **790개 통과 · 실패 0**. fixture 가 `빨래` 라 영향이 없다.

### ⚠ 다음 사람이 알아야 할 것

```
미매칭 로그 기준이 바뀌었다  합성 600에서 **249건이 줄어든다.** 지금까지
                       nlr_unmatched_expressions 에 "사전에 없는 표현" 으로
                       기록되던 것 중 상당수가 실은 사전이 잡은 것이었다.
                       **사전 확장 근거가 그만큼 오염돼 있었다.**
                       320 배포가 이 병합 뒤라면 처음부터 한 기준이다

matched_forms 는 안 나간다  evidence 에 추가했지만 server.py 의 _camel_evidence 가
                       계약에 있는 칸만 고른다. 응답 형식은 그대로다

중간에 버그를 하나 만들었다  처음 구현에서 미매칭이 6건 새로 생겼다. 대표 표면형은
                       haystack 전체에서 고르는데 _unmatched 는 표현 단위로
                       검사해서다. **화면용 대표와 판정용 목록을 분리**해 고쳤다

계약서 예시는 그대로 뒀다   "from": "빨래" 가 표면형과 항목 이름이 같은 경우라 여전히
                       어긋남이 안 보인다. 바꾸면 백엔드 fixture 도 함께 봐야 해서
                       범위에 넣지 않았다. **MR 리뷰 요청 사항에 적었다**
```

---

## 1-8. 노트 이름으로 검색해도 그 노트가 없다 `S15P21E203-346` — 병합됨

**배포된 서비스에서 팀원이 발견했다.** `미모사` 로 검색하면 미모사가 없는 향수가 나왔다.
측정 기록 **34번 + 추기 둘** 에 전부 있다.

```
상태   병합됨 · develop 598e1eb
커밋   230a6f1  feat: 한국어 노트 이름을 accord 로 해석한다
파일   ai/nlr_engine.py · ai/data/note_alias_ko.csv (신규 196행 · 4KB)
```

### 고친 것

```
_add_note_alias_ko()   별칭을 브리지 dict 에 얹고 얹은 키를 돌려준다
                       **note_accord_bridge.csv 는 안 건드린다** — 그 파일은 코퍼스
                       통계(p·lift)이고 별칭은 번역이라 섞으면 값을 지어내야 한다
load_index()           index["note_alias_ko"] 에 남긴다 (긴 순 정렬)
_longest_alias_ko()    문장에 박힌 별칭. **겹치면 가장 긴 것** (343 과 같은 규칙)
understand()           additional_requirements 표현마다 위를 태운다
```

```
미모사
  전   core=[]                              ⑤단계가 "샴페인 칵테일" 로 해석
  후   core=floral · powdery · yellow floral
       Petal Poetry · Walking On Via Fiori Oscuri · Sortilege Elixir
       **셋 다 mimosa 노트를 가진다**
```

### 설계 판단 둘

**겹치면 가장 긴 별칭을 쓴다.** 별칭 196개 중 서로 박히는 쌍이 31개이고 그중 뜻이
틀어지는 것이 7개다 — `로즈마리`·`로즈메리`·`튜베로즈`→rose · `파인애플`→apple ·
`오렌지꽃`·`오렌지 블라썸`→orange · `산딸기`→strawberry.

**원문(`text`)은 안 훑고 ①단계가 만든 표현만 본다.** 원문을 훑는 쪽이 NDCG 는
+0.005 높았지만 `머스크는 싫어 그리고 장미향도 싫어` 에 musky·rose 를 조건으로 넣는다.

```
                  avoid 와 부딪히는 문장     바뀐 쿼리
①단계 표현만 (채택)   설문 2 · 합성 0         155
원문 전체           설문 10 · 합성 2        231
```

### 효과 (GMS 0회)

```
NDCG@5   0.496896 -> 0.522199   **+0.025303**   95% [+0.014537, +0.036367]
결과 0건   67 -> 55 · 바뀐 쿼리 155 (오름 99 · 내림 56)
노트 일치  합성 600 (363문장)  2.97 -> 3.25 /5   결과 0건 12 -> 0
         설문 143 (58문장)   2.54 -> 2.55 /5   결과 0건  2 ->  2
```

사전 조건 5/5 · 살려야 하는 것 3/3 · 86번 향 무관 문장 102개 0개 달라짐 ·
343 `evidence.from` 그대로 · 응답 계약 그대로 · `core ∩ avoid` 0/743.

### ⚠ 남긴 한계 넷 — MR 리뷰 요청 사항에 적었다

```
설문에서 거의 안 움직인다   걸린 별칭이 28회뿐이고 그중 `우디`·`머스크`·`시트러스`
                      16회는 accord 이름이라 이미 잡히던 것이다.
                      **+0.0253 은 합성 600 값이고 실사용 근거가 아니다**

미매칭 로그를 안 건드렸다   `_unmatched()` 는 사전이 잡은 표면형만 보고 브리지는 안
                      본다. 브리지가 답을 낸 표현이 미매칭으로 **기록된다**
                      (설문 2.8% · 합성 12.3%). 고치면 320 표 집계와 사전 확장
                      근거가 또 바뀐다 — 343 이 249건을 줄인 것과 같은 종류다.
                      **코드에 주석으로 남겼다.** 위 NDCG 는 ⑤단계를 끈 값이다

                      ⚠ **MR 리뷰 요청에 "⑤단계를 또 부른다" 고 적었는데 틀렸다.**
                      ⑤단계는 core 가 비었을 때만 돈다 (`nlr_engine.py:943`).
                      **346 은 호출을 줄인다** — 합성 600 에서 237 -> 193 (-18.6%).
                      낭비는 설문 0 · 합성 0 이다. 34번 추기의 정정 참고.
                      **남는 것은 로그 정확도뿐이고 고치려면 응답 계약이 바뀐다**
                      (`unmatch_reason` 에 네 번째 값)

별칭 196개의 근거가 약하다  브리지 755종을 카탈로그 **보유 수**로 줄 세운 상위 150종
                      기반이다. 입력 빈도가 아니다. `미모사` 가 103위라 100->150

`장미향이 없는 향수`       ①단계가 rose 를 avoid 에 넣어 rose 는 빠지지만 브리지가
                      함께 부르는 `floral` 은 남는다. 설문 2건이다
```

```
증상   "미모사" -> 추천 이유 '미모사' -> citrus · fresh · Champagne
      추천된 향수의 노트는 Pear / Jasmine / Amber · Musk · Sandalwood. 미모사가 없다
```

### 경로

```
사전          '미모사' 없음
노트 브리지     _bridge_lookup("미모사") -> ()   **표에 한글이 0행이다**
⑤단계        "미모사는 샴페인 칵테일과 연상되며…" -> Champagne · floral
검색          accord 로만 거른다. **인덱스에 노트가 없다**
```

### 왜 accord 만 쓰나 — 실험 기록을 거슬러 올라갔다

**노트가 검색에 나쁘다는 측정은 없다.** 직접 계기는 LLM 서식 오류였다.

```
노트북 22(v1)  accord 92 + canonical note 178 = 270 을 target 으로 썼다
              LLM 이 target_type 칸에 선택지 문자열을 그대로 써서 **12건 중 7건 검증 실패**
노트북 23(v2)  선택지를 accord 하나로 줄이자 **12/12 유효**
N2            (3) "canonical note 를 다시 넣는다" 를 기각. 근거는 **accord 조합으로
              검색이 된다**는 계산이고 노트가 나빠서가 아니다
```

**N2 가 Trade-off 로 적어 둔 *"note 를 쓰면 더 정확할 수 있지만 검증하지 않았다"* 와
한계 4 *"note 조건을 계산에 넣지 않았다"* 가 6개월 뒤 실사용 증상으로 나왔다.**

### 잰 것

```
노트 결측       accord 있는 129,161개 중 **93,109개(72.1%)** 가 notes_flat 이 비었다
브리지 한글      note_accord_bridge.csv 2,925행 · 755종 · **한글 0행**
              대조 목록 83개 중 **영어 이름은 98%가 표에 있다**
한국어 노트 빈도  설문 143의 **30%** · 합성 600의 **47%**
⑤단계         74번 체크포인트의 노트 표현 11개는 **11/11 브리지와 겹친다.**
              흔한 노트는 잘 답한다. `미모사` 같은 한 낱말이 문제로 보이는데 안 쟀다
파일 크기       notes_flat 을 실으면 4.98MB -> 5.64MB (+13%). **이쪽은 문제가 아니다**
```

### ⚠ 다음 사람이 알아야 할 것

```
A 는 N2 를 안 뒤집는다   N2 가 기각한 것은 "노트를 검색 대상으로 넣는 것"(3) 이다.
                    한국어 노트 이름을 accord 로 바꾸는 것은 N14 가 이미 연 길이고
                    영어만 열려 있다. **판단한 적 없는 선택지다**

B 는 N2 (3) 이다      되살리려면 결측 72.1% 를 정면으로 다뤄야 한다.
                    노트 필터를 하드 조건으로 걸면 카탈로그의 72% 가 사라진다

accord 경계가 흐리다   accord 92개 안에 `Champagne` · `Pear` 같은 원료성 이름이 있다.
                    22번 사전등록 파일이 둘 다 target_type: ACCORD 로 적었다

**내 집계가 한 번 틀렸다**  한국어 노트 목록에 `차`(Tea)·`배`(Pear) 한 글자를 넣어
                    `차갑고`·`차분한`·`담배잎` 에 걸렸다. 343 에서 고친 것과 같은
                    부분 문자열 함정이다. 한 글자를 빼고 다시 쟀다(33%->30% · 54%->47%)
```

---

## 1-9. 노트 보유를 정렬에 넣자는 안 — **기각. 저장소 변경 없음**

측정 기록 **34번 추기 셋째** · `DECISIONS.md` **N9 추기**. 조사와 측정만 했다 · GMS 0회.

> **⚠ Jira 번호를 찾지 말 것.** 이 안은 `S15P21E203-347` 이었는데 작업된 적이 없어
> 2026-09-20 에 **그 번호를 성별·계절·낮밤 작업으로 다시 썼다**(1-10장 · 0장 ①).
> 노트 정렬을 다시 열 때는 **새 번호를 딴다.**

### 시작하려다 두 가지를 먼저 확인하고 접었다

**① 배포 데이터에 노트가 없다.** `build_data.py` 의 `KEEP_COLUMNS` 가 여섯 칸뿐이고
배포 파일 `perfumes_nlr.csv.gz` 를 열어 확인했다. **B2 를 잰 실험은 `perfumes.csv`
(EDA 원본)에서 노트를 읽었다 — 엔진은 그걸 못 본다.**

```
① 배포 데이터   build_data.py + perfumes_nlr.csv.gz 재생성   4.97 -> 6.87MB (**+38%**)
② 엔진 적재     load_index() 가 노트 보유표를 만든다
③ 정렬         _rank() 에 노트 보유를 넣는다                  **N9 를 바꾼다**
```

**⚠ 34번 본문의 "+13% 라 문제가 아니다" 는 틀린 근거였다.** 그건 `notes_flat` 만 실은
값이고 그 칸은 결측이 **72.5%** 다. 노트는 네 칸에 갈려 서로 채운다(합치면 보유 98.1%).
실제 비용은 이름 그대로 +56% · 번호로 바꾸면 +38% 다.

**② 정렬 자리 둘을 다 쟀다.** 엔진의 `_rank` 와 같은 식으로 다시 정렬했고,
**근사가 엔진과 합성 600 에서 600/600 일치**하는 것을 먼저 확인했다.

```
■ 노트 일치 (5개 중)      설문 58문장        합성 363문장
지금                    2.50   36/58      3.25   317/363
최상위 키                 4.82   56/58      4.90   362/363
동점 깨기                 3.34   46/58      3.47   323/363

■ NDCG@5 (합성 600 · **346 병합 상태가 기준**)
지금        0.522199        —
최상위 키     0.515815   **-0.006384**   95% [-0.012165, -0.000914]   바뀐 쿼리 120
동점 깨기     0.521075     -0.001124     95% [-0.003863, +0.001429]   바뀐 쿼리  26
```

**최상위 키는 NDCG 를 유의하게 내린다** — 구간이 0 을 안 포함한다.

### 34번 첫 추기의 B2 수치와 다르다 — 기준이 바뀌었다

```
첫 추기   B2 단독  +0.0036 (0 포함)     346 이 없던 때 · 현행 대비
이번     최상위 키  -0.0064 (유의)       **346 위에 얹은 것**
```

346 이 이미 노트 이름을 accord 로 바꿔 core 에 넣으므로 B2 가 더 얹을 것이 없고 순위만
흔든다. `A' +0.0253` 대 `A'+B2 +0.0187` 의 차이 **-0.0066** 과 같은 값이라 일관적이다.

### 기각 이유

```
얻는 것   노트 일치 3.25 -> 4.90 · 1개 이상 317/363 -> 362/363
주는 것   NDCG -0.0064 (유의) · N9 변경 · 배포 데이터 +38% · build_data.py 변경
```

**팀원이 보고한 미모사 증상은 346 이 이미 고쳤다.** B2 가 더 사는 것은 *"노트를 말한
모든 문장에서 5개 중 4.9개까지 맞춘다"* 이고, 그 값으로 배포 산출물과 정렬 규칙을
동시에 바꿀 근거가 없다. **동점 깨기도 안 한다** — NDCG 는 안 내리지만 얻는 것이
3.25 -> 3.47 로 작은데 배포 데이터 비용은 똑같이 든다.

### ⚠ 이 기각의 약점 — 두 지표가 서로 다른 것을 잰다

**NDCG 는 노트를 못 본다.** 정답 조건 `C` 가 accord 라서 구조적으로 안 잡힌다.
`-0.0064` 는 *"accord 조건에서 멀어진 정도"* 이고 노트를 준 이득은 그 숫자에 없다.
둘을 합칠 수 없어 **결국 사람이 골랐다.**

### 다시 볼 조건

```
실사용 로그    320 배포로 노트 이름 입력 빈도가 보이면. 설문 143에서 별칭이 걸린 것은
             **28회뿐**이었고 그중 16회가 accord 이름이었다
노트 라벨      평가셋 정답 조건에 노트가 들어가면 NDCG 가 노트를 볼 수 있다 (4장 ②)
화면 문구      "미모사가 든 향수" 를 못 준다는 것을 화면에서 어떻게 말할지 정해지면
             (계약서 414·426행)
```

---

## 1-10. 평가자를 넓혔다 — **확장 NDCG.** 347 기각이 뒤집혔다. **저장소 변경 없음**

측정 기록 **35번** · `DECISIONS.md` **N9 정정 추기**. 조사와 측정만 했다 · GMS 0회.

### 왜 만들었나

추천 품질을 재는 자가 NDCG@5 하나인데 정답이 **accord 3개**뿐이다. 그래서 347 에서
*"노트 일치는 4.90 까지 오르는데 NDCG 는 -0.0064"* 가 나와 기각했다.
**지표가 노트를 못 봐서 순위 흔들림만 잡은 것이다.**

### 무엇을 만들었나

```
조건 = 정답 향수의 accord 3개
     + 쿼리가 말했고 **정답 향수도 실제로 가진** 노트 · gender · season · daypart
gain = 추천 향수가 충족한 조건 개수     선형 · 전부 같은 무게
NDCG@5 = Σ gain/log2(i+2) ÷ Σ 최대/log2(i+2)
```

**"정답 향수도 실제로 가진" 이 안전장치다.** 쿼리가 `겨울` 이라 했는데 정답 향수의 1등이
autumn 이면 그 조건을 뺀다 — 작성자의 해석이 정답에 안 들어간다.

전제를 먼저 확인했다. 쿼리가 말한 것과 정답 향수 속성의 일치율이다.

```
gender 97.7% · daypart 91.3% · season 80.7% · note 65.4%
```

### 결과

```
변형            확장 NDCG    지금 대비     95% 구간                    accord만
346 이전       0.602460   -0.029587  [-0.040115, -0.019802] 유의   0.612760
지금           0.632047         —                                 0.640157
노트 (=347)    0.640760   +0.008713  [+0.004428, +0.013236] 유의   0.635962
gender        0.635396   +0.003349  [+0.001546, +0.005339] 유의   0.640845
season        0.643378   +0.011331  [+0.008102, +0.014825] 유의   0.642892
daypart       0.635613   +0.003565  [+0.001691, +0.005973] 유의   0.640308
gsd 셋 다      0.649702   +0.017655  [+0.013468, +0.022205] 유의   0.643544
**노트+gsd**    0.660030  **+0.027983** [+0.021660, +0.034660] 유의  0.640146
```

**전부 유의하게 오른다.** gender 정답을 투표로 채점하면 조금 더 크다 (노트+gsd +0.0305).

### 결론 셋

```
1  **347 기각이 뒤집힌다**   같은 코드인데 accord 만으로는 -0.0064, 확장으로는 +0.0087
2  **함께 하면 상쇄된다**    노트 단독은 accord -0.0042 인데 gsd 와 묶으면 ±0.000
3  **346 기여가 제일 크다**  -0.029587 로 셋을 다 더한 것(+0.017655)보다 크다
```

### 주 지표도 함께 쟀다 (추천 5개 중 맞는 개수)

```
                   문장   기준선   지금   하드필터  정렬최상위  동점깨기
설문 143
  gender 엄격       28    1.49   1.87    4.67     4.73     2.87
  season           28    1.65   2.11    4.53     4.68     2.95
  daypart          10    3.15   4.43    5.00     5.00     4.43
```

**`지금` 이 무작위 기준선에 가깝다.** 설문의 gender 관대는 3.27 로 무작위 3.42 보다
**낮다** — 사용자가 남성용을 달라고 해도 엔진이 안 보고 있다.

### 정해진 것 둘 (2026-09-20)

```
gender 는 **투표 점수**   공식 3값은 unisex 32,443개(후보의 47%)를 못 가른다.
                      그 절반이 투표로는 남/여로 기운다. Layton(남성 0.85) ·
                      Lost Cherry(여성 0.68) · CK One(중성 0.68)이 같은 칸에 있다
                      0표는 1.74% 뿐이고 공식 등급 평균으로 메운다

배포에는 **1등 방향만**   gender 1칸 +2.7% · 셋 다 3칸 **+7.0%**.
                      지금 개입이 쓰는 것이 딱 "1등이 무엇인가" 다
```

### ⚠ 다음 사람이 알아야 할 것

```
확장 지표는 자기 편을 든다   노트로 정렬하면 노트 조건을 잘 맞추는 게 당연하다.
                       **오름폭의 절대값은 부풀려져 있다.** 믿을 것은 `accord만` 칸과
                       **조합했을 때 상쇄되는가** 다

설문에서 못 쟀다          합성 600 에만 정답 향수가 있다. **실사용자는 설문 쪽**이고
                       합성은 ai_summary 에서 만들어 노트가 훨씬 많다 (28회 대 305회)

347 은 아직 안 뒤집었다    기각 근거 1이 무너졌을 뿐이다. 근거 3(**배포 데이터 +38%**)은
                       그대로 유효하다. N9 정정 추기 참고

performance 둘은 범위 밖   33번·68번이 이미 봤다. v4 프롬프트 · 정렬 2차 키 · 배포
                       데이터 세 조각이 함께 가야 한다. 그 기록을 먼저 읽는다

계절 이분화가 거칠다        "1등 계절" 로만 봐서 2등이면 틀렸다고 센다 (일치 80.7%).
                       상위 2개로 넓히거나 비율을 gain 으로 쓰면 달라질 수 있다

내 실수 둘               세 칸을 한꺼번에 돌린 첫 측정이 **후보 풀이 아니라 이미 뽑은
                       5개를 걸러** -0.0225 가 나왔다. 그리고 `male + unisex` 로
                       보자던 규칙은 **근거 없이 내가 정한 것**이었고 측정에서도 제일
                       약했다 (설문 2.67/5)
```

---

## 1-11. 성별·계절·낮밤을 검색에 넣었다 `S15P21E203-347` — **푸시됨 · 리뷰 전**

측정 기록 **35번**(조사·측정)과 그 안의 **구현 절**. 근거와 한계는 거기 있고, 여기는
다음 사람이 이어받을 것만 적는다.

```
브랜치  feature/ai/S15P21E203-347-context-conditions   원격에 있다
커밋    28be934  feat: 배포 데이터에 성별·계절·낮밤 1등 방향 추가
       6128aec  feat: ①단계 성별·계절·낮밤 조건으로 후보를 좁힌다
MR     !207 · 리뷰 둘 반영 후 재푸시
       b44e1fa  fix: 성별 방향을 이름으로 묶고 동점을 둘 다 싣는다
       f9925c7  fix: context 조건이 스키마를 어긴 입력에 죽거나 침묵하지 않는다
```

⚠ **Jira 347 은 원래 노트 정렬 이슈였다.** 작업된 적이 없어 제목·설명을 바꿔
재사용했다. 노트 정렬(0장 ②)을 열 때는 **새 번호를 딴다.**

### 무엇을 고쳤나

```
build_data.py         gender_lead · season_lead · daypart_lead 를 계산해 싣는다
                      **투표 수가 아니라 1등 방향만** — +19.7% 대신 +6.0%
perfumes_nlr.csv.gz   4.97 -> 5.27MB. **기존 6칸은 129,161행 값까지 동일**
nlr_engine.py         _load_context() · context_mask() 신설
                      search() 가 structured 를 받아 후보를 좁힌다
                      **_rank 와 정렬 순서는 안 건드렸다** — N9 그대로
```

### 효과

```
확장 NDCG   0.632047 -> 0.650458   **+0.018411** [+0.014092, +0.022960] 유의
accord     0.640157 -> 0.643859   **기존 지표도 함께 오른다**
주 지표     설문 gender 1.80 -> 5.00 · season 2.11 -> 5.00 · daypart 4.43 -> 5.00
결과 0건    13 / 9 / 3  **그대로**
회귀       사전 조건 4/4 · 살릴 것 3/3 · 343 · 346 전부 그대로
```

### ⚠ 다음 사람이 알아야 할 것

```
배포가 두 조각이다        데이터와 코드가 **둘 다** 가야 기능이 켜진다.
                     어느 쪽이 먼저 가도 안 깨진다 — 칸이 없으면 경고만 남긴다.
                     배포 파일이 300KB 커진다

CONTEXT_MIN = 3      내가 정한 값이다. MIN_RESULTS 와 맞췄다.
                     **이 되돌리기 덕에 결과 0건이 안 늘었다.** 리뷰에 물었다

동점은 둘 다 싣는다      `male|unisex` 처럼. 투표 있는 향수의 6.77% · 엔진
                     모집단의 3.83% 가 동점이다. 처음엔 argmax 로 하나만 골랐는데
                     **딕셔너리 순서가 승자를 정하고 있었다** (리뷰에서 발각)

성별은 커뮤니티 투표다    perceived_* 는 사용자 인식이지 제조사 의도가 아니다.
                     두 채점 기준이 서로 반대를 가리켜 측정으로 못 갈랐고
                     **내가 골랐다.** 리뷰에 물었다

완화 5 에서는 풀린다     평가자 문턱 해제 단계가 마스크를 새로 만들어 context 도
                     함께 풀린다. `context_applied` 가 거기서 빈 목록이다.
                     마지막 수단이라 그게 맞다

응답에 안 나간다        search() 반환에 context_applied 가 늘었지만 server.py 가
                     계약에 있는 칸만 고른다. **응답 형식은 그대로다**

346 주석을 함께 고쳤다    "브리지가 낸 accord 와 ⑤단계가 낸 accord 가 둘 다 core 에
                     들어간다" 가 틀렸다. 같은 함수라 커밋을 안 쪼갰다
```

---

## 2. 막혀 있는 것 — 실사용 로그. 320 배포 여부를 확인해야 한다

**320(DB 표 4개)이 병합됐지만 배포됐는지 모른다.** 배포가 안 되면 아무것도 안 쌓이고,
그러면 아래 질문이 전부 막힌 채로 남는다.

```
오탐의 실사용 빈도        사전 조건 9개가 적절한지 · 더 걸지 뺄지 (측정 기록 25·27번)
긴 문장 오탐 빈도         형태소 분석기를 다시 열지 (측정 기록 28번에서 기각한 것)
한 낱말 입력 빈도         설문 143건에 0건인데 검색창은 다를 수 있다
NO_CONDITION 의 실제 분포
**미매칭 로그의 오염률**   브리지가 푼 표현이 "사전에 없는 표현" 으로 남는다
                      (설문 2.8% · 합성 12.3%). 343 이 249건을 줄인 것 위에
                      얹히는 오염이다. 아래 참고
```

### ⚠ 320 이 배포되면 함께 볼 것 — 미매칭 로그의 오염 (2026-09-20 에 미루기로 정했다)

`_unmatched()` 는 사전이 잡은 표면형만 보고 **브리지는 안 본다.** 346 이 브리지로 푼
표현이 `nlr_unmatched_expressions` 에 "사전에 없는 표현" 으로 남는다.

```
동작 영향    **없다.** ⑤단계는 core 가 비었을 때만 돈다 (nlr_engine.py:943).
           346 은 오히려 호출을 줄인다 — 합성 600 에서 237 -> 193
남는 문제    로그를 캐서 사전을 늘리려는 쪽이 **이미 풀린 것을 빈칸으로 본다**
고치는 값    `unmatch_reason` 에 네 번째 값이 필요하다 -> **응답 계약이 바뀐다**
           (지금은 NOT_IN_LEXICON · CONDITION_NOT_MET · NOT_CORE 셋)
```

**미룬 이유** — 동작에 영향이 없고 계약을 바꿔야 한다. 그리고 **이 로그가 실제로 쓰이려면
320 이 배포돼 데이터가 쌓여야 한다.** 배포되면 12.3% 가 실사용에서 얼마인지도 로그로
직접 볼 수 있다. 0장 ①(⑤단계 되살림)과 함께 백엔드와 이야기하면 된다.

**⚠ 병합된 코드의 주석 하나가 틀려 있다.** `understand()` 안 이 자리의 주석이
*"브리지가 낸 accord 와 ⑤단계가 낸 accord 가 둘 다 core 에 들어간다"* 로 적혀 있는데
**⑤단계가 안 도니까 그런 일은 없다** (커밋 `230a6f1`).

**주석만 바뀌고 동작은 안 바뀐다. 다음에 `ai/nlr_engine.py` 를 건드리는 MR 에 얹는다**
(2026-09-20 결정). 바꿀 문구는 이것이다 — `understand()` 의 부분 일치 루프 위다.

```python
        # ⚠ **여기서 푼 표현도 `_unmatched` 에는 그대로 남는다.** 그 함수는 사전이
        # 잡은 표면형만 보고 브리지는 안 본다 (설문 2.8% · 합성 12.3% 의 표현).
        #
        # **동작에는 영향이 없다.** `recommend()` 는 core 가 비었을 때만 ⑤단계를
        # 부르므로(아래 `if estimator and not understood["core"]`) 여기서 core 를
        # 채우면 ⑤단계는 안 돈다 — 오히려 호출이 준다(합성 600 에서 237 -> 193).
        # 남는 것은 `nlr_unmatched_expressions` 의 정확도다. 로그를 캐서 사전을
        # 늘리려는 쪽이 **이미 풀린 것을 빈칸으로 본다.** 고치려면 `unmatch_reason`
        # 에 네 번째 값이 필요해 응답 계약이 바뀌므로 **범위에 넣지 않았다.**
```

### 확인하는 법

```bash
cd C:/Users/SSAFY/Desktop/S15P21E203
docker compose -f compose.manual-test.yaml up -d --wait
docker compose -f compose.manual-test.yaml exec -T postgres \
  psql -U hyanghae -d hyanghae_test -c "SELECT count(*) FROM nlr_recommendations;"
```

로컬은 이렇게 보고 **운영 서버는 인프라 담당이 확인해야 한다.**

---

## 3. 끝난 것 — 다시 하지 말 것

### 병합됨

```
S15P21E203-307   향 사전 v1.15          짧은 표면형 9개에 문맥 조건. 사전 63 -> 72행
S15P21E203-320   DB 표 4개 (백엔드)      검토 완료 · 전체 774건 통과
S15P21E203-325   응답에 칸 6개          develop 609e132
                 최상위      lexiconVersion · promptSha256
                 unmatched  hadConditions · llmAccords · tier · resultCount
                 llmReasoning 은 일부러 뺐다 — 지어낸 설명이 화면으로 샐 길을 막는다
S15P21E203-326   ⑤단계 캐시 수정         develop 08fcd55 — 1장
백엔드 후속        nlr_* 표에 여섯 칸 저장   develop 0dc97d9 — 1장
S15P21E203-330   계약서 result_count 기준  develop 7e97e11 — 1장
S15P21E203-343   추천 이유 표현              develop 3efb547 — 1-7장
S15P21E203-346   한국어 노트 별칭            develop 598e1eb — 1-8장
```

### 잰 것 — 측정 기록 25~32

```
25  향과 무관한 문장 11건이 11건 다 향수를 받았다. 14번이 예언한 오탐이 실제로 있었다
26  ⑤단계가 한 번 잘못 상상한 것이 파일에 굳어 영구히 되풀이된다
27  사전 v1.15 — 조건을 행에 걸었더니 향 표현 22개가 같이 죽어서 행을 쪼갰다
28  형태소 분석기를 경계 판정 용도로 다시 열었다. 긴 문장 오탐을 막지만 합성어에서 잃는다
29  오타 검문소를 세 방식으로 재봤다. 셋 다 빨레를 못 잡는다  <- **결론이 30번에 뒤집혔다**
30  29번이 포착률을 안 재고 접었다. 재보니 93% 라서 만들었다
31  미뤄 둔 것 셋의 "재지 않았다" 를 쟀다. 셋 다 지금은 이득이 없다
32  오탐 검증을 11문장에서 156문장으로 넓혔다. **사전이 막아도 ⑤단계가 38% 를 되살린다**
28  **추기.** 형태소 분석기를 51문장으로 다시 쟀다. 기각 근거는 무너졌지만 **결론은 기각**
    사전 경로에서 12~13개를 막아도 9/13 이 ⑤단계로 되살아난다. 실질 이득 4/51
33  ⑤단계에 문장 맥락을 주면 오탐 -13.0%p · 커버리지 +12.7%p. **되돌릴 거라던 예상이 틀렸다**
    ⚠ 첫 측정이 `load_prompt()` 캐시 때문에 정반대 결과를 냈다. 33번에 경위를 적었다
33  **추기.** 파이프라인 전체로도 같은 방향 (-13.7%p / +8.0%p). GMS 를 1,239 -> 378 로 줄였다
    ⚠ 그 과정에서 **32번을 정정했다** — 되살림 38% 중 2건은 ⑤단계가 아니라 ①단계 경로다
33  **추기.** NDCG 는 v1 대비 **변화 없음** [-0.0071, +0.0067]. ⑤단계를 켜는 것 자체는 +0.0398
    ⚠ 첫 시도가 74번 accords 파싱 오류로 무효였다 (7-1장 함정 3)
33  **추기.** v1 이 빈 목록 내던 142개 중 **48%** 가 답하기 시작. 정체는
    **v2 가 표현이 아니라 원문에 답하는 것**이다 (자카드 0.150 -> 0.473)
34  노트 이름으로 검색해도 그 노트가 없다. **노트를 뺀 계기는 LLM 서식 오류였다**
    브리지 한글 0행 · 한국어 노트 입력 설문 30%·합성 47%
    ⚠ 처음에 "노트 결측 72.1%" 라고 썼다가 정정했다 — **실제로는 99.997% 가 있다**
34  **추기.** 노트를 넣으면 좋아진다. A+B2 로 노트 일치 3.76 -> 4.91 · NDCG +0.0140
34  **추기.** 별칭 오탐을 재고 **A'** 를 구현했다. NDCG +0.025303 · `미모사` 가 고쳐졌다
    ⚠ 부분 문자열 함정은 743문장에 2가지뿐(`로즈마리`·`파인애플`)이고 **최장 일치로 잡힌다**
    ⚠ A'' 는 `머스크는 싫어` 를 조건으로 넣는다 — 그래서 원문이 아니라 ①단계 표현만 본다
    ⚠ **A'/A'' 첫 측정이 `core - avoid` 를 건너뛰었다.** NDCG 는 안 움직였다
34  **추기.** B2(노트 보유 정렬)를 **기각했다.** 346 위에 얹으면 NDCG -0.0064 (유의)
    ⚠ 배포 데이터에 노트가 없다 — 노트를 실으면 4.97 -> 6.87MB (**+38%**)
    ⚠ 34번 본문의 "+13%" 는 `notes_flat` 만 실은 값이고 그 칸은 결측이 72.5% 다
35  원본 59칸 중 6칸만 쓰고 있었다. **①단계가 뽑는 다섯 칸을 엔진이 버린다**
    (설문 기준 season 19.6% · gender 19.6% · longevity 14.7% · intensity 13.3% · daypart 7.0%)
    **확장 NDCG 를 만들었다** — accord 에 노트·성별·계절·낮밤을 더해 한 숫자로 잰다
    ⚠ **347 기각이 뒤집혔다.** 같은 코드가 accord 만으로는 -0.0064, 확장으로는 +0.0087
    ⚠ 넷을 함께 하면 accord 손실이 상쇄된다 (+0.027983 · accord 는 ±0.000)
    ⚠ 정답 향수 순위는 지표로 못 쓴다 — top200 에 9.0% 밖에 안 든다
    ⚠ 내 실수 둘 — 세 칸 첫 측정이 후보 풀 대신 뽑은 5개를 걸렀다 ·
      `male + unisex` 규칙은 근거 없이 내가 정한 것이었다
```

측정 스크립트는 `77_abnormal_input.py` ~ `85_protected_words_v1_17.py` 열한 개다.
29·30·31·32번의 측정 스크립트는 남기지 않았다(41번 이후 관례). 생성 스크립트(83·84·85)만
남겼다. **32번은 예외로 라벨 CSV 둘을 남겼다** — 다음 사전 버전의 회귀 확인에 그대로 쓴다.

### DECISIONS 갱신

```
N13  결론 유지 · **이 결정이 답한 질문의 범위를 좁혀 적었다** (어간 매칭만 기각한 것)
N15  **파일 캐시가 부분 뒤집혔다.** 추정을 켜는 결정 자체는 유지
N16  신설 — 짧은 표면형에 문맥 조건을 건다
N17  신설 후 **N18 로 대체됨.** 본문은 그대로 뒀다 — 무엇을 안 쟀는지가 남아야 한다
N18  신설 — 오타 검문소를 만든다. N17 을 뒤집는다
N19  신설 — **오탐 판정을 파이프라인 끝까지 잰다.** N16 을 뒤집지 않고 평가 방법을 바꾼다
N2   **추기** — 한계 4와 Trade-off 가 실사용 증상으로 나타났다 (34번). 결정은 유지된다
N14  **추기** — 브리지에 한국어 별칭을 얹고 **①단계 표현 안에서** 찾는다 (346).
     A'' (원문 훑기)를 기각한 근거가 여기 있다. 결정은 유지된다
N9   **추기** — 노트 보유를 정렬에 넣자는 안(347)을 **기각했다.** 결정은 유지된다
N9   **추기의 정정** — 같은 날 **기각 근거 1이 무너졌다.** 확장 NDCG 로는 +0.0087 이다.
     **347 을 다시 열어야 한다.** 다만 근거 3(배포 +38%)은 그대로다
```

### 기각한 것 — 되풀이하지 말 것

```
표면형을 사전에서 뺀다      설문 4건 · 합성 14건 손실. 한국어가 교착어라 `숲향`·`숲속에` 를 잃는다
낱말 경계를 본다(조사 목록)  설문 2건 · 합성 9건 손실. `초록불`과 `초록빛` 을 못 가른다
형태소 분석기 경계 판정      긴 문장 오탐 2건을 더 막지만 합성어에서 3~4건 잃는다.
                        의존성 +1 · 메모리 +10MB. **실사용 빈도가 보이면 다시 연다**
```

---

## 4. 안 한 것 — 바로 할 수 있는 것 하나 · 미뤄 둔 것 셋

### ① ⑤단계가 되살리는 38% — 무엇을 고칠지 미정이다

**32번이 찾았고 `DECISIONS.md` N19 가 판정 방법까지만 정했다.** 어디를 고칠지는
정하지 않았다. 후보가 셋이었고 **하나는 기각됐고 하나는 비용을 쟀다.**

```
⑤단계에 문장 맥락 주기    **세 지표로 다 쟀다(33번 + 추기).** 표현 단위
                     -13.0%p/+12.7%p · 파이프라인 -13.7%p/+8.0%p ·
                     **NDCG 변화 없음.** 가장 유망하다. 아래에 적었다

⑤단계 앞에 향 요청 판별   오타 검문소 자리에 하나 더 세운다. 검문소가 그 자리에서
                     150회 전부 아무 일도 안 했으므로 자리는 비어 있다.
                     **판별을 무엇으로 하는지가 통째로 미정이다**

형태소 분석기          **기각됐다** (1-6장 · 28번 추기). 사전 경로에서 12~13개를
                     막아도 9/13 이 ⑤단계로 되살아나 실질 이득이 4/51 이었다
```

### ⑤단계에 문장 맥락 주기 — 비용 (2026-09-19 측정 · GMS 0회)

**지금 ⑤단계가 받는 것은 표현 조각뿐이다.** `estimator(expression)` 이라 "이 문장이
향 요청인가" 를 볼 수 없다. 그래서 `꿀 한 숟갈 타 먹어` 의 `꿀` 에 **맞게** `honey` 를
답하고, 그것이 오탐이 된다.

```
코드 변경 6곳   data/estimator_prompt_v2.txt          새로 만든다 (v1 을 고치지 않는다)
              llm_stage1.estimate_accords()         인자 추가 + %RAW_TEXT% 치환
              llm_stage1.make_estimator()           lambda 시그니처
              nlr_engine._apply_estimates()         시그니처 + 호출 2곳(병렬·순차)
              nlr_engine.recommend()                _apply_estimates 에 text 를 넘긴다
              unmatched_log.cached_estimator()      캐시 키에 원문을 넣는다

              `unmatched` 항목에 원문이 없다 — expression·normalized_key·
              unmatch_reason·entry_id 뿐이라 recommend() 에서 내려보내야 한다

계약          **응답 형식은 안 바뀐다.** 바뀌는 것은 `estimator` callable 시그니처이고
              내부 계약이다 (`nlr_engine.recommend` docstring 777행)

프롬프트 크기   지금 1,333자 — accord 92개가 894자로 **이미 67%** 다
              원문을 붙이면 설문 +4.5%~11.0% · 합성 +4.4%~4.6%. **작다**

캐시 손실      표현 단위 재사용이 깨진다 (31번 수치를 재현했다)
              설문 386표현 중 10회 **2.6%** · 합성 1,939표현 중 195회 **10.1%**
              그만큼 GMS 호출이 는다

테스트        `ai/` 에 테스트가 없다. 백엔드 786개는 이 경로를 안 탄다
```

**회귀 측정 비용이 가장 크고, 준비물이 없다.** ⑤단계 출력이 통째로 바뀌는데
`74_estimator_checkpoint.csv` 에 **원문이 없다**(`expression`·`accords`·`reasoning`·
`latency_sec`·`error`). 표현 369개를 원문과 짝지어 **다시 만들어야 한다.**

### 효과 — **예상이 틀렸다** (측정 기록 33번 · GMS 576회)

*"N15 의 커버리지 76.8% -> 88.8% 를 되돌릴 것"* 이라고 걱정했는데 **반대였다.**

```
                        v1        v2       차이
되살림 (오탐)  회차 기준   63.0%  ->  50.0%   -13.0%p
정상  (정상)  회차 기준   81.3%  ->  94.0%   +12.7%p

미리 정한 기준 — v1 이 accord 를 냈는데 v2 가 3회 다 빈 목록
  되살림  35개 중  9개  25.7%  [14.2%, 42.1%]   <- 개선
  정상   47개 중  1개   2.1%  [ 0.4%, 11.1%]   <- 손실   Wilson 구간이 안 겹친다
```

**정상에서 죽은 1개는 오히려 v1 이 틀렸던 것이다** — `추운 날` 에 `fresh` 를 내던 것을
v2 가 빈 목록으로 바로잡았다. 흔들림도 정상 쪽에서 14/50 -> 5/50 으로 줄었다.

### 파이프라인 전체로도 봤다 (33번 추기 · GMS 378회)

```
집단     n        v1              v2            차이
오탐    17   43/51  84.3%   36/51  70.6%   -13.7%p
정상    25   58/75  77.3%   64/75  85.3%    +8.0%p

  오탐 16문장 중 v2 가 완전히 막음  2개  12.5%  [3.5%, 36.0%]
  정상 20문장 중 v2 가 죽임        0개   0.0%  [0.0%, 16.1%]
```

**정상 쪽에서 죽은 문장이 0개다.** 향수를 못 받던 문장이 5개에서 1개로 줄었다.
표현 단위와 방향이 같다.

**호출을 1,239회에서 378회로 줄였다** — ①단계를 0회 부르고(평가셋 체크포인트 +
`pipeline2` 기록), ⑤단계는 표현 단위로 한 번씩만 부른 뒤 `recommend()` 를 재생했다.
같은 방법을 다음 측정에도 쓸 수 있다.

### NDCG — 추천 품질은 안 바뀐다 (33번 추기 · GMS 237회)

```
변형                      NDCG@5     조건충족률   결과 0건
⑤단계 끔                 0.499743   0.620222      62
⑤단계 v1                0.539589   0.676407       2
⑤단계 v2 (원문 추가)       0.539235   0.676648       1

v1 - 끔                +0.039847
v2 - v1                -0.000354     95% 구간 [-0.0071, +0.0067] — 0 을 포함한다
```

**v2 는 오탐을 줄이고 커버리지를 올리면서 추천 품질은 안 건드린다.**
부수적으로 **N15 의 효과가 순위 지표로 나왔다** — ⑤단계를 켜는 것 자체가 +0.039847
이고 결과 0건이 62 -> 2 다. N15 는 커버리지로만 근거를 댔었다.

**v1 쪽은 호출 0회였다** — `74_estimator_checkpoint.csv` 가 235개 표현을 100% 덮는다.
74번이 1회분이라 v2 도 1회로 맞췄다.

### ⚠ 커버리지가 오른 이유가 생각과 달랐다 (33번 추기 · GMS 142회)

v1 이 빈 목록을 내던 표현 142개에 v2 를 물었더니 **48%(68개)가 답하기 시작했다**
[Wilson 39.8%~56.1%]. 그 정체를 확인했다.

```
같은 원문 안에서 표현끼리 accord 가 얼마나 겹치나 (원문 62개 · 자카드 평균)
  v1  0.150
  v2  0.473      <- 3배가 넘는다
```

```
원문 "겨울 저녁에 쓸 클래식한 남자 향… 진한 술 향을 중심으로 따뜻하고 값비싸 보이는"
  'classic한 남자 향'   v1 []              ->  v2 [whiskey, amber, alcohol]
  '값비싸 보이는 느낌'     v1 []              ->  v2 [amber, whiskey, warm spicy]
  '따뜻한 느낌'          v1 [warm spicy]    ->  v2 [amber, whiskey, warm spicy]
```

**v2 는 표현이 아니라 원문에 답한다.** `값비싸 보이는 느낌` 에 `whiskey` 가 붙은 것은
그 표현의 뜻이 아니라 원문에 술이 있어서다.

**커버리지 +12.7%p · +8.0%p 의 정체가 이것이다** — 표현을 더 잘 이해해서가 아니라
**원문의 accord 를 모든 표현에 복사해서** 오른 것이다. 결과 방향이 맞아 NDCG 가
안 나빠졌지만 **정확도가 아니라 단위가 바뀐 것이다.**

```
안 일어난 것   부정 표현에 정반대 accord 를 주는 일. `가볍지 않게` -> [amber, woody,
             sweet] 처럼 원문 맥락 덕에 방향이 맞았다. 부정을 이해한 것은 아니다
일어난 것     규칙 위반. `다양한 향과 가격대로 3가지 추천` 이 [fresh, aromatic, woody]
             를 받았다. 프롬프트가 "가격은 향이 아닙니다" 로 금지한 부류다
```

### ⚠ 새 질문 — ⑤단계를 표현 단위로 부를 이유가 남았나

원문에 답할 거라면 표현마다 부를 필요가 없다. **한 번만 부르면 호출이 1/3 로 준다.**
다만 `evidence` 의 `from`(어느 표현에서 왔는지)을 못 적어 응답 계약이 바뀐다.
부록에 질문으로 넣었다.

### ⚠ 그래도 바로 채택하지 않는 이유 셋

```
원문이 표현을 오염시킨다   되살림 쪽에서 v2 가 **새로 만드는 것이 6개** 있다.
                     `주말` 에 `foresty` 를 붙였다(원문이 "주말에 산림욕 하러 갈까 해").
                     순 효과는 감소지만 근거 없는 accord 가 생기는 새 경로다

표본이 작고 독립이 아니다  표현 96개(되살림 46개는 26문장에서 나왔다) ·
                     파이프라인 42문장. 정상 손실 0/20 의 Wilson 상한이 16.1% 다

NDCG 가 1회분이다      평균은 0인데 **개별 쿼리가 0.67 까지 움직인다**(오름 27·내림 30).
                     흔들림과 못 가른다 — 17번이 "①단계 호출 자체가 NDCG 를 0.06
                     흔든다" 고 한 것과 같다. "0.06 수준 변화는 없다" 까지만 말한다

안 쟀다               v1 이 빈 목록을 내던 표현 142개 쪽.
                     그쪽에서 v2 가 답하기 시작하면 오탐이 는다
```

### ⚠ 32번을 정정했다 — 되살림 38% 중 2건은 ⑤단계가 아니다

```
⑤단계가 되살림                    17/50  34.0%
①단계 표현이 사전 조건을 통과시킴      2/50   4.0%
   이불 빨아서 널어놨어 · 옛날 사진관에 가면 분내가 배어 있어
```

`understand()` 의 haystack 이 원문 + `additional_requirements` 라서, **①단계가 만든
표현에 조건 낱말(`향`·`냄새`·`느낌`)이 있으면 사전 조건이 통과한다.** 사전 경로만 볼
때는 막히던 문장이다. **이 경로는 ⑤단계를 고쳐도 안 막힌다** — 별개 과제다.

### ⚠ 이번에 한 번 틀렸다 — 다음 사람이 같은 함정에 빠지지 않게

**첫 측정이 정반대 결과를 냈다**(되살림 -1.4% · 정상 -18.0%). `load_prompt()` 가
**경로를 키로 프롬프트를 캐시해서**(`llm_stage1.py:100`) v2 쪽 288회가 전부 첫 표현의
원문을 썼다. 원문 해시로 경로를 나누고 assert 로 확인한 뒤 576회를 다시 돌렸다.

**첫 결과가 예상과 맞는 방향이어서 의심하지 않았다.** 팀 코드에서 프롬프트 경로를
바꿔 가며 부르는 측정은 전부 이 함정에 걸린다.

**남은 둘 중 어느 것도 B1 되살림 16/27 을 다 막는다고 보장할 수 없다.** B1 은
`꿀`·`산림`·`시럽` 이 제대로 쓰인 문장이고 ⑤단계가 **맞게** 답해서 오탐이 되는
유형이다. 문장 맥락을 주는 쪽이 유일하게 이 유형에 닿을 수 있는 방법이다.

**두 표본이 같은 말을 한다.** 사전 조건으로 막든 경계 판정으로 막든 ⑤단계가 되돌린다 —
38.0% 와 69.2% 다. **막는 방법을 바꾸는 것으로는 안 풀린다.**

그리고 막는 것이 늘 안전하지 않다. 조건이 0개가 되면 ⑤단계가 문장 전체를 보고
상상한다 — `초록물고기 분위기 같은 향수` 가 `green` 에서 `aquatic·green·marine` 이
됐다. **⑤단계를 고칠 때 이것을 함께 봐야 한다.**

### ② 미뤄 둔 것 셋

```
C1 파이프라인 측정    한 낱말 입력 27개를 파이프라인으로 안 돌렸다(GMS 81회).
                   ⑤단계가 되살리면 **"한 낱말 입력은 죽는다" 가 사실이 아니게 된다.**
                   실사용에 0건이라(설문 143) 뒤로 뒀다 (N19 한계 3)

프롬프트 주입 방어    ①단계 프롬프트에 <USER_DATA> 구분자. 고치는 건 20분인데
                   ①단계 출력 전체가 흔들려 회귀 측정(GMS 600회)이 필요하다.
                   3회 중 1회 통했고 피해는 "엉뚱한 추천" 수준이다 (측정 기록 25번)

평가 체계 개편       GPT 조사가 권한 레이어 분리. NDCG 를 맨 뒤로 내리고 OOD 오작동률·
                   오교정률을 앞에 둔다. 측정 기록 17번이 막힌 지점과 맞물린다.
                   **N19 가 이쪽으로 한 걸음 갔다** — 오탐 판정에 파이프라인을 넣었다
```

---

## 5. 알고 있는데 아직 못 막는 것

```
긴 문장 오탐      "꿀꿀한 기분일 때 쓸 향수" 는 '향' 이 있어 v1.15 의 조건을 통과한다.
                 조건이 가르는 것은 "오탐이냐" 가 아니라 "향 낱말이 있냐" 다.
                 **32번이 51문장으로 쟀다 — 51/51 이 예외 없이 샌다**
⑤단계 되살림      사전이 막은 문장의 38% 를 ⑤단계가 되살린다 (32번 · N19). 4장 ①.
                 경계 판정으로 막으면 **69.2%** 가 되살아난다 (28번 추기).
                 막는 방법을 바꾸는 것으로는 안 풀린다
B2 칸             표면형이 제대로 쓰인 향 무관 문장은 **형태소 분석기도 못 닿는다.**
                 `이불에서 쉰 냄새` 의 `이불` 은 진짜 이불이라 독립 형태소가 맞다
조건 없는 표면형    `바닷가` 처럼 조건이 없는 표면형 80개가 그대로 낸다.
                 `조개껍질` 을 막아도 `바닷가에서 조개껍질 주웠어` 는 향수를 받는다
합성어·신조어     `초록불`·`잼민이`·`이불킥` 은 문맥 조건도 형태소 경계 판정도 못 막는다.
                 분석기가 사전에 없는 말을 쪼개기 때문이라 구조적 한계다
우디(인형)        KOREAN_ACCORD 경로로 매칭돼 사전 조건이 아예 닿지 않는다
나무젓가락·과즙기   같은 행의 향 표현(`편백나무`·`상큼한`)을 살리려고 대상에서 뺐다
```

---

## 6. 설문이 공개 저장소에 있는 것 — **2026-09-19 에 그대로 두기로 정했다**

**다시 꺼내지 않는다.** 아래는 무엇을 보고 정했는지의 기록이다.

### 결정

**응답자를 특정할 수 있는 내용이 없어서 그대로 둔다.** 155건에 이름과 연락처가
**0건**이고, 지목됐던 건강 관련 서술 11건 · 신분 관련 5건 · 가격 24건은 전부 서술이지
식별자가 아니다.

`NLR_DEPLOY_HANDOFF.md` 가 같은 성격의 데이터를 개인정보로 다루라고 경고해 둔 것과
어긋나 보이지만, 그 경고는 **응답자 식별 가능성**을 전제로 한 것이고 이 자료는 그
전제를 충족하지 않는다.

### 어떤 파일에 무엇이 있나 — 판단이 뒤집힐 때 필요하다

```
개인 저장소  github.com/masihe/hyanghae   API 조회 200 — 공개(public)

응답 원문이 있다  EDA/data/survey/raw/향수 설문조사.xlsx                      41KB
               EDA/data/survey/processed/survey_nlp_queries_raw.csv        155행
               EDA/data/survey/processed/survey_nlp_queries_candidates.csv 155행
               EDA/analysis_outputs/37_survey_stage1_checkpoint.csv        155행 · 162KB

원문이 없다    EDA/analysis_outputs/37_survey_per_query.csv   query_id 와 숫자뿐
               EDA/data/survey/processed/README.md            출처·문항 설명뿐
               37_survey_condition_count.ipynb                출력 셀 11개를 훑었고
                                                             응답 문장이 박힌 곳이 없다
```

**참조하는 코드가 17개다** (`80`·`82`·`85` 등 사전 스크립트 포함). 추적을 끊으면
로컬은 그대로 돌고 **저장소를 클론한 사람만** 재현을 못 한다.

### 나중에 판단이 뒤집히면 — 방법마다 결과가 다르다

```
.gitignore + git rm --cached   앞으로만 막는다. **과거 커밋은 GitHub 에서 그대로 열린다.**
                              삭제 커밋이 어느 파일이 민감한지 가리키는 표지가 된다
filter-repo + force push       과거에서도 지운다. 커밋 해시가 전부 바뀌고 되돌릴 수 없다.
                              GitHub 은 force push 전 커밋을 한동안 접근 가능하게 두고
                              포크·캐시·검색엔진에 남은 것은 못 지운다
저장소를 private 으로           **즉시 전부 닫힌다.** 이미 공개된 것을 끊는 유일한 방법이다.
                              GitHub 웹 Settings > General > Danger Zone 에서 한다
```

---

## 7. 지켜야 할 것

```
팀 저장소    ai/ 밖은 절대 건드리지 않는다
             브랜치는 {작업유형}/{영역}/{Jira 키}-{기능명} · Co-Authored-By 를 붙이지 않는다
             **커밋과 푸시는 요청받았을 때만 한다** (TEAM_CONVENTIONS.md 7장)
             MR 제목에는 Jira 키를 넣고 커밋 메시지에는 넣지 않는다
             MR 본문은 저장소 템플릿(`.gitlab/merge_request_templates/Default.md`)을 따른다
               ## 셋 + 불릿. 안내 주석은 지운다. 확인 못 한 체크박스는 비우고 이유를 적는다
             남의 브랜치를 볼 때는 worktree 를 쓴다 — 작업 트리가 섞이지 않는다
원본 데이터   perfumes.csv · 체크포인트 CSV 를 덮어쓰지 않는다
측정 스크립트  파일을 만들지 않고 표준출력에만 쓴다 (41번 이후 관례)
             예외 — GMS 를 부르는 69·74·77번은 체크포인트를 남긴다
사전 버전    팀 저장소 기본값이 **v1_17** 이다 (335 병합)
             **버전마다 생성 스크립트와 changelog 가 하나씩 있다** (49·51·53~56·80·84·85번)
             **사전을 올리면 보호 목록도 다시 만든다** (N18 한계 6). 엔진 기본값 두 줄과
               search_rules.md 의 행 수, 계약서의 lexiconVersion 예시 4곳이 따라 바뀐다
             ⚠ **판정에 파이프라인을 넣는다** (N19). `막아야 하는 것 N/N` 만으로는
               절반이다 — **사전이 막은 문장을 ⑤단계까지 돌려** 되살아나는지 본다.
               GMS 비용 때문에 막은 것만 돌린다. 86번 CSV 를 회귀 확인에 그대로 쓴다
             ⚠ **같은 종류는 모아서 한 버전으로 올린다.** 2026-09-19 에 오탐 표면형
               추가를 v1.15 -> v1.16 -> v1.17 로 세 번 올렸다. 기능 문제는 없지만
               lexicon_version 으로 기록을 해석할 때 잔버전이 많으면 불편하다
설문 데이터   19번 판정에 따라 **동의 표현 12건을 뺀 143건**으로 잰다
             **사전 확장에 쓰지 않는다** — 채점용이다 (spec §7.2)
             다만 §7.2 는 사전 **확장**의 규칙이다. **제약**에는 그대로 적용되지 않는다 (N16)
개인 저장소   커밋 제목은 한국어 · `—` 뒤에 알게 된 것을 적는다. `feat:` 접두어를 안 쓴다
             **공개 저장소다**(github.com/masihe/hyanghae). 6장 참고
GMS 호출     **크레딧에 제한이 있다.** 아래 7-1장을 반드시 읽는다
```

---

## 7-1. ⚠ GMS 측정에서 걸린 함정 셋 — 2026-09-19~20 에 787회를 버렸다

**하루에 세 번 틀렸고 약 2,137회 중 787회(37%)가 무효가 됐다.** 셋 다 같은 원인이고
셋 다 **2~3건짜리 예비 실행을 먼저 했으면 잡혔다.** 다음 사람이 같은 자리에서 버리지
않도록 남긴다.

### 함정 1 — `load_prompt()` 가 경로를 키로 캐시한다

```
어디      llm_stage1.py:100 · `_prompt_cache` 가 모듈 전역이고 키가 경로다
무슨 일    ⑤단계 프롬프트에 원문을 채워 **같은 경로에 덮어쓰며** 576회를 돌렸다.
          첫 호출 뒤로는 파일을 다시 안 읽어 **288회가 전부 첫 표현의 원문을 썼다**
결과      오탐 -1.4% · 정상 -18.0% 라는 **정반대 결론**이 나왔다.
          고치니 오탐 -13.0% · 정상 +12.7% 였다 (33번)
왜 못 봤나  첫 결과가 **내가 미리 걱정해 둔 방향과 일치**해서 의심하지 않았다
피하는 법   프롬프트 내용이 달라지면 **경로도 다르게** 한다(원문 sha256 앞 16자).
          호출마다 `assert 원문[:20] in s1.load_prompt(tmp)` 를 넣는다
잃은 것    576 + 14 = 590회
```

### 함정 2 — `structured` 가 `None` 이면 ⑤단계가 아예 안 돈다

```
어디      nlr_engine.py:439 · `_unmatched()` 의 `if not structured: return []`
무슨 일    평가셋에 없는 문장(내가 지어낸 오탐 문장)을 `recommend(index, text, None, ...)`
          으로 재생했다. unmatched 가 비어 ⑤단계가 안 돌았는데 **0/57 을
          "전부 막혔다" 로 읽을 뻔했다**
왜 잡혔나  같은 문장이 `pipeline2` 에서 19/19 로 되살아났던 것과 **모순**이라서
피하는 법   재생할 때 `assert u["unmatched"]` 를 넣는다.
          그 assert 가 32번의 정정 사항(①단계 경로 2건)까지 잡아냈다
잃은 것    0회 (재생이라 GMS 를 안 썼다)
```

### 함정 3 — `74_estimator_checkpoint.csv` 의 `accords` 는 파이프 구분 문자열이다

```
어디      analysis_outputs/74_estimator_checkpoint.csv
무슨 일    `'floral|white floral|rose'` 인데 JSON 으로 파싱했다.
          369행 중 227행이 조용히 `[]` 가 되어 **v1 이 아무 조건도 못 만들었다**
결과      NDCG 측정에서 `v1` 과 `⑤단계 끔` 의 수치가 **완전히 같게** 나왔다
피하는 법   외부 CSV 는 **값을 몇 줄 출력해 보고** 파서를 쓴다.
          파싱 실패 건수를 세서 0 인지 확인한다
잃은 것    197회 — **그런데 파싱과 무관하게 멀쩡했던 v2 응답 197건을
          파일로 저장하지 않아 같이 버렸다.** 이게 더 아팠다
```

### 그래서 지킬 것 넷

```
1  GMS 측정은 **2~3건만 먼저 돌려 결과를 보여주고 승인받은 뒤** 전체를 돌린다
2  **내 실수로 인한 재측정은 비용을 적어서 반드시 먼저 묻는다.**
   2026-09-19~20 에 576회와 197회를 묻지 않고 돌렸다
3  **응답을 받는 즉시 파일로 저장한다.** 메모리에만 두면 스크립트가 죽거나
   뒷단이 틀렸을 때 멀쩡한 호출까지 버린다 (함정 3)
4  팀 코드와 외부 파일은 **실제 동작·실제 값을 확인하고** 쓴다.
   `estimate_accords` 는 읽었지만 그 안의 `load_prompt` 는 안 읽은 것이 함정 1 이다
```

**공통 원인은 하나다 — 값싼 검증을 건너뛰고 비싼 측정을 먼저 돌렸다.**
셋 다 결과가 이상해서 뒤늦게 잡았지 사전에 막은 것이 아니다.

---

## 8. 저장소 상태 (2026-09-20 기준 · 343 병합 뒤)

```
팀 저장소    develop 은 **598e1eb** 까지 최신 — 346 병합이 머리다
            작업 트리는 **feature/ai/S15P21E203-347-context-conditions** (원격에 푸시됨)
              28be934 배포 데이터 3칸 · 6128aec 엔진이 후보를 좁힌다  **MR 은 아직 없다**
            343·346 브랜치는 병합 후 지웠다
            추적되지 않은 것 — .vscode/ · ai/docs/nlr_briefing.html (내가 만든 것이 아니다)
            사전은 **v1_17** 그대로다. 오늘 팀 저장소에 들어간 것은 343 과 346 둘이다

개인 저장소   master · 원격과 동기 · 작업 트리 깨끗
```

### 2026-09-20 에 개인 저장소에 쌓은 것 (오래된 것이 아래)

```
c297aed  343 병합에 맞춰 인수인계를 갱신했다 — 배포된 서비스에서 나온 첫 버그였다
debfbf4  인수인계 맨 위에 한 줄 요약을 넣었다
145a977  인수인계 0장을 다시 쓰고 ⑤단계 문장 맥락을 보류로 적었다
2353ee1  v2 가 커버리지를 올린 이유를 확인했다 — 표현이 아니라 원문에 답한다
3747809  ⑤단계 v2 의 NDCG 를 쟀다. 실수 셋을 7-1장에 모았다
c43f161  파이프라인 전체로 v1 vs v2 를 다시 쟀다 — 32번을 정정했다
3e47bd4  ⑤단계에 문장 맥락 주는 효과를 쟀다 — 예상이 틀렸다
324de58  ⑤단계에 문장 맥락 주는 비용을 쟀다
e2ebe2d  형태소 분석기를 기각했다 — 막아도 ⑤단계가 69% 를 되살린다
18210ba  설문을 공개 저장소에 그대로 두기로 정했다
3e716c9  형태소 분석기를 51문장으로 다시 쟀다 — 보류 (e2ebe2d 에서 기각)
57be730  인수인계를 갱신했다
d49dfaf  오탐 검증을 156문장으로 넓혔다 — 86번 CSV 둘 · 32번 · N19
```

### 오늘 만든 산출물

```
EDA/analysis_outputs/86_misfire_sentences.csv             156행 · 다음 사전 버전 회귀 확인용
EDA/analysis_outputs/86_misfire_word_classification.csv   303행 · 낱말 분류와 거른 근거
EDA/docs/nlr_engineering_notes.md   32·33번 신설 · 28번 추기 · 32번 정정 · 부록 6행
EDA/docs/DECISIONS.md               N19
EDA/docs/plans/NLR_NEXT_SESSION.md  1-5~1-11장 · 7-1장 · 0장 재구성 · 6장 결정 기록
팀 ai/nlr_engine.py · ai/docs/nlr_api_contract.md   343 (병합됨)
팀 ai/nlr_engine.py · ai/data/note_alias_ko.csv     346 (병합됨)
팀 ai/build_data.py · nlr_engine.py · perfumes_nlr.csv.gz   347 (**푸시됨 · 리뷰 전**)
347 은 저장소 변경이 없다 — 재고 기각했다 (1-9장). **그 뒤 근거가 무너졌다 (1-10장)**
35번(확장 NDCG)도 저장소 변경이 없다 — 조사와 측정만 했다
```

**GMS 는 약 2,660회를 썼고 그중 787회를 내 실수로 버렸다.** 경위는 7-1장에 있다.

---
## 관련 자료

**먼저 읽을 것**

- **7-1장** — GMS 측정에서 걸린 함정 셋. **측정을 시작하기 전에 읽는다.** 787회를 버렸다
- `docs/nlr_engineering_notes.md` **34번 + 추기 둘** — 노트 이름 입력(346).
  **왜 accord 만 쓰는지 · 노트를 넣으면 좋아지는지 · A'/A'' 중 왜 A' 인지**가 여기 있다.
  둘째 추기에 별칭 오탐 측정과 구현·검증이 있다
- `DECISIONS.md` **N14 추기** — 한국어 별칭을 얹는 결정. A'' 기각 근거 셋
- `docs/nlr_engineering_notes.md` **32번** — 오탐 검증 156문장.
  여섯 칸 설계와 ⑤단계 되살림 38% 가 여기 있다 (33번 추기가 34% 로 정정했다)
- `docs/nlr_engineering_notes.md` **33번 + 추기 셋** — ⑤단계에 문장 맥락 주기.
  비용 · 표현 단위 효과 · 파이프라인 · NDCG · **그 효과의 정체(원문에 답한다)**
- `DECISIONS.md` **N19** — 오탐 판정을 파이프라인 끝까지 잰다. N16 의 평가 방법을 바꾼다
- `docs/nlr_engineering_notes.md` **28번 추기** — 형태소 분석기 51문장 재비교.
  손실이 `초록` 하나라는 것과 **왜 기각인지**가 여기 있다
- `analysis_outputs/86_misfire_sentences.csv` — 문장 156개 · 칸 · 출처 · 기대 · 결과.
  **다음 사전 버전의 회귀 확인에 그대로 쓴다**
- `analysis_outputs/86_misfire_word_classification.csv` — 낱말 303개 · 분류 · 근거.
  `rare` 189개를 왜 걸렀는지가 여기 있다
- `docs/nlr_engineering_notes.md` **29·30·31번** — 오타 검문소를 접었다가 다시 연 과정과
  미뤄 둔 것 셋의 측정. **31번의 노출면 측정을 32번이 낱말 추출에 다시 썼다**
- `DECISIONS.md` **N17 → N18** — N17 이 검문소를 접었고 N18 이 뒤집었다.
  **N17 본문을 남겨 뒀다** — 무엇을 안 쟀는지가 남아야 같은 실수를 안 한다
- `docs/nlr_engineering_notes.md` **부록** — 미해결 질문. 31·32·33번으로 갱신됐고
  새 질문 다섯이 늘었다 (⑤단계 되살림 · 판정 기준 · 조건 근거 · promptSha256 · 호출 단위)

**346 — 한국어 노트 별칭 (병합됨)**

- **1-8장** — 고친 것 · 설계 판단 둘 · 효과 · **남긴 한계 넷**
- 팀 `ai/nlr_engine.py` `_longest_alias_ko` — 겹치면 가장 긴 별칭. 343 과 같은 규칙
  (커밋 `230a6f1`)
- 팀 `ai/data/note_alias_ko.csv` — 별칭 196행. 브리지 상위 150종 기반

**347 — 성별·계절·낮밤 (푸시됨 · 리뷰 전)**

- **1-11장** — 고친 것 · 효과 · **다음 사람이 알아야 할 것 여섯**
- `docs/nlr_engineering_notes.md` **35번의 구현 절** — 적용 자리가 측정과 다른 이유 등
- 팀 `ai/build_data.py` `_gender_lead` — 투표 0표를 공식 등급 평균으로 메우는 자리
- 팀 `ai/nlr_engine.py` `context_mask` — 3개 밑이면 그 칸을 포기하는 자리

**35번 — 평가자를 넓혔다 (확장 NDCG)**

- **1-10장** — 왜 만들었나 · 설계 · 결과 · **결론 셋** · 다음 사람이 알아야 할 것 여섯
- `docs/nlr_engineering_notes.md` **35번** — 원본 59칸 조사 · gender 공식 대 투표 ·
  context 세 칸 측정 · 확장 NDCG 전체. **여기가 원본이다**
- `DECISIONS.md` **N9 정정 추기** — 347 기각 근거 1이 무너진 경위
- 팀 `ai/build_data.py` `KEEP_COLUMNS` — 배포 데이터를 바꿔야 하는 자리
- **Jira `S15P21E203-347`** `[AI][Feature] 1단계 성별·계절·낮밤 조건 검색 반영` —
  원래 노트 정렬 이슈였는데 2026-09-20 에 **범위를 바꿔 재사용**했다

**노트 보유 정렬 (기각 · 근거가 무너짐) — Jira 번호 없음

- **1-9장** — 세 조각 · 정렬 자리 둘의 측정 · 기각 이유 · **다시 볼 조건 셋**
- `DECISIONS.md` **N9 추기 둘** — 정렬 규칙을 안 바꾼 결정과 **그 정정**
- 팀 `ai/build_data.py` `KEEP_COLUMNS` — 배포 데이터에 노트가 없는 자리

**343 — 배포된 서비스에서 나온 문제**

- **1-7장** — 추천 이유에 안 쓴 낱말이 뜨던 문제. 원인·고친 것·검증
- 팀 `ai/nlr_engine.py` `_lexicon_lookup` — `from` 에 표면형을 넣는다 (커밋 `7454247`)
- 팀 `ai/docs/nlr_api_contract.md` `matched[].from` — 설명을 보강했다.
  ⚠ **예시는 여전히 `빨래` 라 어긋남이 안 보인다**

**사전**

- `analysis_outputs/80_lexicon_v1_15_changelog.md` — v1.15 (짧은 표면형 문맥 조건)
- `analysis_outputs/84_lexicon_v1_16_changelog.md` — v1.16 (침대·침구)
- `analysis_outputs/85_lexicon_v1_17_changelog.md` — v1.17 (껍질·시럽)
- `DECISIONS.md` **N16** — 사전 행을 쪼개서 조건을 거는 방법. 84·85번이 그대로 따랐다
- 생성 스크립트 `80` → `84_lexicon_v1_16_bedding_condition.py`
  → `85_lexicon_v1_17_peel_syrup_condition.py` · 보호 목록은 `83` → `84` → `85`

**⑤단계와 캐시**

- `DECISIONS.md` **N15** — ⑤단계 추정을 켠 결정. **파일 캐시는 326 으로 부분 뒤집혔다**
- `docs/nlr_engineering_notes.md` 20·21·23·26번 — 추정기와 캐시 키
- `docs/nlr_engineering_notes.md` 22번 — `optional` 을 버리는 결함. **31번이 NDCG 를 쟀다**

**오탐**

- `docs/nlr_engineering_notes.md` 14번 — *"오탐이 0인 것은 운이다"* (25번의 예언)
- `docs/nlr_engineering_notes.md` 25·27·28번 — 오탐 확인 · v1.15 · 형태소 분석기 기각
- `77_abnormal_input.py` — 검증 문장 25개. **⚠ 지금 저장소에서 안 돈다** — 326 이
  `cached_estimator()` 의 `path` 인자를 없앴는데 두 곳에서 쓴다 (`TypeError`)

**계약과 팀**

- 팀 `ai/docs/nlr_api_contract.md` — 325·330·334·335 로 갱신됐다
- 팀 `ai/docs/search_rules.md` — 검색·정렬 규칙
- `docs/plans/NLR_DB_TABLE_REQUEST.md` — 320 이 구현한 요청서
- `TEAM_CONVENTIONS.md` — 브랜치 · 커밋 · MR · Jira.
  **Jira 제목은 명사형이다** (`[AI][Fix] … 오탐 차단`). 서술형(`~한다`)으로 쓰지 않는다
