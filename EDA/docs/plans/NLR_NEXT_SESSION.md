# 자연어 추천 — 다음 세션 인수인계

| | |
|---|---|
| 작성 | 2026-09-18 (전면 개정) · 2026-09-19 갱신 — 326·330·332·334·335 병합 |
| 담당 | 김용석 (AI) |
| 저장소 | 팀 `C:\Users\SSAFY\Desktop\S15P21E203` 의 **`ai/` 만** · 개인 `C:\Users\SSAFY\Desktop\hyanghae` 는 자유 |
| 이 문서 | **다음 세션이 시작할 때 읽는다.** 일이 끝나면 갱신하거나 지운다 |

---
## 0. 남은 일은 넷이다 — 이 표가 이 문서의 목차다

```
①  320 배포 여부 확인        2장    **인프라 담당**   이것 하나가 넷을 막고 있다
②  설문 공개 저장소           6장    설문 수집자·팀    개인정보. 판단이 필요하다
③  오타 검증 180개로 확대     4장    **승인받았다**    사용자가 하기로 했는데 아직 안 했다
④  미뤄도 되는 것 둘          4장    나중에          프롬프트 주입 방어 · 평가 체계 개편
```

**③만 지금 바로 할 수 있다.** ①②는 사람의 답을 기다리고, ④는 미뤄 둔 것이다.

### ① 320 배포 여부 — 가장 크다

배포가 안 됐으면 `nlr_*` 표에 아무것도 안 쌓이고, 그러면 **아래 넷이 전부 막힌 채로
남는다.** 오늘 한 작업 중 여럿이 여기서 다시 열린다.

```
오타 검문소의 실제 오교정      corrections.jsonl 과 nlr_* 표가 쌓여야 안다 (N18 다시 볼 조건)
사전 조건이 적절한지          지금 조건을 건 표면형이 v1.17 기준 19개다 (25·27·31번)
형태소 분석기를 다시 열지      긴 문장 오탐 빈도가 보여야 판단된다 (28번에서 기각한 것)
한 낱말 입력 빈도            조건을 건 표면형은 한 낱말로 입력하면 죽는다. 설문엔 0건이다
```

**확인은 인프라 담당에게 물어보는 것이다.** 로컬 확인법은 2장에 있다.

### ② 설문 공개 저장소 — 개인정보

개인 저장소가 **public** 인데 실사용자 설문이 올라가 있다(건강 서술 11건 · 신분 5건).
**요청 범위 밖이라 손대지 않았다.** 6장에 파일 목록이 있다.

### ③ 오타 검증 180개 — **승인받았는데 안 했다**

**2026-09-19 에 사용자가 하기로 정한 것이다.** 지금 오탐 판정이 문장 11개 기준이라
Wilson 95% 구간이 74.1%~100% 로 아무 말도 못 한다. 표면형마다 정상·오탐 문장을 만들어
180개로 넓히면 **①을 기다리지 않고도** 긴 문장 오탐·형태소 분석기 판단이 가능해진다.

```
비용   문장 작성 노동이 크고 GMS 호출이 수백 회 든다
한계   지어낸 문장이라 실사용 분포는 여전히 모른다 (그건 ① 이 푼다)
```

### ④ 미뤄도 되는 것 둘

프롬프트 주입 방어와 평가 체계 개편이다. 4장에 각각 왜 미뤘는지 적혀 있다.

---

## 1. 끝난 것 — 이번에 병합된 여섯

```
S15P21E203-326   ⑤단계 캐시 파일 복원 오류 수정      develop 08fcd55 · 커밋 734aa76
                 cached_estimator 가 기동 시 load_estimates() 를 부르지 않는다.
                 수정 전 코드와 같은 확인 여섯 개 중 하나만 실패 -> 통과로 뒤집혔다
                 **⚠ 오타 환각 자체는 안 고쳤다.** 파일에서 되살아나는 것만 막았다

백엔드 후속        nlr_* 표에 여섯 칸 저장            develop 0dc97d9
                 리뷰 의견 셋을 냈고 셋 다 반영됐다
                 **내 의견 ③이 틀렸다** — V23 의 주석을 고치면 Flyway 체크섬이 깨져
                 이미 적용한 DB 가 전부 기동을 못 한다. V24 로 덧붙인 쪽이 맞다

S15P21E203-330   계약서 result_count 기준 명시      develop 7e97e11 · 커밋 7b4e3a3
                 "AI 가 찾은 개수이고 사용자가 본 개수가 아니다" 를 적었다.
                 예시의 resultCount 5 를 1 로 고쳤다 — 그 예시의 results 가 하나뿐이라
                 자기모순이었고 백엔드 fixture 가 그대로 베껴 갔었다

S15P21E203-332   ⑤단계 앞 오타 검문소                develop 4f8eba5 — **1-1장**
S15P21E203-334   사전 v1.16 · 침대·침구 오탐          develop 13639da — **1-2장**
S15P21E203-335   사전 v1.17 · 껍질·시럽 오탐          develop ad0792c — **1-4장**
```

**`develop` 전체 백엔드 테스트를 돌려 확인했다 — 786개 통과 · 실패 0** (330 병합 시점).
V24 가 추천 밖 통합 테스트에도 영향이 없다.

---

## 1-1. 오타 검문소 `S15P21E203-332` — 병합됨. 다음 사람이 알아야 할 것

**한 번 접었다가(N17) 다시 열었다(N18).** 접은 근거가 포착률을 안 재고 내린 것이었다.
측정 기록 **29·30번**과 `DECISIONS.md` **N17·N18** 에 전부 있다.

```
커밋   431c3b4  feat: ⑤단계 앞에 오타 검문소 추가
      fb61c8b  fix: 검문소 판정이 조용히 사라지는 경로에 경고 추가 (리뷰 반영)
파일   ai/nlr_engine.py · ai/server.py · ai/unmatched_log.py
      ai/data/protected_words_v1_15.txt (628줄 · 4.6KB) — **지금은 v1_17 이다**
```

### 무엇을 하나

```
검문소는 사전이 조건을 하나도 못 뽑았을 때만 돈다  (⑤단계 앞)
   ↓  표현 안의 낱말을 하나씩 본다
사전 표면형 / 보호 목록에 있음 / 1글자   -> 그대로
   ↓  사전 표면형과 **자모** 거리 1 후보를 찾는다
후보 1개   -> 교정하고 사전 경로를 다시 태운다
후보 여럿  -> 빈 결과 (지어내지 않는다)
후보 없음  -> 그대로 ⑤단계 LLM 으로
```

```
포착률 (생성 오타 3,775개)     93.0%    엉뚱한 사전 항목으로 교정 0.0%
⑤단계 실제 표현 369개          그대로 361 · 교정 8 · 오교정 0 · LLM 호출 367 -> 362
검문소 지연                   중앙 0.779ms
```

### ⚠ 세 가지

```
보호 목록은 사전 버전에 묶인다  사전을 올릴 때마다 생성 스크립트를 다시 돌려야 한다.
                          334·335 에서 두 번 돌렸고 **표면형이 안 바뀌어 내용은 같았다**.
                          **안 하면 조용히 나빠진다** — 새 표면형의 오타를 못 잡거나
                          새로 위험해진 낱말을 못 막는다. 절차는 7장에 목록으로 있다

빨레 냄새는 여전히 안 잡힌다   `빨레` 가 국어 사전에 있어 보호 목록이 지킨다. 보호 목록은
                          `풀`·`너무`·`코트` 를 지키느라 `빨레` 도 같이 지킨다.
                          **`빨레향`(붙여쓴 형태)은 잡힌다.** 이 맞바꿈이 놓침 4.6% 다

오타의 실사용 빈도를 모른다    설문 155문장에 실제 오타가 사실상 0건이었다(브랜드명과
                          외래어뿐). 검문소가 얼마나 자주 일할지 모른 채 만들었다.
                          `ai/logs/corrections.jsonl` 이 쌓이면 보인다 — 2장이 막고 있다
```

### 리뷰에서 나온 것 — 아직 안 한 것 하나

```
영문 표면형 17개가 typo_targets 에 들어가 있다 (EDP·clean·cute…). 한글 낱말과는
거리 1이 될 수 없어 **무해하지만 영원히 매칭되지 않는 죽은 항목**이다.
빼는 것은 동작이 같고 검증을 다시 돌려야 해서 두었다. 리뷰어도 넘어갔다
```

---
## 1-2. 사전 v1.16 `S15P21E203-334` — 침대·침구 오탐. 병합됨

`77번이 확인한 사전 오탐 다섯 중 `침대` 가 v1.15 이후에도 남아 있었다.` 를 고쳤다.
측정 기록 **31번** · changelog `analysis_outputs/84_lexicon_v1_16_changelog.md`.

```
커밋   f106782  fix: 사전 v1.16 으로 침대·침구 오탐 차단   MR !195 · develop 13639da
만든 것 EDA/84_lexicon_v1_16_bedding_condition.py   (80번의 apply_condition 재사용)
       EDA/84_protected_words_v1_16.py             (83번의 v1.16 판)
       사전 72행 -> 74행
```

### 왜 남아 있었나

**누락이라기보다 분류가 갈린 것이다.** 78번이 C2 로 `침대`·`침구`·`이불` 을 함께
제안했는데 80번(v1.15)이 `이불` 만 가져갔고 `MISFIRE` 목록에 `침대` 가 없어 시험조차
하지 않았다. 기록 27 은 `침대`·`침구` 를 *"행 조건에 딸려 죽던 향 표현 22개"* 안에 넣어
**살릴 것** 으로 봤다. 같은 행의 `이불` 은 **막을 것** 으로 보고 쪼갰다.

### 판정 (v1.15 와 같은 기준)

```
막아야 하는 것  5/5     침대에서 뒹굴기만 했다 · 침대보 · 침구 정리 · 받침대 · 침대칸
살려야 하는 것  5/5     침대 냄새 같은 향수 · 침대에 누운 것 같은 포근한 향 …
평가셋 손실    0건      설문 143 · 합성 600
NDCG@5 차이   +0.000000
```

### ⚠ 다음 사람이 알아야 할 것

```
lexiconVersion 가 바뀌었다   응답과 nlr_recommendations.lexicon_version 이 v1_15 -> v1_16
                          이 됐고, **같은 날 335 로 v1_17 이 됐다.** 집계하는 쪽은 값이
                          셋으로 갈린다는 걸 알아야 한다 — 7장의 "모아서 올린다" 참고

보호 목록도 다시 만들었다     N18 한계 6 의 절차다. **내용은 v1.15 와 같았다**(sha256 일치) —
                          표면형 집합이 안 바뀌었기 때문이다. 다음에 표면형이 바뀌면 달라진다

남은 구멍 하나            kr.scene.bedding 의 **musky 행**에는 `침대`·`침구` 가 조건 없이
                          남는다. 그 행이 optional 이라 80번의 쪼개기가 안 건드렸고 엔진이
                          optional 을 버리므로 지금은 안 샌다. **기록 22(optional 살리기)를
                          고치면** `침대에서 뒹굴기만 했다` 가 optional=['musky'] 를 낸다.
                          core 가 비어 향수는 안 나가지만 다른 조건이 함께 잡힌 문장에서는
                          점수에 섞인다. **둘을 같이 볼 때 이 행도 쪼개야 한다**

한 낱말 `침대` 입력이 죽는다  `이불`·`꿀`·`잼`·`빨래` 는 v1.15부터 이미 그렇고
                          한 낱말 입력은 설문 143건에 0건이다
```

---

## 1-3. 미뤄 둔 것 셋을 쟀다 — 측정 기록 31번

부록과 기록 22·23 이 **"비용을 재지 않았다"** 고 명시한 채 미뤄 둔 것들이다.
**셋 다 지금은 이득이 없다.** 부록의 해당 네 행을 갱신했다.

```
optional 을 살리면   NDCG -0.000236 · 95% 구간 [-0.00226, +0.00180] 로 0 을 포함하고
                  **내린 쿼리가 더 많다**(오름 6 · 내림 9). 기록 22 의 추정이 맞았다.
                  다만 optional 이 생기는 문장이 합성 4.7% · **설문 18.1%** 라
                  평가셋이 이 변경을 잴 자료가 아니다. **설문 라벨이 생기면 다시 연다**

회피 커버리지       사전·브리지를 태우면 33.8% -> 41.5% 로 오르지만 **avoid 가 하드 제외**라
                  올릴수록 위험하다 — `sweet` 하나가 카탈로그의 46.9%, `woody` 가 63.0% 를
                  지운다. **먼저 정할 것은 커버리지가 아니라 강도(하드 제외 vs 감점)다.**
                  반대로 N12 가 걱정한 `머스크` 는 +1.0%p 뿐이었다(`soapy` 가 1.3%)

캐시 키 원문화      잃는 것(표현 단위 재사용)이 **설문 2.6% · 합성 10.1%** 로 작다.
                  이득(같은 원문 재요청 빈도)은 여전히 로그가 있어야 한다
```

**부수적으로 오탐 노출면을 실사용 데이터 없이 재는 방법이 생겼다.** 국어 낱말 23만 개로
표면형이 품은 실재 낱말을 센다. 이 측정으로 위의 `침대`·`침구` 를 찾았다.
**노출면이 크다고 오탐은 아니다** — `나무`(624개)는 대부분 다른 나무라 `woody` 가 맞고,
v1.15 가 `나무` 를 뺀 판단이 옳았다는 것이 이 측정으로 확인된다.

---
## 1-4. 사전 v1.17 `S15P21E203-335` — 껍질·시럽 오탐. 병합됨

측정 기록 **31번**이 국어 낱말 23만 개로 오탐 노출면을 재면서 찾은 둘이다.
changelog `analysis_outputs/85_lexicon_v1_17_changelog.md`.

```
상태   병합됨 · MR !196 · develop ad0792c
커밋   e970b8e  fix: 사전 v1.17 로 껍질·시럽 오탐 차단
만든 것 EDA/85_lexicon_v1_17_peel_syrup_condition.py   (80번의 apply_condition 재사용)
      EDA/85_protected_words_v1_17.py
      사전 74행 -> 76행
```

```
조개껍질 주웠어 · 달걀 껍질 까느라 힘들었어 · 눈껍질이 무겁다   ->  citrus -> 향수 5개
귀찬시럽다 · 거치장시럽다                                ->  sweet  -> 향수 5개
```

**`시럽` 쪽은 방언 어미 `-시럽다`(=`-스럽다`)다.** 국어 낱말 목록에 49개가 있고
`껍질` 은 11개다. 둘 다 향 표현과 한 행에 있어 v1.15 방식대로 `.short` 로 쪼갰다 —
`상큼한`·`새콤`·`과즙`·`톡 쏘는`·`당밀` 은 조건 없이 그대로 산다.

### 판정 (v1.15·v1.16 과 같은 기준)

```
막아야 하는 것  5/5     ·  살려야 하는 것  6/6
평가셋 손실    0건      설문 143 · 합성 600
NDCG@5 차이   +0.000000
```

팀 저장소 코드로 15건을 확인했고 **v1.15·v1.16 의 수정도 그대로 살아 있다.**
검문소 회귀 경로 다섯도 통과했다.

### ⚠ 남은 것

```
과즙                80번이 NOT_BLOCKED 에 남긴 `과즙기` 오탐이 그대로다.
                   행을 쪼개면 막을 수 있지만 한 번에 하나씩 바꾸려고 뒀다
나무껍질이 벗겨졌다     `껍질` 은 막혔지만 `나무` 가 woody 를 낸다. `나무` 는 품은 낱말
                   624개가 거의 다 다른 나무라 대상이 아니다(31번)
보호 목록            절차대로 다시 만들었고 v1.16 과 **같은 파일**이었다(sha256 일치)
```

---

## 2. 막혀 있는 것 — 실사용 로그. 320 배포 여부를 확인해야 한다

**320(DB 표 4개)이 병합됐지만 배포됐는지 모른다.** 배포가 안 되면 아무것도 안 쌓이고,
그러면 아래 질문이 전부 막힌 채로 남는다.

```
오탐의 실사용 빈도        사전 조건 9개가 적절한지 · 더 걸지 뺄지 (측정 기록 25·27번)
긴 문장 오탐 빈도         형태소 분석기를 다시 열지 (측정 기록 28번에서 기각한 것)
한 낱말 입력 빈도         설문 143건에 0건인데 검색창은 다를 수 있다
NO_CONDITION 의 실제 분포
```

### 확인하는 법

```bash
cd C:/Users/SSAFY/Desktop/S15P21E203
docker compose -f compose.manual-test.yaml up -d --wait
docker compose -f compose.manual-test.yaml exec -T postgres \
  psql -U hyanghae -d hyanghae_test -c "SELECT count(*) FROM nlr_recommendations;"
```

로컬은 이렇게 보고 **운영 서버는 인프라 담당이 확인해야 한다.**

---

## 3. 끝난 것 — 다시 하지 말 것

### 병합됨

```
S15P21E203-307   향 사전 v1.15          짧은 표면형 9개에 문맥 조건. 사전 63 -> 72행
S15P21E203-320   DB 표 4개 (백엔드)      검토 완료 · 전체 774건 통과
S15P21E203-325   응답에 칸 6개          develop 609e132
                 최상위      lexiconVersion · promptSha256
                 unmatched  hadConditions · llmAccords · tier · resultCount
                 llmReasoning 은 일부러 뺐다 — 지어낸 설명이 화면으로 샐 길을 막는다
S15P21E203-326   ⑤단계 캐시 수정         develop 08fcd55 — 1장
백엔드 후속        nlr_* 표에 여섯 칸 저장   develop 0dc97d9 — 1장
S15P21E203-330   계약서 result_count 기준  develop 7e97e11 — 1장
```

### 잰 것 — 측정 기록 25~30

```
25  향과 무관한 문장 11건이 11건 다 향수를 받았다. 14번이 예언한 오탐이 실제로 있었다
26  ⑤단계가 한 번 잘못 상상한 것이 파일에 굳어 영구히 되풀이된다
27  사전 v1.15 — 조건을 행에 걸었더니 향 표현 22개가 같이 죽어서 행을 쪼갰다
28  형태소 분석기를 경계 판정 용도로 다시 열었다. 긴 문장 오탐을 막지만 합성어에서 잃는다
29  오타 검문소를 세 방식으로 재봤다. 셋 다 빨레를 못 잡는다  <- **결론이 30번에 뒤집혔다**
30  29번이 포착률을 안 재고 접었다. 재보니 93% 라서 만들었다
31  미뤄 둔 것 셋의 "재지 않았다" 를 쟀다. 셋 다 지금은 이득이 없다
```

측정 스크립트는 `77_abnormal_input.py` ~ `84_protected_words_v1_16.py` 아홉 개다.
29·30번의 측정 스크립트는 남기지 않았다(41번 이후 관례). 83번만 생성 스크립트라 남겼다.

### DECISIONS 갱신

```
N13  결론 유지 · **이 결정이 답한 질문의 범위를 좁혀 적었다** (어간 매칭만 기각한 것)
N15  **파일 캐시가 부분 뒤집혔다.** 추정을 켜는 결정 자체는 유지
N16  신설 — 짧은 표면형에 문맥 조건을 건다
N17  신설 후 **N18 로 대체됨.** 본문은 그대로 뒀다 — 무엇을 안 쟀는지가 남아야 한다
N18  신설 — 오타 검문소를 만든다. N17 을 뒤집는다
```

### 기각한 것 — 되풀이하지 말 것

```
표면형을 사전에서 뺀다      설문 4건 · 합성 14건 손실. 한국어가 교착어라 `숲향`·`숲속에` 를 잃는다
낱말 경계를 본다(조사 목록)  설문 2건 · 합성 9건 손실. `초록불`과 `초록빛` 을 못 가른다
형태소 분석기 경계 판정      긴 문장 오탐 2건을 더 막지만 합성어에서 3~4건 잃는다.
                        의존성 +1 · 메모리 +10MB. **실사용 빈도가 보이면 다시 연다**
```

---

## 4. 안 한 것 — 승인받은 것 하나 · 미뤄 둔 것 둘

### ③ 승인받았는데 아직 안 했다 — 오타 검증 180개

**2026-09-19 에 사용자가 하기로 정했다.** 세션이 끝나 못 했을 뿐이다.

```
왜        오탐 판정이 문장 11개 기준이라 Wilson 95% 구간이 74.1%~100% 다.
          표본이 작아 **"11/11 막았다" 가 무엇을 뜻하는지 말할 수 없다**
무엇을     표면형마다 정상 문장과 오탐 문장을 만들어 180개 규모로 넓힌다
얻는 것    **320 배포(2장)를 기다리지 않고도** 긴 문장 오탐·형태소 분석기 재개
          여부를 판단할 수 있게 된다. 지금은 둘 다 표본이 없어 막혀 있다
비용      문장 작성 노동이 크고 GMS 호출이 수백 회 든다
한계      지어낸 문장이라 **실사용 분포는 여전히 모른다.** 그건 2장이 푼다
시작점     `77_abnormal_input.py` 의 `CASES` 25개가 지금 쓰는 문장이다
```

### ④ 미뤄 둔 것 둘

```
프롬프트 주입 방어    ①단계 프롬프트에 <USER_DATA> 구분자. 고치는 건 20분인데
                   ①단계 출력 전체가 흔들려 회귀 측정(GMS 600회)이 필요하다.
                   3회 중 1회 통했고 피해는 "엉뚱한 추천" 수준이다 (측정 기록 25번)
평가 체계 개편       GPT 조사가 권한 레이어 분리. NDCG 를 맨 뒤로 내리고 OOD 오작동률·
                   오교정률을 앞에 둔다. 측정 기록 17번이 막힌 지점과 맞물린다
```

---

## 5. 알고 있는데 아직 못 막는 것

```
긴 문장 오탐      "꿀꿀한 기분일 때 쓸 향수" 는 '향' 이 있어 v1.15 의 조건을 통과한다.
                 조건이 가르는 것은 "오탐이냐" 가 아니라 "향 낱말이 있냐" 다
합성어·신조어     `초록불`·`잼민이`·`이불킥` 은 문맥 조건도 형태소 경계 판정도 못 막는다.
                 분석기가 사전에 없는 말을 쪼개기 때문이라 구조적 한계다
우디(인형)        KOREAN_ACCORD 경로로 매칭돼 사전 조건이 아예 닿지 않는다
나무젓가락·과즙기   같은 행의 향 표현(`편백나무`·`상큼한`)을 살리려고 대상에서 뺐다
```

---

## 6. ⚠ 작업 중 발견한 것 — 고치지 않았다

**실사용자 설문이 공개 저장소에 올라가 있다.**

```
개인 저장소  github.com/masihe/hyanghae   API 조회 200 — 공개(public)
             EDA/data/survey/raw/향수 설문조사.xlsx
             EDA/data/survey/processed/survey_nlp_queries_raw.csv
             EDA/analysis_outputs/37_survey_stage1_checkpoint.csv
```

155건 중 건강 관련 서술 11건 · 신분 관련 5건 · 가격 24건이다(이름·연락처는 0건).
`NLR_DEPLOY_HANDOFF.md` 가 같은 성격의 데이터를 개인정보로 다루라고 경고해 뒀는데
공개 저장소에는 그대로 있다.

**요청 범위 밖이라 손대지 않았다.** 응답자에게 공개를 전제로 받았는지 확인이 필요하고,
판단은 설문을 수집한 사람과 팀에 달렸다.

---

## 7. 지켜야 할 것

```
팀 저장소    ai/ 밖은 절대 건드리지 않는다
             브랜치는 {작업유형}/{영역}/{Jira 키}-{기능명} · Co-Authored-By 를 붙이지 않는다
             **커밋과 푸시는 요청받았을 때만 한다** (TEAM_CONVENTIONS.md 7장)
             MR 제목에는 Jira 키를 넣고 커밋 메시지에는 넣지 않는다
             MR 본문은 저장소 템플릿(`.gitlab/merge_request_templates/Default.md`)을 따른다
               ## 셋 + 불릿. 안내 주석은 지운다. 확인 못 한 체크박스는 비우고 이유를 적는다
             남의 브랜치를 볼 때는 worktree 를 쓴다 — 작업 트리가 섞이지 않는다
원본 데이터   perfumes.csv · 체크포인트 CSV 를 덮어쓰지 않는다
측정 스크립트  파일을 만들지 않고 표준출력에만 쓴다 (41번 이후 관례)
             예외 — GMS 를 부르는 69·74·77번은 체크포인트를 남긴다
사전 버전    팀 저장소 기본값이 **v1_17** 이다 (335 병합)
             **버전마다 생성 스크립트와 changelog 가 하나씩 있다** (49·51·53~56·80·84·85번)
             **사전을 올리면 보호 목록도 다시 만든다** (N18 한계 6). 엔진 기본값 두 줄과
               search_rules.md 의 행 수, 계약서의 lexiconVersion 예시 4곳이 따라 바뀐다
             ⚠ **같은 종류는 모아서 한 버전으로 올린다.** 2026-09-19 에 오탐 표면형
               추가를 v1.15 -> v1.16 -> v1.17 로 세 번 올렸다. 기능 문제는 없지만
               lexicon_version 으로 기록을 해석할 때 잔버전이 많으면 불편하다
설문 데이터   19번 판정에 따라 **동의 표현 12건을 뺀 143건**으로 잰다
             **사전 확장에 쓰지 않는다** — 채점용이다 (spec §7.2)
             다만 §7.2 는 사전 **확장**의 규칙이다. **제약**에는 그대로 적용되지 않는다 (N16)
개인 저장소   커밋 제목은 한국어 · `—` 뒤에 알게 된 것을 적는다. `feat:` 접두어를 안 쓴다
             **공개 저장소다**(github.com/masihe/hyanghae). 6장 참고
```

---

## 8. 저장소 상태 (2026-09-19 기준 · 335 병합 뒤)

```
팀 저장소    develop 은 ad0792c 까지 최신(335 병합이 머리다) · 작업 트리도 develop 이다
            **내 작업 브랜치는 로컬에도 원격에도 없다.** 임시 worktree 도 없다
            추적되지 않은 것 — .vscode/ · ai/docs/nlr_briefing.html (내가 만든 것이 아니다)

개인 저장소   master · 원격과 동기
            2842da6  사전 v1.17 — 껍질·시럽에도 문맥 조건
            ae08b8a  334 병합에 맞춰 인수인계를 갱신했다
            0f02689  사전 v1.16 — 침대·침구에도 문맥 조건
            cfb4df4  미뤄 둔 것 셋의 "재지 않았다" 를 쟀다
            278eac2  오타 검문소를 만들었다 — 29번이 포착률을 안 재고 접은 것이었다
            92ba194  오타 검문소를 세 방식으로 재고 접었다 — 빨레가 국어 사전에 있다
```

---
## 관련 자료

**먼저 읽을 것**

- `docs/nlr_engineering_notes.md` **29·30·31번** — 오타 검문소를 접었다가 다시 연 과정과
  미뤄 둔 것 셋의 측정. **31번이 남은 일 ③의 근거다**
- `DECISIONS.md` **N17 → N18** — N17 이 검문소를 접었고 N18 이 뒤집었다.
  **N17 본문을 남겨 뒀다** — 무엇을 안 쟀는지가 남아야 같은 실수를 안 한다
- `docs/nlr_engineering_notes.md` **부록** — 미해결 질문 29개. 31번으로 네 행이 갱신됐다

**사전**

- `analysis_outputs/80_lexicon_v1_15_changelog.md` — v1.15 (짧은 표면형 문맥 조건)
- `analysis_outputs/84_lexicon_v1_16_changelog.md` — v1.16 (침대·침구)
- `analysis_outputs/85_lexicon_v1_17_changelog.md` — v1.17 (껍질·시럽)
- `DECISIONS.md` **N16** — 사전 행을 쪼개서 조건을 거는 방법. 84·85번이 그대로 따랐다
- 생성 스크립트 `80` → `84_lexicon_v1_16_bedding_condition.py`
  → `85_lexicon_v1_17_peel_syrup_condition.py` · 보호 목록은 `83` → `84` → `85`

**⑤단계와 캐시**

- `DECISIONS.md` **N15** — ⑤단계 추정을 켠 결정. **파일 캐시는 326 으로 부분 뒤집혔다**
- `docs/nlr_engineering_notes.md` 20·21·23·26번 — 추정기와 캐시 키
- `docs/nlr_engineering_notes.md` 22번 — `optional` 을 버리는 결함. **31번이 NDCG 를 쟀다**

**오탐**

- `docs/nlr_engineering_notes.md` 14번 — *"오탐이 0인 것은 운이다"* (25번의 예언)
- `docs/nlr_engineering_notes.md` 25·27·28번 — 오탐 확인 · v1.15 · 형태소 분석기 기각
- `77_abnormal_input.py` — 지금 쓰는 검증 문장 25개. **남은 일 ③의 시작점**

**계약과 팀**

- 팀 `ai/docs/nlr_api_contract.md` — 325·330·334·335 로 갱신됐다
- 팀 `ai/docs/search_rules.md` — 검색·정렬 규칙
- `docs/plans/NLR_DB_TABLE_REQUEST.md` — 320 이 구현한 요청서
- `TEAM_CONVENTIONS.md` — 브랜치 · 커밋 · MR · Jira.
  **Jira 제목은 명사형이다** (`[AI][Fix] … 오탐 차단`). 서술형(`~한다`)으로 쓰지 않는다
