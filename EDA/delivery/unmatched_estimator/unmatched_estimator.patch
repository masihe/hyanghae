diff --git a/ai/llm_stage1.py b/ai/llm_stage1.py
index 5adf236..a50c1eb 100644
--- a/ai/llm_stage1.py
+++ b/ai/llm_stage1.py
@@ -263,3 +263,77 @@ def structure(text, *, timeout=DEFAULT_TIMEOUT, prompt_path=PROMPT_PATH, post=No
     except (KeyError, IndexError, TypeError) as exc:
         LOGGER.error("GMS 응답에 choices[0].message.content 가 없다: %s", exc)
     return None
+
+
+# ---------------------------------------------------------------------------
+# ⑤단계 — 사전이 못 받은 표현을 accord 로 추정
+# ---------------------------------------------------------------------------
+ESTIMATOR_PROMPT_PATH = pathlib.Path(__file__).resolve().parent / "data" / "estimator_prompt_v1.txt"
+ESTIMATOR_TOP_N = 3          # 표현 하나가 만들 accord 개수 상한. 측정 기록 20번의 E3
+ESTIMATOR_TIMEOUT = 8.0      # [측정] 호출 지연 중앙 1.16s · 최대 3.43s (측정 기록 20번)
+
+
+def estimate_accords(expression, accords, *, top_n=ESTIMATOR_TOP_N,
+                     timeout=ESTIMATOR_TIMEOUT, prompt_path=ESTIMATOR_PROMPT_PATH,
+                     post=None):
+    """한국어 향 표현 하나를 accord 로 추정한다. dict 또는 None.
+
+    expression : str            사전이 못 받은 표현 하나
+    accords    : list[str]      고를 수 있는 accord 이름. 목록 밖은 버린다
+    top_n      : int            돌려줄 최대 개수. LLM 이 확신 순으로 낸다
+    post       : callable|None  네트워크를 대신할 함수. 검증 스크립트가 쓴다
+
+    반환 — `{"accords": [...], "reasoning": str}`. **엔진의 `estimator` 계약이다.**
+    향과 무관한 표현이면 `accords` 가 비고, 그때 엔진은 그 표현을 건너뛴다.
+
+    **실패하면 None 이다.** `structure()` 와 같은 관례다 — 예외를 올리지 않고 로그만
+    남긴다. 부른 쪽은 그 표현을 못 푼 것으로 두고 넘어가면 된다.
+
+    **이 함수는 `spec.md` 1장의 예외다.** 1장은 *"매핑은 사전이 하고 LLM 은 구조화만
+    한다"* 인데 여기서는 LLM 이 매핑을 한다. 근거와 한계는 `DECISIONS.md` N15 ·
+    측정 기록 20번에 있다. 그래서 결과에 `tier="LLM"` 이 붙고 `rationale` 은 비며
+    설명은 `llm_reasoning` 으로 따로 나간다 — 사전의 근거와 섞이지 않는다.
+    """
+    api_key = os.environ.get("GMS_API_KEY")
+    if not api_key:
+        LOGGER.warning("GMS_API_KEY 가 없어 ⑤단계 추정을 건너뛴다")
+        return None
+
+    body = (load_prompt(prompt_path)
+            .replace("%ACCORDS%", "\n".join(f"- {a}" for a in accords))
+            .replace("%EXPRESSION%", str(expression)))
+    payload = {"model": os.environ.get("GMS_MODEL", DEFAULT_MODEL),
+               "messages": [{"role": "user", "content": body}]}
+    endpoint = os.environ.get("GMS_ENDPOINT", DEFAULT_ENDPOINT)
+    send = post or _post_json
+
+    try:
+        response = send(endpoint, payload, api_key, timeout)
+        parsed = parse_content(response["choices"][0]["message"]["content"])
+        if not isinstance(parsed, dict):
+            raise Stage1Error("NOT_OBJECT", f"객체가 아니다: {type(parsed).__name__}")
+        allowed = set(accords)
+        got = [a for a in (parsed.get("accords") or []) if a in allowed][:top_n]
+        return {"accords": got, "reasoning": str(parsed.get("reasoning") or "")}
+    except urllib.error.HTTPError as exc:
+        LOGGER.error("⑤단계 추정 HTTP %s — 이 표현은 못 푼 것으로 둔다", exc.code)
+    except (urllib.error.URLError, TimeoutError, OSError) as exc:
+        LOGGER.error("⑤단계 추정 호출 실패 %s: %s", type(exc).__name__, exc)
+    except Stage1Error as exc:
+        LOGGER.error("⑤단계 추정 출력이 형식에 안 맞는다 %s: %s", exc.code, exc)
+    except (KeyError, IndexError, TypeError) as exc:
+        LOGGER.error("⑤단계 추정 응답에 choices[0].message.content 가 없다: %s", exc)
+    return None
+
+
+def make_estimator(index, **kwargs):
+    """엔진이 바로 넣을 수 있는 `estimator` 를 만든다. callable.
+
+        out = nlr_engine.recommend(index, text, structured,
+                                   estimator=llm_stage1.make_estimator(index))
+
+    accord 목록을 인덱스에서 꺼내 고정해 둔다. 엔진은 accord 이름을 모르는 함수를
+    원하지 않는다 — 목록 밖 값은 어차피 버려진다.
+    """
+    accords = list(index["accords"])
+    return lambda expression: estimate_accords(expression, accords, **kwargs)
diff --git a/ai/nlr_engine.py b/ai/nlr_engine.py
index 3430c14..76f3bfd 100644
--- a/ai/nlr_engine.py
+++ b/ai/nlr_engine.py
@@ -33,6 +33,7 @@ import json
 import logging
 import re
 import unicodedata
+from concurrent.futures import ThreadPoolExecutor
 from pathlib import Path
 
 import numpy as np
@@ -49,6 +50,13 @@ PEOPLE_MIN = 10           # [측정] 평가자 문턱. 커버리지 손실 0.3%p
 BRAND_CAP = 1             # [제안] 브랜드당 최대 개수
 MIN_CORE_FOR_AND = 2      # AND 를 쓸 최소 조건 수. 하나뿐이면 SINGLE_BROAD 로 간다
 
+# ⑤단계 미매칭 추정을 동시에 몇 개까지 부를지. 표현 하나당 호출 하나다.
+# [측정] 한 문장의 미매칭 표현은 중앙 2~3개 · 최대 7개였다(측정 기록 20번).
+# 순차로 부르면 문장당 중앙 2.3~3.5초가 붙어 실사용 2.1% 가 8초 예산을 넘었다.
+# GMS 한도는 분당 30,000요청(= 초당 500)이라 이 동시성은 1.4% 수준이다 [측정 2026-09-17].
+# 1 로 두면 순차로 돈다 — 결과는 같고 지연만 늘어난다.
+ESTIMATE_MAX_WORKERS = 8
+
 # ①단계 구조화 결과에서 이 엔진이 실제로 읽는 칸. 나머지(context·performance)는 안 읽는다 —
 # 계절 데이터가 0건이라 아직 검색에 못 쓴다(spec 6장 제약 1).
 STRUCTURED_KEYS = ("scent_preference", "avoid", "additional_requirements")
@@ -675,21 +683,45 @@ def recommend(index, text, structured=None, top_k=TOP_K, estimator=None):
     }
 
 
-def _apply_estimates(index, understood, estimator):
+def _apply_estimates(index, understood, estimator, max_workers=ESTIMATE_MAX_WORKERS):
     """사전이 못 받은 표현을 추정해 조건에 넣는다. dict[key, dict].
 
     등급은 `LLM` 이고 **`rationale` 은 비운다.** 사전의 `rationale` 은 사람이 판단한 뒤
     쓴 근거이고 LLM 이 붙인 설명은 사후 해석이다. 한 칸에 섞으면 나중에 구분할 수 없다
     (spec 1장). 추정 설명은 `llm_reasoning` 에 따로 담는다.
     """
+    items = understood["unmatched"]
+    if not items:
+        return {}
+
+    # **호출만 동시에 한다. 적용은 입력 순서 그대로다.**
+    # `ThreadPoolExecutor.map` 이 입력 순서를 지키므로 `guesses[i]` 가 `items[i]` 에
+    # 대응한다. 그래서 `core` 도 `evidence` 도 순차로 부를 때와 똑같이 나온다.
+    # **결과가 달라지면 이 함수가 잘못된 것이다** — 병렬은 "언제 부르나" 만 바꾼다.
+    #
+    # 예외를 삼키지 않는다. `llm_stage1` 의 추정 함수가 실패 시 `None` 을 돌려주므로
+    # 정상 경로에서는 예외가 올라오지 않는다. 그래도 올라오면 요청을 실패시킨다.
+    workers = min(max_workers, len(items))
+    if workers > 1:
+        with ThreadPoolExecutor(max_workers=workers) as pool:
+            guesses = list(pool.map(lambda it: estimator(it["expression"]), items))
+    else:
+        guesses = [estimator(it["expression"]) for it in items]
+
     estimate, core = {}, set(understood["core"])
-    for item in understood["unmatched"]:
-        guess = estimator(item["expression"]) or {}
-        accords = [a for a in (guess.get("accords") or []) if a in index["aidx"]]
-        if not accords:
+    for item, guess in zip(items, guesses):
+        # **`None` 은 호출 실패이고 `{"accords": []}` 는 "향이 아니다" 라는 판정이다.**
+        # 둘을 섞으면 네트워크가 한 번 끊긴 표현이 영영 "향이 아님" 으로 굳는다.
+        # 실패는 기록하지 않는다 — 다음 요청에서 다시 묻는다.
+        if guess is None:
             continue
+        accords = [a for a in (guess.get("accords") or []) if a in index["aidx"]]
+        # 빈 판정도 기록한다. 그래야 `unmatched_log` 가 캐시로 살려내고
+        # 재시작 뒤에도 같은 답이 된다 (전체 표현의 38% 가 여기 해당한다).
         estimate[item["normalized_key"]] = {"accords": accords,
                                             "reasoning": guess.get("reasoning")}
+        if not accords:
+            continue
         for accord in accords:
             if accord in core:
                 continue
diff --git a/ai/server.py b/ai/server.py
index 534687a..6815c11 100644
--- a/ai/server.py
+++ b/ai/server.py
@@ -66,6 +66,10 @@ _BODY_PARSE_MESSAGE = "요청 본문을 읽지 못했습니다. UTF-8 로 인코
 
 # 환경마다 다를 수 있는 값은 환경변수로 받는다. 컨테이너에서는 볼륨 경로를 넣는다.
 LOG_PATH = pathlib.Path(os.environ.get("NLR_LOG_PATH", unmatched_log.DEFAULT_LOG))
+
+# ⑤단계 미매칭 추정 스위치. 끄면 이 기능을 얹기 전과 똑같이 돈다.
+# 요청마다 GMS 호출이 붙을 수 있어(캐시에 없을 때만) 끌 수 있게 둔다.
+ESTIMATOR_ENABLED = os.environ.get("NLR_ESTIMATOR", "on").lower() not in ("0", "off", "false")
 LEXICON_PATH = os.environ.get("NLR_LEXICON_PATH") or None
 PERFUMES_PATH = os.environ.get("NLR_PERFUMES_PATH") or None
 
@@ -107,8 +111,24 @@ async def lifespan(app: FastAPI):
     LOGGER.info("①단계 LLM 구조화 %s (model=%s prompt=%s)",
                 "켜짐" if llm["enabled"] else "꺼짐 — GMS_API_KEY 가 없다",
                 llm["model"], llm["prompt_sha256"])
+
+    # ⑤단계 미매칭 추정. **조건이 하나도 안 잡혔을 때만** 돈다.
+    # `cached_estimator` 가 미매칭 로그를 캐시로 쓴다 — 같은 표현은 항상 같은 답이고
+    # 재시작해도 파일에서 다시 채운다. 캐시가 없으면 같은 문장에 매번 다른 향수가
+    # 나온다 [측정 2026-09-17 · 같은 설정으로 두 번 불러 5문장 중 5문장이 달랐다].
+    if ESTIMATOR_ENABLED and llm["enabled"]:
+        app.state.estimator = unmatched_log.cached_estimator(
+            llm_stage1.make_estimator(app.state.index), path=LOG_PATH)
+        LOGGER.info("⑤단계 미매칭 추정 켜짐 — 캐시 %d개를 %s 에서 읽었다",
+                    len(app.state.estimator.cache), LOG_PATH)
+    else:
+        app.state.estimator = None
+        LOGGER.info("⑤단계 미매칭 추정 꺼짐 (NLR_ESTIMATOR=%s · GMS 키 %s)",
+                    os.environ.get("NLR_ESTIMATOR", "on"),
+                    "있음" if llm["enabled"] else "없음")
     yield
     app.state.index = None
+    app.state.estimator = None
 
 
 app = FastAPI(title="향해 자연어 추천", lifespan=lifespan)
@@ -308,7 +328,8 @@ def recommend(body: RecommendRequest, request: Request):
     **인기 향수를 대신 보여주면 안 된다** — 추천처럼 보이지만 근거가 없다.
     """
     index = request.app.state.index
-    out = nlr_engine.recommend(index, body.text, _structure(body), top_k=body.topK)
+    out = nlr_engine.recommend(index, body.text, _structure(body), top_k=body.topK,
+                               estimator=request.app.state.estimator)
     _record_unmatched(out)
     return {
         "status": out["status"],
diff --git a/ai/unmatched_log.py b/ai/unmatched_log.py
index 149b523..d7967e8 100644
--- a/ai/unmatched_log.py
+++ b/ai/unmatched_log.py
@@ -179,7 +179,10 @@ def load_estimates(path=DEFAULT_LOG):
     """
     out = {}
     for row in read(path):
-        if row.get("llm_accords"):
+        # **`[]` 와 `null` 을 구분한다.** `[]` 는 "LLM 이 향이 아니라고 답했다" 이고
+        # `null` 은 "아직 묻지 않았다" 다. 빈 것도 캐시로 살려야 재시작 뒤에 같은
+        # 답이 나오고 같은 표현을 다시 묻지 않는다 (전체의 38%).
+        if row.get("llm_accords") is not None:
             out[row["normalized_key"]] = {"accords": row["llm_accords"],
                                           "reasoning": row.get("llm_reasoning")}
     return out
@@ -201,7 +204,11 @@ def cached_estimator(estimate, path=DEFAULT_LOG):
     def wrapped(expression):
         key = normalize_key(expression)
         if key not in cache:
-            guess = estimate(expression) or {}
+            guess = estimate(expression)
+            # **실패(`None`)를 캐시에 넣지 않는다.** 넣으면 네트워크가 한 번 끊긴
+            # 표현이 "향이 아님" 으로 굳어 영영 안 풀린다. 다음 요청에서 다시 묻는다.
+            if guess is None:
+                return None
             cache[key] = {"accords": list(guess.get("accords") or []),
                           "reasoning": guess.get("reasoning")}
         return cache[key]
