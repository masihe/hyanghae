diff --git a/ai/README.md b/ai/README.md
index 891184a..7e6cd36 100644
--- a/ai/README.md
+++ b/ai/README.md
@@ -124,7 +124,7 @@ unmatched_log.py   사전이 못 받은 표현을 기록합니다 (아래 참고
 build_data.py      원본 덤프에서 data/perfumes_nlr.csv.gz 를 다시 만듭니다
 data/
   perfumes_nlr.csv.gz        5.0MB  향수 129,161개 (id·이름·브랜드·accord·평가자수·평점)
-  domain_lexicon_v1_8.csv     24KB  한국어 향 표현 53행 -> accord
+  domain_lexicon_v1_14.csv    31KB  한국어 향 표현 63행 -> accord
   accord_dictionary.csv        7KB  accord 92개 마스터 (적재 시 검증용)
   note_accord_bridge.csv      95KB  노트(향 원료) 이름 -> accord 2,925행
   stage1_prompt_v1.txt        2KB   ①단계 구조화 프롬프트 (sha256 앞자리 4f0a368d)
@@ -150,7 +150,7 @@ curl http://localhost:8000/health
 ```
 
 `"llm"` 이 ①단계 LLM 구조화를 쓸 수 있는 상태인지 알려줍니다. `false` 면 `GMS_API_KEY`
-가 없는 것이고, 서버는 정상으로 돌지만 **조건을 뽑는 비율이 88.2% → 75.5% 로 떨어집니다.**
+가 없는 것이고, 서버는 정상으로 돌지만 **조건을 뽑는 비율이 88.8% → 76.8% 로 떨어집니다.**
 응답만 봐서는 구분이 안 되니 배포 후 이 칸을 확인하십시오. 기동 로그에도 남습니다.
 
 ```
@@ -210,7 +210,7 @@ Invoke-RestMethod -Uri "http://localhost:8000/nlr/recommend" -Method Post `
 | `GMS_MODEL` | `gpt-5.4-nano` | 모델을 바꿔 비교할 때 |
 | `GMS_ENDPOINT` | SSAFY GMS 게이트웨이 | — |
 | `NLR_LOG_PATH` | `ai/logs/unmatched.jsonl` | 컨테이너에서 볼륨 경로를 줄 때 |
-| `NLR_LEXICON_PATH` | `data/domain_lexicon_v1_8.csv` | 사전 버전을 바꿔 비교할 때 |
+| `NLR_LEXICON_PATH` | `data/domain_lexicon_v1_14.csv` | 사전 버전을 바꿔 비교할 때 |
 | `NLR_PERFUMES_PATH` | `data/perfumes_nlr.csv.gz` | — |
 
 **키를 코드나 파일에 적지 마십시오.** 환경변수로만 받습니다. 만료되면 HTTP 401 로
@@ -224,10 +224,10 @@ Invoke-RestMethod -Uri "http://localhost:8000/nlr/recommend" -Method Post `
 없음 -> llm_stage1.structure() 로 채웁니다. 실패하면 사전 매칭으로 내려갑니다
 ```
 
-| | 조건을 뽑는 비율 (합성 600문장 · 사전 v1.8) |
+| | 조건을 뽑는 비율 (합성 600문장 · 사전 v1.14) |
 |---|---|
-| 구조화 + 사전 | **529/600 = 88.2%** |
-| 사전만 | 453/600 = 75.5% |
+| 구조화 + 사전 | **533/600 = 88.8%** |
+| 사전만 | 461/600 = 76.8% |
 
 **`/nlr/understand` 를 부른 뒤 `/nlr/recommend` 를 이어 부를 거면 첫 응답의
 `structured` 를 두 번째 요청의 `structured` 에 넣으십시오.** 그래야 LLM 을 한 번만
@@ -337,22 +337,23 @@ python build_data.py --source /경로/perfumes.csv
 
 ## 지금 어느 정도 되나 — 실측
 
-합성 평가셋 600건 · **사전 v1.8 · 노트 브리지 켬** 기준, **결과가 나오는 비율**입니다.
+합성 평가셋 600건 · **사전 v1.14 · 노트 브리지 켬** 기준, **결과가 나오는 비율**입니다.
 
 | | 비율 | 검색 단계 |
 |---|---|---|
-| **LLM 구조화 + 사전** | **529/600 = 88.2%** | AND 412 · SINGLE_BROAD 75 · 완화 42 |
-| **사전만 (LLM 없이)** | **453/600 = 75.5%** | AND 286 · SINGLE_BROAD 164 · 완화 3 |
+| **LLM 구조화 + 사전** | **533/600 = 88.8%** | AND 417 · SINGLE_BROAD 73 · 완화 43 |
+| **사전만 (LLM 없이)** | **461/600 = 76.8%** | AND 295 · SINGLE_BROAD 163 · 완화 3 |
 
 조건을 하나라도 뽑으면 **결과가 0개인 경우는 없습니다.**
 
 **LLM 이 없어도 돌아가므로 API 가 죽어도 서비스를 내리지 마십시오.** 다만 사전만 쓰면
-`SINGLE_BROAD` 가 75 → 164 로 늘어납니다 — 향 조건이 하나뿐이라 **순위를 향이 아니라
+`SINGLE_BROAD` 가 73 → 163 으로 늘어납니다 — 향 조건이 하나뿐이라 **순위를 향이 아니라
 인기도가 정하는 상태**입니다. 비율뿐 아니라 이 분포도 같이 보십시오.
 
-> 이 표는 사전 v1.8 의 값입니다. 이전 판에는 v1.2 기준 `77.7% / 35.7%` 가 적혀 있었습니다.
-> 사전만 쓰는 쪽이 35.7% → 75.5% 로 오른 것은 LLM 이 아니라 **사전 확장의 몫**입니다
-> (`DECISIONS.md` N10).
+> 이 표는 사전 v1.14 의 값입니다. 이전 판에는 v1.2 기준 `77.7% / 35.7%`,
+> 그 뒤 v1.8 기준 `88.2% / 75.5%` 가 적혀 있었습니다.
+> 사전만 쓰는 쪽이 35.7% → 76.8% 로 오른 것은 LLM 이 아니라 **사전 확장의 몫**입니다
+> (`DECISIONS.md` N10). v1.8 → v1.14 의 몫은 작습니다 — 측정 기록 14번.
 
 ---
 
@@ -417,6 +418,6 @@ python build_data.py --source /경로/perfumes.csv
 | **실사용은 더 어렵습니다** | 설문 155건에서 **48.4% 가 조건 0개** 였습니다. 위 22.3% 보다 나쁩니다 |
 | **추천 이유 문장화** | 근거까지만 만듭니다. LLM 문장화는 미구현입니다 |
 | **한국어 형태소 분석** | 지금은 문자열 포함 매칭이라 표기 변형은 사전의 `aliases` 나열에 의존합니다 |
-| **사전 53행 중 37행이 `candidate`** | 서비스에 써도 되는지 팀 판정이 아직 없습니다. 나머지 16행은 `active` 입니다 |
+| **사전 63행 중 47행이 `candidate`** | 서비스에 써도 되는지 팀 판정이 아직 없습니다. 나머지 16행은 `active` 입니다 |
 
 설계와 측정 근거는 별도 분석 저장소에 있습니다 — 노트북 40여 개, 측정 기록 11건, 의사결정 기록 N1~N10.
diff --git a/ai/docs/nlr_api_contract.md b/ai/docs/nlr_api_contract.md
index 9275247..b10de3a 100644
--- a/ai/docs/nlr_api_contract.md
+++ b/ai/docs/nlr_api_contract.md
@@ -185,7 +185,7 @@ Content-Type: application/json
 > 넘기면 그대로 쓰고 LLM 을 부르지 않는다 — Spring 이 스스로 구조화하기로 바뀌어도
 > 요청 형식은 안 바뀐다.
 >
-> 사전 v1.8 기준 합성 600문장에서 조건을 뽑는 비율이 **75.5% → 88.2%** 로 오른다.
+> 사전 v1.14 기준 합성 600문장에서 조건을 뽑는 비율이 **76.8% → 88.8%** 로 오른다.
 > LLM 이 죽어도 사전 매칭으로 내려가므로 **서비스를 내리지 않는다.**
 
 ### 요청은 `structured`, 응답은 `conditions` — 다른 것이다
@@ -388,7 +388,7 @@ reasons  [ { phrase:"빨래", evidence: ??? } ]
 | **프론트엔드** | `features/recommend/api.ts` 의 목 함수를 실제 호출로 바꾼다. `status` 4종 분기를 만든다. 타입에서 `x`·`y` 를 뺀다 |
 | **백엔드** | `POST /api/v1/recommendations/natural-language` 를 만든다. AI 서버 클라이언트를 붙인다. 위 「백엔드가 하는 변환」 3단계를 구현한다. 호출량 제한을 건다 |
 | **AI** | ~~`POST /nlr/understand` · `/nlr/recommend` 를 만든다~~ (S15P21E203-202 에서 완료). ~~LLM 구조화를 붙인다~~ (완료. `llm_stage1.py`). 미매칭 추정(⑤단계)을 붙인다. 향 특성 92개에 한국어 이름을 단다 |
-| **인프라** | AI 서버를 배포에 태운다. 백엔드가 쓸 AI 서버 주소를 환경변수로 넣는다. **AI 서버에도 `GMS_API_KEY` 를 넣는다** — 없으면 서버는 뜨지만 조건을 뽑는 비율이 88.2% → 75.5% 로 떨어진다 |
+| **인프라** | AI 서버를 배포에 태운다. 백엔드가 쓸 AI 서버 주소를 환경변수로 넣는다. **AI 서버에도 `GMS_API_KEY` 를 넣는다** — 없으면 서버는 뜨지만 조건을 뽑는 비율이 88.8% → 76.8% 로 떨어진다 |
 
 ---
 
diff --git a/ai/docs/search_rules.md b/ai/docs/search_rules.md
index 113dc89..af7dd64 100644
--- a/ai/docs/search_rules.md
+++ b/ai/docs/search_rules.md
@@ -34,7 +34,7 @@
 
 ## ① 조건 추출
 
-`domain_lexicon_v1_8.csv` 53행에서 **문장에 들어 있는 표현**을 찾습니다.
+`domain_lexicon_v1_14.csv` 63행에서 **문장에 들어 있는 표현**을 찾습니다.
 
 ```
 "빨래 냄새 나는 향수 찾고 있어"
@@ -70,8 +70,8 @@ scent_preference: ["flower"] ->   (표에 없습니다. 노트가 아니라 형
 문턱 `1.4` 는 `DECISIONS.md` N4 의 통제군 값입니다. `load_index(bridge_lift_min=)` 으로
 바꿀 수 있습니다.
 
-합성 600문장에서 조건을 뽑는 비율이 **86.7% → 88.2%** 로 오르고, 조건이 하나뿐인
-`SINGLE_BROAD` 가 **91 → 75** 로 줍니다 (`DECISIONS.md` N14).
+합성 600문장에서 조건을 뽑는 비율이 **87.5% → 88.8%** 로 오르고, 조건이 하나뿐인
+`SINGLE_BROAD` 가 **89 → 73** 으로 줍니다 (`DECISIONS.md` N14 · 사전 v1.14 기준).
 
 **`ai/data/note_accord_bridge.csv` 가 없으면** 기동 로그에 경고가 남고 이 단계를
 건너뜁니다. 서버는 정상으로 돕니다.
@@ -328,7 +328,7 @@ min_results = min(min_results, top_k)
 |---|---|
 | **장면 표현 3개 불가** | `데이트`·`섹시한`·`호텔 라운지` — 사전에 매핑이 없습니다 |
 | **실사용은 더 어렵습니다** | 설문 155건에서 **48.4%가 조건 0개** 였습니다. 위 22.3% 보다 나쁩니다 |
-| **사전 53행 중 37행이 `candidate`** | 서비스에 써도 되는지 팀 판정이 아직 없습니다. 나머지 16행은 `active` 입니다 |
+| **사전 63행 중 47행이 `candidate`** | 서비스에 써도 되는지 팀 판정이 아직 없습니다. 나머지 16행은 `active` 입니다 |
 | **점수 식 미검증** | `strength` 단순 합입니다. 쿼리→향수 점수 식은 검증된 적이 없습니다 |
 | **완화 시 동점이 큽니다** | 중앙값 3,204개. 향 조건이 순위를 거의 못 정합니다 |
 | **한국어 형태소 분석 없음** | `kiwipiepy` 미설치. 지금은 순수 문자열 포함 매칭이라 표기 변형은 `aliases` 나열에 의존합니다 |
diff --git a/ai/llm_stage1.py b/ai/llm_stage1.py
index 5adf236..d158f24 100644
--- a/ai/llm_stage1.py
+++ b/ai/llm_stage1.py
@@ -11,10 +11,10 @@
 그래야 DB 도 인터넷도 없이 `python nlr_engine.py "빨래"` 로 검증할 수 있고,
 여기가 죽어도 사전 매칭으로 서비스가 돈다.
 
-왜 필요한가 — 실측 (합성 평가셋 600문장, 사전 v1.8)
+왜 필요한가 — 실측 (합성 평가셋 600문장, 사전 v1.14)
 ---------------------------------------------------
-    구조화 없이 사전만    453/600 = 75.5%   <- 지금까지의 서버
-    구조화 + 사전         529/600 = 88.2%   <- 노트 브리지 포함
+    구조화 없이 사전만    461/600 = 76.8%   <- 지금까지의 서버
+    구조화 + 사전         533/600 = 88.8%   <- 노트 브리지 포함
 
 LLM 은 **뽑기만 한다. 해석은 사전이 한다.** 프롬프트가 *"이미지·감각 표현(깨끗한·포근한·
 차가운)을 임의의 향으로 바꾸지 마세요"* 로 그 경계를 긋고 있다. 이 제약을 풀면 같은 뜻에
diff --git a/ai/nlr_engine.py b/ai/nlr_engine.py
index 9dd1794..5f530ef 100644
--- a/ai/nlr_engine.py
+++ b/ai/nlr_engine.py
@@ -16,7 +16,7 @@
     python nlr_engine.py "빨래 냄새 나는 향수 찾고 있어"
 
 ①단계 구조화 결과가 있으면 `structured` 로 넘긴다. 없으면 사전 문자열 매칭만 쓴다.
-사전 v1.8 기준 합성 600문장에서 조건을 뽑는 비율이 **88.2% -> 75.5%** 로 떨어지지만
+사전 v1.14 기준 합성 600문장에서 조건을 뽑는 비율이 **88.8% -> 76.8%** 로 떨어지지만
 서비스는 죽지 않는다.
 
 **입력은 `structured`, 출력은 `conditions` 다.** 이름을 나눈 이유는 모양이 달라서다 —
@@ -56,7 +56,7 @@ STRUCTURED_KEYS = ("scent_preference", "avoid", "additional_requirements")
 # 기본 데이터 경로. 인자로 덮어쓸 수 있다.
 _DATA_DIR = Path(__file__).resolve().parent / "data"
 DEFAULT_PERFUMES = _DATA_DIR / "perfumes_nlr.csv.gz"
-DEFAULT_LEXICON = _DATA_DIR / "domain_lexicon_v1_8.csv"
+DEFAULT_LEXICON = _DATA_DIR / "domain_lexicon_v1_14.csv"
 DEFAULT_ACCORDS = _DATA_DIR / "accord_dictionary.csv"
 DEFAULT_NOTE_BRIDGE = _DATA_DIR / "note_accord_bridge.csv"
 
@@ -99,7 +99,7 @@ def load_index(perfumes_csv=None, lexicon_csv=None, accord_csv=None, verify=True
     """향수 데이터와 사전을 읽어 조회용 인덱스를 만든다.
 
     perfumes_csv : str | Path | None   기본 data/perfumes_nlr.csv.gz
-    lexicon_csv  : str | Path | None   기본 data/domain_lexicon_v1_8.csv
+    lexicon_csv  : str | Path | None   기본 data/domain_lexicon_v1_14.csv
     accord_csv   : str | Path | None   기본 data/accord_dictionary.csv (accord 92개 마스터)
     verify       : bool                accord 보유 수를 마스터와 대조해 틀리면 RuntimeError
     bridge_csv   : str | Path | None   기본 data/note_accord_bridge.csv (노트→accord)
diff --git a/ai/server.py b/ai/server.py
index 534687a..721ffeb 100644
--- a/ai/server.py
+++ b/ai/server.py
@@ -23,7 +23,7 @@ Spring 이 호출하는 내부 API 다. **외부에 열지 않는다** — 호
     없음 -> llm_stage1.structure()   실패하면 None 이라 사전 매칭으로 내려간다
 
 `GMS_API_KEY` 가 없으면 구조화를 건너뛰고 **서버는 그대로 뜬다.** 사전 매칭만으로도
-합성 600문장의 75.5% 는 조건을 뽑는다(사전 v1.8). 키가 있으면 88.2% 다.
+합성 600문장의 76.8% 는 조건을 뽑는다(사전 v1.14). 키가 있으면 88.8% 다.
 
 이번 범위가 아닌 것
 ------------------
@@ -97,7 +97,7 @@ async def lifespan(app: FastAPI):
     추천 한 번은 4ms 다. 적재에 실패하면 기동을 중단한다 — 반쯤 뜬 서버가 더 나쁘다.
 
     **LLM 구조화는 실패해도 기동을 막지 않는다.** 키가 없으면 사전 매칭으로 돈다.
-    어느 쪽인지 로그로 남긴다 — 배포된 서버가 75.5% 로 도는지 88.2% 로 도는지는
+    어느 쪽인지 로그로 남긴다 — 배포된 서버가 76.8% 로 도는지 88.8% 로 도는지는
     응답만 봐서는 알 수 없다.
     """
     logging.basicConfig(level=logging.INFO)   # 이미 설정돼 있으면 아무 일도 하지 않는다
